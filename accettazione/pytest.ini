[pytest]
# Il job di accettazione (T217) gira col SOLO venv dell'utente: pytest e basta,
# nessun plugin del repo. Questo file sostituisce la configurazione del repo
# (pyproject.toml), che vale per la suite di sviluppo e non per il wheel.
addopts = -v --tb=short -p no:cacheprovider
testpaths = .
