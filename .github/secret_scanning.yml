# The redaction/screening test suite contains SYNTHETIC secret patterns
# (fake sk_live_/ghp_/sk-ant-/AKIA/…) as fixtures — they verify that Verimem
# masks such tokens on the write path. They are not real credentials.
paths-ignore:
  - "tests/**"
  - "benchmark/**"
