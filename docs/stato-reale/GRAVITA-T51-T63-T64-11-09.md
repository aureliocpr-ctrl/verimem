# Gravità di T51 (con orologio), T63 e T64 — ws7 «Iris» (11/09, in lettura)

> Chiesto dal lead nel post «START 11/09 19:20». **Sono LETTRICE**: questa pagina è
> scritta leggendo, **zero esecuzioni sulla macchina**. Dove serve un numero che non
> ho, c'è una riga `Q:` da mettere in coda a un operatore, non una stima.

## 0. Il criterio, invariato dal 09/09

Tre domande — **blocca un percorso d'uso? · blocca la promessa centrale? · l'utente
se ne accorge da solo?** — più l'ordinatore fra due P0: **un difetto che SCRIVE batte
un difetto che LEGGE**. Continua
[GRAVITA-T55-T59-10-09.md](GRAVITA-T55-T59-10-09.md).

## 1. T51 CON OROLOGIO — **P1 che diventa P0 a scadenza**, ed è una forma nuova

**Che cosa ho misurato io** (`7957152e`, ieri): la maniglia `superseded_undo_ops`
**c'è** nella ricevuta di `add()`, ed è una mappa `fact_id → op_id` non vuota.

**Che cosa NON ho misurato e cito da @Aldo**: il **TTL di 168 ore**, i **530 ritiri
same-source** e i **62 ancora riparabili**. Non li ho verificati e non li adotto.

⚠️ **LA FORMA, e non l'avevamo mai nominata: UNA RIPARABILITÀ CHE SCADE.**

T51 è P1 perché «l'undo ripara». Ma se la maniglia muore dopo 168 ore, quella
motivazione **ha una data di scadenza**: il ticket è

    P1  per i fatti ancora dentro la finestra
    P0  RETROATTIVAMENTE, e per sempre, per ogni fatto che esce dalla finestra

Non è una gravità sola: è una gravità **che cambia da sé mentre nessuno tocca
niente**. Tutte le altre righe delle mie tabelle stanno ferme finché qualcuno non
scrive codice; questa peggiora **al passare del tempo**, e peggiora in silenzio.

⇒ **Conseguenza sull'ordine della coda, e la scrivo perché il lead me l'ha chiesta**:
il ripristino dei 62 **non va in coda dietro le cure**. Una cura rimandata di un
giorno costa un giorno; **62 fatti rimandati di un giorno possono costare 62 fatti.**
È l'unico elemento aperto che ha un orologio, e per questo va prima — non perché sia
il più grave in assoluto, ma perché è **l'unico che scade**.

**Che cosa manca per agire, e non ce l'ho io**: il piano di @Aldo (T4 nella coda di
Tara) con il comando della porta del prodotto e i conteggi prima/dopo. Fino a quel
piano nessuno deve ripristinare niente a mano.

## 2. T63 — **P1 per il prodotto, e il nostro incidente di ieri È il suo caso d'uso**

**I numeri, e sono due**: il lead scrive **~1,9 GB** per processo (START 11/09), il
messaggio che mi ha svegliata dice **2,4 GB**; ieri il verbale diceva **22,8 GB in
12 server**. Non scelgo: sono perimetri o istanti diversi, e **la misura è di Tara**
(T3 nella sua coda). Qui uso solo l'ordine di grandezza — *qualche GB per processo* —
che è quello che decide la gravità.

**Perché non è un problema nostro di macchina.** Il README, righe 600-604, descrive
il caso d'uso **alla lettera**:

> «Several local agents — Claude Code windows, Cursor, a cron job — should share ONE
> memory, **not each spin up a model-loading store that fights the same SQLite
> file**. Point them at a running server and they become **thin clients**: no model
> load, just HTTP.»

⇒ **Il prodotto ha già la cura, e si chiama thin client.** Ma vale **solo** se
l'utente imposta `VERIMEM_SERVER_URL`. **Senza quella variabile, ogni agente carica
il proprio modello**, ed è esattamente ciò che il README dice di non fare.

🔑 **E qui sta la gravità vera, che non è tecnica: il COSTO DI NON CONFIGURARLO NON È
DICHIARATO DA NESSUNA PARTE.** Il README spiega come stare bene (punta al server) e
non dice che cosa succede se non lo fai. Un utente che apre dieci finestre di Claude
Code — il primo esempio della riga 600 — paga **dieci volte** quel costo e lo scopre
quando la macchina si ferma.

⚠️ **E noi siamo quell'utente.** Ieri: bugcheck 0x7E alle 19:39, commit di memoria al
96,7 %, **22,8 GB in 12 server MCP**. Non è un aneddoto interno: è il caso d'uso del
README, eseguito da noi, finito in un crash. **Il dogfooding ha già prodotto il
verdetto e l'abbiamo letto come un problema di macchina.**

| 1. percorso | 2. promessa | 3. se ne accorge | **gravità** |
|---|---|---|---|
| **sì**: apri dieci agenti e la macchina si ferma | in parte: il README indica la via buona e tace sul costo dell'altra | **sì, ma tardi e senza capire perché** | **P1** |

**P1 e non P0** perché l'utente **se ne accorge** (la macchina rallenta, poi cade) e
perché la via buona **esiste ed è documentata**. Ma è un P1 col dito puntato: il
difetto non è il consumo, è il **silenzio sul consumo**.

**La cura a costo zero, prima di qualunque ottimizzazione** — ed è mia, è una riga di
README: dire accanto a quella frase **quanto pesa un agente che NON punta al
server**, con il numero di Tara. Chi legge sceglie sapendo. L'ottimizzazione (torch e
giudice caricati solo quando servono) è un'altra cosa e viene dopo.

    Q: (per @Tara, T3) — nella tua misura, oltre a «cosa pesa», serve UN numero
       per il README: quanti MB tiene un processo `engram mcp` APPENA AVVIATO,
       senza nessuna richiesta servita, con e senza VERIMEM_SERVER_URL.
       atteso: due numeri in MB · per: la riga di README di T63

## 3. T64 — **confermo P0, e sopra T49**. La gravità non cambia, il numero sì

T64 è il reperto di @Giano di ieri sera, che ho già classificato in
[GRAVITA-T55-T59-10-09.md §5](GRAVITA-T55-T59-10-09.md) prima che avesse un numero:
**la deny-list del thin mode protegge solo mentre il server condiviso è VIVO**,
perché la guardia è `... and _remote() is not None` (`mcp_server.py:7939`) e
`_remote()` cade fail-soft quando il server non risponde (`:202`).

**Confermo P0 e sopra T49.** La ragione, in una riga: **la difesa si spegne
esattamente quando serve di più.** A server vivo lo store locale è secondario; a
server **giù** l'utente è già nella situazione peggiore, ed è proprio allora che la
guardia smette di dirglielo e lascia che un `forget` «cancelli» dal locale vuoto
rispondendo `ok`. Sta **sopra «nessuna difesa»**: una difesa assente lascia l'utente
prudente, una che si disattiva da sola gli fa abbassare la guardia nel momento
sbagliato.

🔗 **E T63 e T64 si toccano, il che è il motivo per cui li ho messi nella stessa
pagina.** T63 spinge l'utente verso il server condiviso (è la via che costa meno
memoria). T64 dice che **proprio in quella topologia**, quando il server non
risponde, le protezioni si spengono. **Più utenti seguono il consiglio del README,
più T64 conta.** Curare T63 senza T64 aumenta la popolazione esposta.

**Che cosa manca**: la misura di @Giano (lui l'ha dichiarata NON MISURATA). La prova
che basta è una sola:

    Q: (per l'operatore che tocca il thin mode) — un tool della deny-list
       invocato con VERIMEM_SERVER_URL puntata a una PORTA MORTA.
       atteso: se rende ok/removed invece del rifiuto, T64 è misurato
       per: T64

## 4. L'ordine che propongo, se il lead lo vuole

Il lead mi ha chiesto di dire se vedo qualcosa di più grave dell'ordine della coda.
**Non ribalto la coda**: T38 è la cura che riapre la finestra e viene prima di
tutto. Ma segnalo **una sola inversione**, e per una ragione di orologio, non di
gravità:

> **il ripristino dei 62 di T51 non va in fondo.** È l'unico elemento che scade. Se
> la finestra si riapre domani e il ripristino slitta, il conto lo pagano dei fatti
> che oggi si potevano ancora salvare.

Tutto il resto dell'ordine mi sta bene com'è.
