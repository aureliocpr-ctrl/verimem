# Mappa di `verimem/analogy.py` — 4 righe, 182 righe di codice (lead, 09/09 01:03)

Letto per intero. Prova: pytest del lotto sul tip `20257636` (`51 passed in 17.79s`, EXIT=0, con `tests/test_structural_analogy.py` e `tests/test_un_analogia_che_non_puo_esistere_lo_dice.py`). Chiamanti letti: `verimem/mcp_server.py:12434-12450` (`hippo_find_analogues`, pool da `a.skills.all(...)`), `verimem/reasoning.py:25`, `verimem/skill_inspect.py:11`. Claim README: nessuna riga (grep su «analog» → nessuna). Il docstring porta la misura del 31/07 sul corpus reale (325 skill, 20.000 coppie): **il coseno semantico fra due skill qualunque ha minimo 0,7425 e le coppie sotto il default `max_semantic=0,5` sono ZERO**, perché firma strutturale ed embedding si calcolano dallo stesso testo (r=0,7503). La cura scelta allora: restituire gli estremi osservati nel `report`, non una soglia nuova.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/analogy.py:46` `_tokens` | token `[a-z0-9]+` sul testo in minuscolo (spezza su `_` e `-`, come i nomi dei predicati) | `structural_signature` (66) | `tests/test_structural_analogy.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 2 | `verimem/analogy.py:53` `structural_signature` | il sacchetto di token di nome + trigger + pre + post | `find_structural_analogues` (144, 154) | `tests/test_structural_analogy.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 3 | `verimem/analogy.py:69` `structural_jaccard` | Jaccard; 0 se ENTRAMBI vuoti (un 1,0 «tutto uguale a tutto» ingannerebbe) | `find_structural_analogues` (155) | `tests/test_structural_analogy.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 4 | `verimem/analogy.py:81` `find_structural_analogues` | le skill con struttura ≥ 0,4 e coseno ≤ 0,5 (il regime di Gentner: stessa forma, dominio diverso), top-5, e nel `report` gli estremi osservati per distinguere «nessuna analogia» da «vincoli irraggiungibili» | `verimem/mcp_server.py:12434` (`hippo_find_analogues`), `reasoning.py:25`, `skill_inspect.py:11` | `tests/test_structural_analogy.py`, `tests/test_un_analogia_che_non_puo_esistere_lo_dice.py` | - | FUNZIONA COME PROMESSO (con i default, sul corpus reale, il risultato è vuoto per costruzione: misurato il 31/07 e dichiarato nel payload) | pytest 51 passed |

Reperti: (a) **una capacità che sul corpus vero non produce mai un risultato con i default** (zero coppie sotto 0,5 su 20.000): il tool dice onestamente perché, ma «analogia strutturale» resta una voce dell'elenco dei 249 strumenti che nessun utente vedrà funzionare; la scelta fra cambiare la misura (firma su un testo DIVERSO da quello dell'embedding) e ritirare il tool è collegiale, non presa; (b) il costo del coseno è pagato solo per chi passa il filtro strutturale (dichiarato); (c) V1 «intentionally simple», SME rimandato a ≥ 1k skill: siamo a 325. Nessun P0.
