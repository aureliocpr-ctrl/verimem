# Mappa di `verimem/entity_populate.py` — 3 righe, 175 righe di codice (lead, 09/09 02:48)

Letto per intero. Prova: pytest del lotto E sul tip `20257636` (`233 passed in 145.12s`, EXIT=0, con `tests/test_entity_populate.py`, `tests/test_entity_live_write_path.py`, `tests/test_entity_live_latency.py`, `tests/test_flow_entity_events.py`, `tests/test_fact_delete_cascade_r3.py`). Chiamanti letti: `entity_kg_path_for` ← `semantic.py:4759,5457`, `gateway.py:1442`, `conversation_ingest.py:369`, `tier_inventory.py:90`, due banchi; `populate_entities_for_fact` ← `semantic.py:3470` (la scrittura viva: il fatto entra e il grafo si aggiorna), `conversation_ingest.py:423`; `populate_entity_graph` ← `semantic.py:3459`, `scripts/backfill_entity_graph.py:17`. Claim README: la riga 282 («pipeline as explicit writes (extraction → gate → provenance)») descrive l'ingest delle conversazioni, non questo modulo.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/entity_populate.py:32` `entity_kg_path_for` | `<data_dir>/entity_kg/entity_kg.db` come fratello di `semantic/semantic.db` (un DB temporaneo deriva un KG temporaneo: i test restano ermetici) | `semantic.py:4759,5457`, `gateway.py:1442`, `conversation_ingest.py:369`, `tier_inventory.py:90` | `tests/test_entity_live_write_path.py`, `tests/test_fact_delete_cascade_r3.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 2 | `verimem/entity_populate.py:46` `populate_entities_for_fact` | per UN fatto: estrai (o usa le entità già estratte) → upsert → link → clique di co-occorrenza fra le prime 8 (≤ 28 archi ×2 versi), tutto in UNA sessione del KG (prima ~76 aperture per fatto, 122 ms/store); evento `flow.entity` con i nodi nati e toccati | `verimem/semantic.py:3470`, `conversation_ingest.py:423`, `populate_entity_graph` (149) | `tests/test_entity_live_write_path.py`, `tests/test_entity_live_latency.py`, `tests/test_flow_entity_events.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 3 | `verimem/entity_populate.py:105` `populate_entity_graph` | il backfill sui fatti vivi non `orphaned/quarantined` (DB aperto in sola lettura), un fatto malformato non ferma il giro; statistiche con `entities_total` ed `elapsed_s` | `verimem/semantic.py:3459`, `scripts/backfill_entity_graph.py` | `tests/test_entity_populate.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |

Reperti: (a) `statuses_excluded=("orphaned","quarantined")` è la **QUARTA copia** del filtro degli status nascosti (con `_STATUS_RANK`, `list_facts(hide_low_trust)`, `rank_list_builders`), e anche qui manca `user_belief`: i fatti `user_belief` entrano nel grafo su cui il PPR cammina; (b) `except Exception: continue` nel backfill nasconde quanti fatti sono caduti: le statistiche contano i riusciti, non i falliti (classe «l'assenza non ha un canale»). Nessun P0.
