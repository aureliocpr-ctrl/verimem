# Mappa di `verimem/fact_contract.py` — 3 righe, 146 righe di codice (lead, 09/09 02:50)

Letto per intero. Prova: pytest del lotto E sul tip `20257636` (`233 passed in 145.12s`, EXIT=0, con `tests/test_fact_ha_un_contratto_di_uscita.py`, `tests/test_il_contratto_non_promette_una_verifica.py`, `tests/test_il_payload_non_paga_i_default.py` (+5)). Chiamanti letti: `fact_payload` ← `mcp_server.py:72`, `semantic.py:1695` (`Fact.as_payload`), `briefing.py:21`, `fact_priority.py:11`, `facts_by_confidence.py:10`, `facts_export.py:9`, `facts_recent.py:9`, `facts_topics.py:11`, `trust_score.py:18` (+ altri: 30 punti); `verifica_sostenuta` ← `trust_report.py:80`. È **la superficie unica** del fatto in uscita, nata il 30/07 da una misura: 13 punti in `mcp_server.py` costruivano il dict a mano e 7 su 13 dimenticavano il verdetto del moat. Claim README: la riga 232 («Provenance on every read — answers cite where each fact came from») poggia su `verified_by` sempre presente (`SEMPRE`), e la distinzione «chiave assente = la superficie non lo espone / `null` = il moat non ha girato» è quella che le istruzioni del server vendono (`grounding_score`).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/fact_contract.py:51` `verifica_sostenuta` | vero solo se `last_verified_at` poggia su un `grounding_score` (misurato il 07/08: il campo avanzava su 2.762 fatti, ZERO con verdetto: un tocco, non una verifica); una definizione per due superfici | `fact_payload` (138), `verimem/trust_report.py:80` | `tests/test_il_contratto_non_promette_una_verifica.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 2 | `verimem/fact_contract.py:73` `_nomi_campi` | i nomi dal dataclass `Fact` meno `NON_ESCONO`, calcolati una volta (import pigro contro il ciclo con `semantic`) | `fact_payload` (106) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 233 passed |
| 3 | `verimem/fact_contract.py:88` `fact_payload` | il fatto in uscita da QUALUNQUE oggetto fatto-simile: nomi dal dataclass, valori da `getattr`, vuoti omessi tranne `SEMPRE` (id, proposition, topic, confidence, created_at, status, grounding_score, verified_by); `last_verified_at` solo se sostenuto da un verdetto e diverso da `created_at`; `writer_role` omesso se è il default | 30 punti del prodotto (sopra) | `tests/test_fact_ha_un_contratto_di_uscita.py`, `tests/test_il_payload_non_paga_i_default.py` | README:232 (provenienza su ogni lettura) | FUNZIONA COME PROMESSO | pytest 233 passed |

Reperti: (a) il modulo è l'esempio riuscito della classe ① curata: **una superficie invece di 47 copie**, e il commento racconta anche la cura sbagliata una volta (la regola nuova aveva SOSTITUITO la vecchia, presa dal test dell'autore); (b) `grounding_score` sempre presente ma il **verdetto** (`moat`, `disposition`) non è un campo di `Fact`: chi legge un fatto vede il numero e non la soglia con cui è stato giudicato (la soglia viaggia nella ricevuta di scrittura, non nel fatto) — da collegare alla mappa di `anti_confab_gate` (ws1). Nessun P0.
