# Mappa di `verimem/semantic_conflict.py` — 14 righe, 198 righe di codice (lead, 08/09 21:45)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_semantic_conflict.py tests/test_gate_semantic_conflict_wire.py tests/test_nli_auto_enable.py tests/test_agent_reconcile_nli_wiring.py tests/test_reconcile_judge_env_wiring.py` → `36 passed, 2 warnings in 15.97s`, EXIT=0. Chiamanti letti: `verimem/anti_confab_gate.py:2202-2262` (a livello `full`, se il modo non è `off`, il gate chiama `detect_semantic_conflicts` sui fratelli del fatto nuovo con il giudice LLM se c'è, altrimenti il giudice NLI locale; `observe` avvisa, `enforce` quarantena; stessa fonte più recente = evoluzione, non conflitto), `verimem/agent.py:36-44` (il giudice di riconciliazione da variabile d'ambiente: `local` → `LocalRelationJudge`, valore vero → `LLMRelationJudge`), `verimem/justified_memory.py:244` (`collect_contradicted_ids` riusa `detect_semantic_conflicts`). Claim README: righe 188-193 («… costs nothing; `VERIMEM_SEMANTIC_CONFLICT=0` opts out explicitly. A cross-source clash quarantines the new instead — one source never retires another's fact …»). La docstring dichiara il gap misurato che lo giustifica (6 conflitti su 6 mancati dallo stack lessicale, `benchmark/semantic_conflict_bench.py`) e la scelta fail-safe: un verdetto illeggibile è NEUTRAL.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/semantic_conflict.py:41` `Relation` | le tre relazioni: contraddizione, implicazione, neutro | tutto il file, il gate (2258-2259), `justified_memory` | `tests/test_semantic_conflict.py` (tutti) | - | FUNZIONA COME PROMESSO | pytest 36 passed |
| 2 | `verimem/semantic_conflict.py:64` `_fact_ts` | la data «quando asserito» di un fatto come stringa ordinabile (`created_at`, poi `ts`, `timestamp`; gli epoch diventano ISO; None se manca) | `_stamp` (90) | `::test_timestamp_is_passed_to_judge_when_present` | - | FUNZIONA COME PROMESSO | pytest 36 passed |
| 3 | `verimem/semantic_conflict.py:86` `_stamp` | prefissa `[data]` alla proposizione così il giudice distingue l'evoluzione nel tempo da un conflitto allo stesso tempo (HaluMem FPR 0,10 → 0,0125, misura del 20/06) | `detect_semantic_conflicts` (171, 178) | `::test_timestamp_is_passed_to_judge_when_present`, `::test_nli_system_has_temporal_supersession_rule` | - | FUNZIONA COME PROMESSO (il numero 0,10 → 0,0125 è un banco del 20/06, non un test) | pytest 36 passed |
| 4 | `verimem/semantic_conflict.py:100` `parse_relation` | la risposta del giudice → relazione dalla PRIMA parola; vuoto, prolisso o negato («no contradiction») → NEUTRAL, mai una contraddizione inventata | `LLMRelationJudge.classify` (142) | `::test_parse_relation_compliant_and_failsafe` | - | FUNZIONA COME PROMESSO | pytest 36 passed |
| 5 | `verimem/semantic_conflict.py:118` `build_nli_prompt` | `(system, messages)` per il giudice, puro; il system contiene la regola temporale | `LLMRelationJudge.classify` (137) | `::test_build_nli_prompt_contains_both_and_labels`, `::test_nli_system_has_temporal_supersession_rule` | - | FUNZIONA COME PROMESSO | pytest 36 passed |
| 6 | `verimem/semantic_conflict.py:124` `RelationJudge` | il protocollo: `classify(a, b) -> Relation` | `detect_semantic_conflicts` (tipo), `local_relation.LocalRelationJudge` (ws4) | via tutti i test con giudici finti | - | FUNZIONA COME PROMESSO | pytest 36 passed |
| 7 | `verimem/semantic_conflict.py:125` `RelationJudge.classify` | la firma del protocollo | - | - | - | FUNZIONA COME PROMESSO (dichiarazione di protocollo, nessun corpo) | letto |
| 8 | `verimem/semantic_conflict.py:128` `LLMRelationJudge` | NLI via un LLM iniettato (in produzione `verimem.llm.ClaudeCLILLM`: abbonamento, nessuna chiave esterna) | `verimem/anti_confab_gate.py:2211,2215`, `verimem/agent.py:40-41` | `tests/test_gate_semantic_conflict_wire.py::test_on_contradiction_escalates_to_downgrade`, `tests/test_reconcile_judge_env_wiring.py::test_truthy_value_wires_llm_judge` | `README.md:188` | FUNZIONA COME PROMESSO | pytest 36 passed |
| 9 | `verimem/semantic_conflict.py:132` `LLMRelationJudge.__init__` | tiene llm e modello | come sopra | come sopra | - | FUNZIONA COME PROMESSO (plumbing) | pytest 36 passed |
| 10 | `verimem/semantic_conflict.py:136` `LLMRelationJudge.classify` | un `complete` di 8 token; un errore del giudice → NEUTRAL, mai un crash della scrittura | il gate (2258) | i test del gate con llm finto; il ramo dell'eccezione non ha un test con il nome (letto: `except Exception → NEUTRAL`) | - | FUNZIONA COME PROMESSO (il ramo d'errore: letto, NON MISURATO) | pytest 36 passed |
| 11 | `verimem/semantic_conflict.py:145` `FixedRelationJudge` | risponde sempre `rel` (test e default no-op) | nessuno nel prodotto (`git grep -n FixedRelationJudge -- verimem/` → solo qui); i test | `tests/test_semantic_conflict.py`, `tests/test_justified_memory_bridge.py` | - | MAI CHIAMATA (candidata: utilità dei test dentro il prodotto, dichiarata tale nel docstring) | comando sopra |
| 12 | `verimem/semantic_conflict.py:148` `FixedRelationJudge.__init__` | tiene `rel` | i test | i test | - | MAI CHIAMATA (come sopra) | comando sopra |
| 13 | `verimem/semantic_conflict.py:151` `FixedRelationJudge.classify` | rende `rel` | i test | i test | - | MAI CHIAMATA (come sopra) | comando sopra |
| 14 | `verimem/semantic_conflict.py:155` `detect_semantic_conflicts` | due stadi: prefiltro coseno ≥ `min_cosine` (il giudice costoso solo sui candidati), poi il giudice: CONTRADICTION → avviso `semantic_conflict`, ENTAILMENT → `semantic_duplicate`, NEUTRAL → niente; puro, nessuna mutazione | `verimem/anti_confab_gate.py:2258`, `verimem/justified_memory.py:253` | `::test_detect_contradiction_emits_semantic_conflict`, `::test_detect_entailment_emits_semantic_duplicate`, `::test_detect_neutral_emits_nothing`, `::test_cosine_prefilter_skips_low_similarity`, `tests/test_gate_semantic_conflict_wire.py` (16: off per default se non c'è un modello locale, observe/enforce, evoluzione stessa fonte, cross-source mai supersede, moat senza llm end-to-end), `tests/test_nli_auto_enable.py` (5) | `README.md:188-193` | FUNZIONA COME PROMESSO | pytest 36 passed |

Reperti: (a) i numeri della docstring (0/6 lessicale, FPR 0,10 → 0,0125) sono banchi, non presidi: il comportamento (fail-safe NEUTRAL, prefiltro, regola temporale, cross-source mai supersede) è presidiato riga per riga; (b) `FixedRelationJudge` vive nel prodotto ma serve solo ai test. Il claim README:188-193 è vero e presidiato (`test_off_by_default_detector_not_called`, `test_enforce_cross_source_never_supersedes`, `test_enforce_evolution_with_supersede_disabled_quarantines`). Nessun P0.
