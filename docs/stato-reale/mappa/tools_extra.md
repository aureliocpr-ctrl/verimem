# Mappa — `verimem/tools_extra.py`

*ws3 Ricerca. 47 fra funzioni, classi e metodi: **le capacità che possono fare
danno** — filesystem, shell, rete, visione, webcam, desktop — e le guardie che
ci stanno davanti. Banchi: `ws3-mappa-prova-tools-extra.py` e
`ws3-mappa-prova-tools-extra-due.py` (09/09 14:20-14:21).*

⛔ **Quello che NON ho fatto, dichiarato prima delle righe**: non ho mosso il
mouse, non ho premuto tasti, non ho scattato schermate del desktop di Aurelio,
non ho acceso la shell (`HIPPO_ENABLE_SHELL` è rimasto spento **e l'ho
verificato**), non ho chiamato modelli di visione, non ho acceso la webcam, e
non sono uscito in rete verso l'esterno. Ho provato **le guardie** — che sono
pure — e i casi che devono essere **RIFIUTATI**: un rifiuto si misura senza fare
la cosa pericolosa.

🟡 **Il fatto che riguarda tutto il file**: `all_tools()` espone all'agente
**18** strumenti, e fra questi ci sono `shell_run`, `desktop_click`,
`desktop_type`, `desktop_key`, `desktop_screenshot`. Sono **sempre nell'elenco**
che il modello vede; la protezione non è l'assenza, è **l'interruttore dentro la
funzione** (riga 10). Chi legge l'elenco pensa di poterli usare, e la risposta
«disabilitato» arriva solo dopo aver provato.

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/tools_extra.py:39` `_enabled` | l'interruttore di una capacità: `HIPPO_ENABLE_<NOME>` accende, `HIPPO_DISABLE_<NOME>` frena un default acceso | **FUNZIONA COME PROMESSO** | `_enabled('shell', False)` senza variabile → **False** · con `HIPPO_ENABLE_SHELL=1` → **True** · con `=yes` → **True** · con `=0` → **False** · default `True` ma `HIPPO_DISABLE_FS=1` → **False** (il freno vince). ⚠️ La funzione **antepone da sé** `HIPPO_ENABLE_`: chiamarla con il nome completo cerca `HIPPO_ENABLE_HIPPO_ENABLE_SHELL` — errore mio nel primo giro |
| 2 | `verimem/tools_extra.py:46` `_fs_roots` | «radici permesse. Politica di default (v0.2): **STRICT — solo la data dir del progetto**. Il precedente default permissivo (`$HOME`) esponeva `~/.ssh`, `~/.aws`, `~/.gnupg`…» | **FUNZIONA COME PROMESSO** | con la data dir di casa → radici `['<home>']`, e **la HOME dell'utente NON è una radice** (verificato per confronto esplicito). Puntando la data dir a un tmp, la radice diventa il tmp: la politica segue la configurazione, non un percorso cablato |
| 3 | `verimem/tools_extra.py:88` `_strip_editor_backup_suffixes` | «toglie i suffissi di backup/swap così che **`.env~` corrisponda come `.env`**: `~`, `.bak`, `.backup`, `.swp`…» | **FUNZIONA COME PROMESSO — sette casi** | `.env~` → `.env` · `.env.bak` → `.env` · `.env.backup` → `.env` · `.env.swp` → `.env` · `id_rsa~` → `id_rsa` · `normale.txt` → invariato · `.env` → invariato |
| 4 | `verimem/tools_extra.py:128` `_is_sensitive` | «vero se il path sta sotto (o corrisponde a) un nome noto come sensibile» | **FUNZIONA COME PROMESSO — e prende anche le varianti** | `~/.ssh/id_rsa` → **True** · **`~/.ssh/id_rsa~` → True** (la variante di backup non sfugge) · `~/.aws/credentials` → **True** · `.env` → **True** · `.env.bak` → **True** · `note.txt` → **False** |
| 5 | `verimem/tools_extra.py:158` `_is_within_any` | il path sta dentro una delle radici? | **FUNZIONA COME PROMESSO** | dentro → **True** · nella HOME → **False** · **con una risalita `..` → False** (il tentativo di uscire non paga) |
| 6 | `verimem/tools_extra.py:177` `fs_read_file` | lettura di un file, dentro le radici e fuori dalla lista dei sensibili | **FUNZIONA COME PROMESSO — quattro casi, positivo compreso** | dentro la radice → `ok=True`, contenuto `"il canone e' 5900 euro\r\n"` · fuori → `ok=False, "path outside allowed roots: [...]"` · **`.env` dentro la radice** → `ok=False`, «path matches sensitive deny-list (ssh/aws/credentials…)» con log `fs_sensitive_blocked` · **`.env~`** → **stessa cosa**: la variante di backup non aggira il divieto · `max_bytes=5` → `'il ca'`, il tetto taglia |
| 7 | `verimem/tools_extra.py:200` `fs_write_file` | scrittura, con `append` | **FUNZIONA COME PROMESSO** | dentro → `ok=True`, log `fs_write append=False bytes=4` · con `append=True` → il file diventa `'ciao ancora'` · fuori dalle radici → `ok=False` |
| 8 | `verimem/tools_extra.py:222` `fs_list_dir` | elenco di una cartella | **FUNZIONA COME PROMESSO** | dentro → `ok=True`, «8 entries in …» · nella HOME → `ok=False`, «outside allowed roots» |
| 9 | `verimem/tools_extra.py:239` `fs_search_files` | «trova i file che corrispondono al glob; opzionalmente filtra per sottostringa nel contenuto» | **FUNZIONA COME PROMESSO — e il filtro sul contenuto morde** | `'*.txt'` → «2 matches» · `'*.txt'` con `contains='canone'` → **«1 matches»** · con una parola assente → **«0 matches»** (`ok=True`: zero risultati non è un errore) · un path assoluto → `ok=False`, «Non-relative patterns are unsupported» |
| 10 | `verimem/tools_extra.py:270` `shell_run` | «esegue un comando di shell arbitrario. **GATED da `HIPPO_ENABLE_SHELL=1`**» | **FUNZIONA COME PROMESSO — e l'ho verificato SENZA accenderla** | con la variabile assente: `shell_run('echo ciao')` → `ToolResult(ok=False, error='shell access disabled. set HIPPO_ENABLE_SHELL=1')`. Il messaggio dice **come** si accenderebbe, e io non l'ho accesa |
| 11 | `verimem/tools_extra.py:315` `_ip_is_blocked` | «vero se l'IP è loopback / RFC1918 / link-local / multicast / riservato / non specificato / **metadati cloud**» | **FUNZIONA COME PROMESSO — undici casi** | bloccati: `127.0.0.1`, `::1`, `10.0.0.5`, `192.168.1.1`, `172.16.0.1`, **`169.254.169.254`** (i metadati cloud), `224.0.0.1`, `0.0.0.0` · **non** bloccati: `8.8.8.8`, `93.184.216.34` (pubblici) e una stringa che non è un IP. Il verso positivo e quello negativo sono entrambi provati |
| 12 | `verimem/tools_extra.py:335` `_host_is_allowlisted` | «rispetta la allowlist di `OLLAMA_HOST`: un host ollama configurato **esplicitamente** è permesso anche se punta al loopback. **Corrispondenza esatta**» | **FUNZIONA COME PROMESSO** | senza la variabile: `'localhost'` → **False** · con `OLLAMA_HOST=http://127.0.0.1:11434`: `'127.0.0.1'` → **True**. L'eccezione esiste solo se l'operatore l'ha scritta |
| 13 | `verimem/tools_extra.py:351` `_is_blocked_host` | «rifiuta loopback / RFC1918 / link-local / metadati cloud. **Risolve il nome**, così un DNS rebind…» | **FUNZIONA COME PROMESSO, con un limite dichiarato** | `'localhost'` → **True** · `'127.0.0.1'` → **True** · `'169.254.169.254'` → **True** · ⚠️ `'metadata.google.internal'` → **False** su questa macchina, perché il nome **non risolve** qui: il giudizio è sull'IP risolto, e un nome che non risolve non è «bloccato» (la connessione fallirebbe comunque, ma per un'altra ragione). Su una macchina dentro una cloud quel nome risolverebbe a 169.254.169.254 e sarebbe bloccato — **non l'ho potuto misurare** |
| 14 | `verimem/tools_extra.py:381` `_SSRFBlocked` · `verimem/tools_extra.py:385` `_SSRFGuardBackend` · `verimem/tools_extra.py:402` `_SSRFGuardBackend.__init__` · `verimem/tools_extra.py:405` `_SSRFGuardBackend.connect_tcp` · `verimem/tools_extra.py:442` `_SSRFGuardBackend.connect_unix_socket` · `verimem/tools_extra.py:445` `_SSRFGuardBackend.sleep` | «involucro del backend di rete che **chiude la finestra di DNS-rebind / TOCTOU**: `_is_blocked_host()` risolve e valida, ma poi httpx risolve di nuovo» | **NON MISURATO** (la finestra TOCTOU richiede un DNS che cambia risposta fra due risoluzioni) | la classe è raggiunta da `_ssrf_client`, e i tre `web_fetch` bloccati passano dalla guardia **prima** di arrivare qui. Il caso che giustifica la classe — un nome che risolve a un IP pubblico e poi a uno privato — richiede un DNS controllato: lo scrivo invece di fingere di averlo provato |
| 15 | `verimem/tools_extra.py:449` `_install_ssrf_guard` · `verimem/tools_extra.py:462` `_ssrf_client` | «un `httpx.Client` le cui connessioni uscenti sono protette dal rebind. Sostituto **drop-in** per ogni fetch di un URL che un attaccante può scegliere» | **NON MISURATO direttamente** | usato da `web_fetch`, che nei tre casi bloccati non arriva ad aprire la connessione. Per misurarlo servirebbe una richiesta **permessa** verso l'esterno: non l'ho fatta |
| 16 | `verimem/tools_extra.py:480` `_read_body_capped` | «legge un corpo in streaming fino a `max_bytes`, poi si ferma. **Chiude la finestra del download illimitato**» | **NON MISURATO** | richiede una risposta HTTP vera: non sono uscito in rete |
| 17 | `verimem/tools_extra.py:498` `web_fetch` | «recupera un URL e restituisce il testo pulito. **Rinforzato contro l'SSRF**: rifiuta loopback, RFC1918, link-local e metadati cloud (CVE-006)» | **FUNZIONA COME PROMESSO sul lato che conta** | tre destinazioni pericolose, tre rifiuti **senza connessione**: `http://127.0.0.1:8080/x`, `http://169.254.169.254/latest/meta-data/`, `http://192.168.1.1/` → `ok=False`, «destination host '…' is on the SSRF…», con log `web_fetch_blocked reason=ssrf`. ⚠️ Il ramo che **scarica davvero** è **NON MISURATO** per scelta |
| 18 | `verimem/tools_extra.py:573` `web_search` | ricerca su DuckDuckGo → lista di (titolo, url, riassunto) | **NON MISURATO** (esce in rete) | non eseguita. La sua metà pura — il parsing — è misurata alla riga 20 |
| 19 | `verimem/tools_extra.py:590` `_strip_html` (+ `verimem/tools_extra.py:607` `_strip_html._Stripper` · `verimem/tools_extra.py:610` `_strip_html._Stripper.__init__` · `verimem/tools_extra.py:615` `_strip_html._Stripper.handle_starttag` · `verimem/tools_extra.py:619` `_strip_html._Stripper.handle_endtag` · `verimem/tools_extra.py:623` `_strip_html._Stripper.handle_data` · `verimem/tools_extra.py:627` `_strip_html._Stripper.get_text`) | «HTML→testo con il parser della libreria standard (prima era una regex, falso positivo di CodeQL)» | **FUNZIONA COME PROMESSO** | su un HTML con `<style>` e `<script>`: testo `"TitoloIl canone e' 5900 euro."` — **`alert` non c'è** e **`color:red` non c'è**. Le due sorgenti di codice sono escluse, non solo nascoste |
| 20 | `verimem/tools_extra.py:641` `_parse_ddg` | estrae i risultati dalla pagina di ricerca | **FUNZIONA COME PROMESSO** (su una pagina finta, senza rete) | HTML costruito da me con due risultati → `[{'title': 'Titolo uno', 'url': 'https://esempio.it/uno', 'snippet': 'Riassunto uno'}, {…due…}]`: titolo, URL e riassunto separati |
| 21 | `verimem/tools_extra.py:753` `_resolve_vision_model` | «sceglie il modello capace di visione: 1) `HIPPO_VISION_MODEL`, 2) `VISION_MODELS[provider]`, 3) None» | **FUNZIONA COME PROMESSO** | `anthropic` → **`claude-haiku-4-5-20251001`** · `ollama` → `llava` · `openai` → `gpt-4o-mini` · un provider inventato → **stringa vuota** (nessun modello inventato) |
| 22 | `verimem/tools_extra.py:664` `_read_image_to_b64_and_media_type` | «la sorgente è un percorso o un URL http(s); restituisce (b64, media_type)» | **FUNZIONA COME PROMESSO sul rifiuto** | su un file di testo (non un'immagine) l'esito è un errore esplicito, non un base64 senza senso. ⚠️ Il ramo con un'immagine vera è **NON MISURATO** |
| 23 | `verimem/tools_extra.py:772` `vision_describe` · `verimem/tools_extra.py:808` `_vision_anthropic` · `verimem/tools_extra.py:828` `_vision_ollama` · `verimem/tools_extra.py:858` `_vision_openai_compat` | descrizione di un'immagine con il provider multimodale attivo | **NON MISURATE** (quattro funzioni) | richiedono una chiamata a un modello esterno: **O4** dice Claude only e nessuna chiave esterna, e comunque sarebbe una chiamata vera. Non le ho eseguite |
| 24 | `verimem/tools_extra.py:900` `webcam_snapshot` · `verimem/tools_extra.py:931` `webcam_describe` | «cattura un fotogramma dalla telecamera predefinita» | **NON MISURATE** | accendere la webcam di Aurelio per una mappa non è una misura, è un'intrusione |
| 25 | `verimem/tools_extra.py:961` `_init_pyautogui_safety` · `verimem/tools_extra.py:975` `desktop_screenshot` · `verimem/tools_extra.py:1004` `desktop_click` · `verimem/tools_extra.py:1018` `desktop_type` · `verimem/tools_extra.py:1032` `desktop_key` | gli strumenti che pilotano il desktop | **NON MISURATE** (cinque funzioni) — **esistono e sono esposte** | verificato che **esistono** (`hasattr` → True per tutte e cinque) e che `all_tools()` **le espone**; **non le ho eseguite**: un click o un tasto finirebbero sul desktop vero di Aurelio. È il caso in cui «non misurato» è la risposta giusta, non una scusa |
| 26 | `verimem/tools_extra.py:1067` `extra_tools` · `verimem/tools_extra.py:1213` `all_tools` | l'elenco degli strumenti dato all'agente | **FUNZIONA COME PROMESSO, e il contenuto va guardato** | `extra_tools()` → **14**: `fs_read_file`, `fs_write_file`, `fs_list_dir`, `fs_search_files`, `web_fetch`, `web_search`, `vision_describe`, `webcam_snapshot`, `webcam_describe`, `desktop_screenshot`, `desktop_click`, `desktop_type`, `desktop_key`, `shell_run`. `all_tools()` → **18** (le 14 più le 4 di `tools.py`: `submit_solution`, `run_python`, `syntax_check`, `find_function`). ⚠️ **Gli strumenti pericolosi sono sempre nell'elenco**: la difesa è l'interruttore dentro la funzione, non l'assenza dalla lista |
