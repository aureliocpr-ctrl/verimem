# Mappa — `verimem/continuity.py`

*ws3 Ricerca. 19 fra funzioni ed eccezioni: **la catena** — il lineage dei
fatti, i checkpoint di sessione, gli handoff fra istanze, la riparazione dei
collegamenti. È il modulo che il progetto usa ogni giorno su sé stesso. Banco:
`ws3-mappa-prova-continuity.py` (09/09 13:31), tutto su uno store **temporaneo**:
`save_checkpoint` e `handoff_prepare` scrivono davvero.*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/continuity.py:26` `LineageRefError` | «riferimento di lineage malformato (uscita CLI 2): troppo corto, vuoto, usato male» | **FUNZIONA COME PROMESSO** | è l'eccezione che esce dai casi malformati qui sotto, e il messaggio dice **quale** regola è stata violata |
| 2 | `verimem/continuity.py:30` `LineageNotFound` | «il riferimento non si è risolto (uscita CLI 1): nessuna corrispondenza, o ambiguo» | **FUNZIONA COME PROMESSO** | distinta dalla precedente: un riferimento **ben formato ma inesistente** dà questa, non quella. Due uscite diverse per due errori diversi |
| 3 | `verimem/continuity.py:34` `_parse_ids` | «id separati da virgola → lista, scartando i vuoti (righe legacy `''` / NULL)» | **FUNZIONA COME PROMESSO — sei casi** | `'aaa,bbb'` → `['aaa','bbb']` · `'aaa,,bbb'` → `['aaa','bbb']` (il vuoto in mezzo cade) · `''` → `[]` · `None` → `[]` · `','` → `[]` · `'  aaa , bbb '` → `['aaa','bbb']` (spazi tolti) |
| 4 | `verimem/continuity.py:46` `_node` | la riga del database come nodo della catena | **FUNZIONA COME PROMESSO**, e legge **per posizione** | con le colonne di `_NODE_COLS`: `{'id': 'fc02c45901c7', 'topic': 'project/verimem/uno', 'preview': "Il canone…", 'created_at': 1788953521.5, 'lineage_to': []}`. ⚠️ Con un `SELECT *` i campi escono **sfasati** (`topic` col testo, `created_at` con la confidence): non è un difetto, è la query sbagliata — e l'ho misurato di proposito **nei due modi** perché la prima volta stavo per scriverlo come difetto |
| 5 | `verimem/continuity.py:67` `resolve_prefix` | «risolve un prefisso di id (**≥ 6 caratteri**) nell'id completo unico» | **FUNZIONA COME PROMESSO — quattro casi** | `'430'` → `LineageRefError: fact reference '430' is shorter than 6 chars` · `'430fdc'` → l'id intero · l'id intero → sé stesso · `'zzzzzzzz'` → `LineageNotFound: no fact matches prefix 'zzzzzzzz'` |
| 6 | `verimem/continuity.py:86` `resolve_lineage` | risolve `auto` \| `topic` \| `latest` \| prefisso; «`auto` = il fatto più recente che condivide il **PRIMO segmento** del topic» | **FUNZIONA COME PROMESSO** | `'auto'` → l'ultimo fatto del namespace `project` · `'latest'` → lo stesso (store piccolo) · `'topic'` con un topic **mai usato** → `LineageNotFound: lineage 'topic': no prior fact with topic 'project/verimem/tre'` — dice **quale** topic non ha trovato · un prefisso → l'id · un riferimento inventato → `LineageNotFound` |
| 7 | `verimem/continuity.py:140` `tip_fact` | «il fatto più recente dello store (il "dove eravamo") **o None se è vuoto**» | **FUNZIONA COME PROMESSO** | restituisce l'ultimo id scritto, e cambia dopo ogni scrittura |
| 8 | `verimem/continuity.py:149` `walk_backward` | «segue la dorsale del primo genitore da `start_id` alla radice; **radice→partenza**» | **FUNZIONA COME PROMESSO** | dal checkpoint → `['acbf09dc0347', 'b60fd784d353']`: prima la radice, poi il punto di partenza — l'ordine dichiarato |
| 9 | `verimem/continuity.py:180` `walk_forward` | «BFS sui discendenti… **riconosce tutte e quattro le codifiche** dell'appartenenza nella colonna separata da virgole (esatta, testa, …)» | **FUNZIONA COME PROMESSO — le quattro codifiche provate una per una** | costruite a mano nel database quattro righe con `lineage_to` = `<id>` · `<id>,xxxx` · `xxxx,<id>` · `xxxx,<id>,yyyy`; `walk_forward` le trova **tutte e quattro** (`True` × 4). È il claim più preciso del file ed è quello che regge meglio |
| 10 | `verimem/continuity.py:211` `find_orphans` | «i fatti nella finestra **senza puntatore di lineage**, più il totale della finestra» | **FUNZIONA COME PROMESSO** | `{'orphans': [...], 'total': 7}` → **2 orfani su 7** fatti nell'ultima ora. ⚠️ Restituisce un **dizionario**, non una tupla: spacchettarlo in due nomi dà le *chiavi*, e `len("orphans")` stampa **7** — un numero plausibile che non è il numero. L'ho scritto perché è la forma che inganna di più |
| 11 | `verimem/continuity.py:225` `save_checkpoint` | «scrive un checkpoint di sessione **ATTRAVERSO il gate**, incatenato. La scrittura è `Memory.add(meta_narrative=True)` — ricevuta inclusa, iniezione/L3/L4 attivi» | **FUNZIONA COME PROMESSO, e la ricevuta lo dice tutto** | ricevuta: `{'moat': 'not_run:no_source', 'stored': True, 'id': 'b60fd784d353', 'status': 'model_claim', 'grounding_score': None}`. ⚠️ Da leggere insieme: **senza `source` il moat non gira**, quindi un checkpoint entra come `model_claim` **mai giudicato** (`grounding_score: None`, non zero). È la regola di casa `O3` vista dalla porta: il campo `moat` della ricevuta la dichiara invece di nasconderla |
| 12 | `verimem/continuity.py:273` `handoff_show` | «l'ultimo fatto di handoff per `label` (proposizione intera) **o None**» | **FUNZIONA COME PROMESSO** | `handoff_show('ws3')` → `{'id': '90b389bc619b', 'topic': 'continuity/handoff/ws3', 'proposition': "Consegna: la mappa di gateway.py è a metà."}`; con un'etichetta inesistente → **None** |
| 13 | `verimem/continuity.py:287` `handoff_log` | «la storia degli handoff per `label`, dal più recente» | **FUNZIONA COME PROMESSO** | dopo un handoff → **1 voce** |
| 14 | `verimem/continuity.py:298` `handoff_prepare` | «salva un checkpoint di consegna **agganciato a ENTRAMBE le catene**: il precedente handoff della stessa etichetta e…» | **FUNZIONA COME PROMESSO** | ricevuta `{'moat': 'not_run:no_source', 'stored': True, 'id': '90b389bc619b', 'status': 'model_claim'}`, topic `continuity/handoff/ws3` generato da sé. ⚠️ **NON MISURATO** il doppio aggancio con un handoff **precedente** già presente: qui era il primo, quindi una delle due catene era vuota |
| 15 | `verimem/continuity.py:322` `relink` | «riparazione della catena: punta `child` a `parent`. `add=False` **sostituisce** il set dei genitori, `add=True` accoda. Mutazione di soli metadati» | **FUNZIONA COME PROMESSO** | `relink(figlio, secondo_fatto, add=False)` → `{'id': 'd792f13c639c', 'lineage_to': ['92cc8de645b7'], 'previous': ['acbf09dc0347']}` — restituisce **il prima e il dopo**, e `walk_backward` sullo stesso figlio ora risale al nuovo genitore. ⚠️ **NON MISURATO** il ramo `add=True` |
| 16 | `verimem/continuity.py:347` `recent_facts` | «gli N fatti più recenti come nodi (la vista di default rispecchia l'insieme nascosto del recall: orphaned/quarantined/user_belief esclusi)» | **FUNZIONA COME PROMESSO** | `recent_facts(sm, 5, False)` → 2 nodi su 2 fatti serviti. ⚠️ **NON MISURATO** il confronto `include_hidden=True` contro `False` su uno store con quarantinati: è il controllo che proverebbe l'esclusione, e va fatto |
| 17 | `verimem/continuity.py:360` `since_epoch` | «`today` \| `Nh` \| `Nd` \| `Nm` \| `Nw` → istante di taglio» | **FUNZIONA COME PROMESSO — cinque forme più il rifiuto** | `today` → mezzanotte locale (1788904800.0) · `2h`, `3d`, `1w`, `5m` → tagli coerenti e ordinati · `'spazzatura'` → `LineageRefError: invalid --since 'spazzatura' (expected NUM[m\|h\|d\|w] or 'today')`, che **elenca le forme valide** |
| 18 | `verimem/continuity.py:379` `_namespace` | il namespace di un topic a una data profondità | **FUNZIONA COME PROMESSO** | `('project/verimem/uno', 1)` → `'project'` · `(…, 2)` → `'project/verimem'` · `('senza', 1)` → `'senza'` (nessun segmento da tagliare, nessun errore) |
| 19 | `verimem/continuity.py:385` `collect_digest` | «il racconto della finestra: conteggi per stato, temi per namespace, salute della catena. **A differenza di un elenco secco è il riepilogo trasparente**: mostra cosa ha fatto il gate» | **FUNZIONA COME PROMESSO** | `{'window_hours': 24, 'n_facts': 8, 'by_status': {'model_claim': 4, 'quarantined': 4}, 'themes': [{'namespace': 'project/verimem', 'n_facts': 7, 'linked_ratio': 0.71, …}], 'velocity_per_hour': 0.33, 'orphan_ratio': 0.25, 'tip': {…}}`. I quarantinati **sono contati** invece di sparire, e `orphan_ratio` dice quanta parte della finestra non è agganciata a niente |
