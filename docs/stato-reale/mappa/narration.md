# Mappa di `verimem/narration.py` — 5 righe, 228 righe di codice (lead, 09/09 04:02)

Letto per intero. Prova: pytest del lotto G sul tip `20257636` (`117 passed, 1 skipped in 94.44s`, EXIT=0, con `tests/test_narration.py`, `tests/test_narration_extract_precision.py`, `tests/test_narration_llm.py`, `tests/test_audit_mutations.py`). Chiamanti letti: `archive_and_extract_narration` ← `verimem/cli.py:4839,4845` (il comando della CLI); le altre due pubbliche sono usate qui dentro e dai test. Motivazione misurata (13/06): ~34% dei fatti curati erano NARRAZIONE (riassunti di sessione datati, in prima persona) che dominava il recall e veniva letta come verità corrente. Claim README: nessuna riga (grep su «narrat» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/narration.py:80` `_clean_clause` | via il `#` iniziale, l'enfasi markdown e la punteggiatura ai bordi (non `_` né `#` in mezzo: identificatori e `#5`) | `extract_atomic_candidates` (133) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 117 passed |
| 2 | `verimem/narration.py:87` `_has_real_content` | almeno una parola di 3 lettere e ≥ 60% di caratteri alfanumerici (un UUID nudo non passa) | `extract_atomic_candidates` (138) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 117 passed |
| 3 | `verimem/narration.py:96` `is_session_narration` | vero solo se lunga ≥ 300 E (apre con una data ISO, o «ENGRAM/HippoAgent … data» entro 30 caratteri, o un marcatore Cycle/CICLO/Wave/Loop/HANDOFF/RESUME/SESSIONE/RIPRENDI); il nome del progetto da solo NON basta (controesempio del critico del 13/06); `date_window` ignorato («kept for signature stability») | `archive_and_extract_narration` (185) | `tests/test_narration.py`, `tests/test_audit_mutations.py` | - | FUNZIONA COME PROMESSO | pytest 117 passed |
| 4 | `verimem/narration.py:116` `extract_atomic_candidates` | le clausole fra 12 e 220 caratteri con un'ancora verificabile (SHA non-UUID, PR/#n, file o file:riga, verbo d'esito, numero+unità), pulite, con contenuto vero, dedup case-folded, ordine conservato | `archive_and_extract_narration` (190) | `tests/test_narration.py`, `tests/test_narration_extract_precision.py` | - | FUNZIONA COME PROMESSO | pytest 117 passed |
| 5 | `verimem/narration.py:148` `archive_and_extract_narration` | reversibile: `dry_run` di default; con mutazione sposta le narrazioni in `narrative` e le cancella da `facts` con `record_mutation` nella STESSA transazione (fail-closed); i candidati atomici sono CONTATI, mai inseriti («un passo separato e rivisto»); `principal` obbligatorio | `verimem/cli.py:4845` | `tests/test_narration.py`, `tests/test_audit_mutations.py` | - | FUNZIONA COME PROMESSO | pytest 117 passed |

Reperti: (a) il passo «inserimento dei candidati come fatti veri» non esiste in nessun modulo (dichiarato «separate, reviewed step»): la migrazione conta gli atomi e archivia la prosa, quindi con `dry_run=False` la conoscenza atomica NON entra nel corpus — il modulo fa metà della decisione del 13/06 («extract the atomic claims, THEN archive») e lo dice; (b) la lista dei verbi d'esito in `_VERIFY_ANCHOR` è inglese (`merged|shipped|fixed|…`): una narrazione italiana («consegnato», «corretto») non produce candidati per verbo, solo per SHA/numero (classe «liste monolingue»); (c) `is_session_narration` con `min_len=300` a sinistra del filtro: le narrazioni corte restano nel corpus per scelta di precisione, dichiarata. Nessun P0.
