# Mappa di `verimem/selection.py` — 8 righe, 277 righe di codice (lead, 08/09 23:05)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_selection.py tests/test_episode_priority.py` → `17 passed in 10.36s`, EXIT=0. Chiamanti letti: `verimem/wake.py:36` (import), `:659` (`consider_skills` sul pool con l'encoder e l'rng dell'agente, poi l'evento `skill_selection` con relevance/theta/score del primo), `:678` (`select_top` con `skills_top_k`), `:1244` (`consider_episodes` sui candidati con gli embedding già calcolati). Il README non nomina la selezione delle skill né il ciclo di veglia. Il docstring dichiara il difetto che sostituisce (tre gate scollegati: la similarità decideva, la fitness era solo un taglio binario) e la scelta: Thompson sampling sulla Beta della fitness × coseno, entrambi in [0, 1].

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/selection.py:56` `Choice` | la valutazione di una skill, congelata: rilevanza, campione theta e prodotto separati, così il pannello dice PERCHÉ una skill è stata scelta | `consider_skills` (153), `verimem/wake.py:665-676` (l'evento legge i tre campi) | `tests/test_selection.py::test_choice_records_carry_relevance_theta_and_score` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 2 | `verimem/selection.py:72` `_unit_norm` | il vettore normalizzato, invariato se a norma zero | `consider_skills` (140, 144), `consider_episodes` (250, 259) | via tutti | - | FUNZIONA COME PROMESSO (plumbing) | pytest 17 passed |
| 3 | `verimem/selection.py:77` `_skill_embedding` | il vettore hebbiano se c'è, altrimenti l'encoder su `nome\ntrigger` (la stessa convenzione di `_skill_similarity`); senza né l'uno né l'altro `ValueError`, mai un vettore zero silenzioso | `consider_skills` (144) | `::test_canonical_encoding_used_when_no_learned_embedding`, `::test_missing_encoder_and_no_embedding_raises` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 4 | `verimem/selection.py:106` `consider_skills` | per ogni skill: coseno tagliato a [0, 1] × theta ~ Beta(α+successi, β+fallimenti); prior da CONFIG; lista ordinata per punteggio; vuoto → vuoto; deterministica con rng seminato | `verimem/wake.py:659` | `::test_high_fitness_beats_similarity_alone_in_expectation`, `::test_unexplored_skill_gets_exploration_chance`, `::test_determinism_with_seed`, `::test_degenerates_to_top_cosine_when_evidence_is_overwhelming`, `::test_empty_input_returns_empty`, `::test_zero_relevance_skill_loses_even_with_high_fitness` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 5 | `verimem/selection.py:162` `select_top` | i primi k della lista ordinata (tenuta separata per comporre politiche più ricche) | `verimem/wake.py:678` | `::test_select_top_returns_first_k_skills` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 6 | `verimem/selection.py:185` `EpisodeChoice` | la valutazione di un episodio: rilevanza, recenza, punteggio affine | `consider_episodes` (262) | `tests/test_episode_priority.py::test_choice_records_carry_relevance_recency_score` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 7 | `verimem/selection.py:194` `_recency_factor` | decadimento esponenziale, 1 adesso, 1/e dopo `tau_s`, mai sopra 1 per un episodio nel futuro | `consider_episodes` (261) | `::test_more_recent_wins_at_equal_relevance`, `::test_realistic_mix_relevance_outweighs_old_recency` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 8 | `verimem/selection.py:208` `consider_episodes` | punteggio deterministico `rilevanza + peso·recenza` con gli embedding forniti dal chiamante; un embedding mancante → `KeyError`, mai un vettore zero | `verimem/wake.py:1244` | `::test_higher_cosine_wins_at_equal_recency`, `::test_more_recent_wins_at_equal_relevance`, `::test_empty_input_returns_empty`, `::test_recency_weight_zero_uses_cosine_only`, `::test_missing_episode_embedding_raises` | - | FUNZIONA COME PROMESSO | pytest 17 passed |

Reperti: nessuno. Il modulo mantiene ogni riga del docstring e la promessa «un solo posto dove rilevanza e affidabilità si incontrano» è cablata nel ciclo di veglia con l'evento che la rende leggibile. Nessun P0.
