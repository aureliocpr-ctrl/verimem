# Mappa di `verimem/agent.py` — 6 righe, 108 righe di codice (lead, 09/09 00:05)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_agent_reconcile_nli_wiring.py tests/test_reconcile_judge_env_wiring.py tests/test_il_build_dell_agent_non_tiene_il_lock.py` → `6 passed, 2 warnings in 9.56s`, EXIT=0 (`VerimemAgent.build` è costruito da centinaia di test: 118 moduli del prodotto lo nominano, 381 file di test). È la FACCIATA: memoria episodica + skill + store semantico + veglia + sonno + grafo delle entità; il server MCP, la CLI, la dashboard e il gateway costruiscono l'agente da qui. Il README non nomina mai `VerimemAgent` (né `HippoAgent`): la superficie pubblica documentata è `Memory`/`open_memory` (client.py, ws3), questa è la facciata interna. L'alias `HippoAgent` (riga 108) tiene vivo il nome pre-0.7.0.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/agent.py:21` `wire_reconcile_judge` | opt-in da `ENGRAM_RECONCILE_NLI`: `local` → giudice NLI locale (niente `claude -p`), valori veri → giudice LLM; altro → nessun giudice (lessicale); pigro; non alza mai | `build` (88) | `tests/test_reconcile_judge_env_wiring.py` (4: local, llm, non impostata, non alza mai), `tests/test_agent_reconcile_nli_wiring.py` (2) | - | FUNZIONA COME PROMESSO | pytest 6 passed |
| 2 | `verimem/agent.py:47` `VerimemAgent` | la facciata con i sei componenti | `mcp_server`, `cli`, `dashboard_routes/layout`, `gateway`, `code`, … (118 moduli) | tutti i test che chiamano `build` | - | FUNZIONA COME PROMESSO | pytest 6 passed; `git grep -lE "\bagent\b" -- verimem/` → 118 file |
| 3 | `verimem/agent.py:56` `VerimemAgent.build` | costruisce: store semantico con `repo_root` (senza, ogni scrittura `verified` verrebbe degradata: il gate verifica `file:` e `commit` contro il repo vero), LLM pigro (la dashboard costruisce anche senza chiave), veglia con i tool, giudice di riconciliazione opt-in, sonno | `verimem/mcp_server.py`, `verimem/cli.py`, `verimem/dashboard_routes/layout.py`, `verimem/gateway.py` (letto per nome; le righe esatte stanno nelle mappe di quei file) | `tests/test_il_build_dell_agent_non_tiene_il_lock.py`, `tests/test_il_pannello_non_esplode_senza_il_db.py`, `tests/test_rename_verimem_aliases.py`, e i 381 file che lo costruiscono | - | FUNZIONA COME PROMESSO | pytest 6 passed |
| 4 | `verimem/agent.py:93` `VerimemAgent.run_task` | delega alla veglia | `verimem/mcp_server.py` (`hippo_run_task`, letto per nome), `verimem/cli.py` | i test del ciclo di veglia (mappa di `wake.py`, ws5) | - | FUNZIONA COME PROMESSO (delega di una riga; la promessa è di `WakeAgent.run`) | letto |
| 5 | `verimem/agent.py:96` `VerimemAgent.consolidate` | delega al ciclo del sonno | `verimem/mcp_server.py` (`hippo_consolidate`), `verimem/cli.py` | i test del sonno (mappa di `sleep.py`, ws4) | - | FUNZIONA COME PROMESSO (delega di una riga) | letto |
| 6 | `verimem/agent.py:99` `VerimemAgent.reset` | emette `agent_reset` e svuota memoria, skill e store con il principal `system:agent-reset` | `git grep -n "\.reset()" -- verimem/mcp_server.py verimem/cli.py` → da leggere nelle mappe di quei file | `tests/test_un_anello_rimosso_si_dichiara.py` (letto per nome) | - | FUNZIONA COME PROMESSO (letto; la cancellazione con principal è presidiata nei test dello store, ws6) | letto |

Reperti: (a) la facciata non è nel README e va bene così (è interna), ma `repo_root=CONFIG.project_root` alla riga 72 decide se le scritture `verified` di TUTTE le porte restano tali: un utente con `project_root` sbagliato vede ogni `verified` diventare `model_claim` in silenzio, «paranoid default» dichiarato nel commento e non nel README; (b) `HippoAgent` resta come alias del vecchio nome, presidiato da `test_rename_verimem_aliases.py`. Nessun P0.
