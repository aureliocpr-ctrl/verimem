# Mappa di `verimem/hot_reload.py` — 14 righe, 216 righe di codice (lead, 08/09 21:55)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_hot_reload.py` → `9 passed in 8.04s`, EXIT=0. **Nessun modulo del prodotto lo importa**: `git grep -nE "hot_reload|HotReloadWatcher|collect_pending_changes" -- verimem/ scripts/ pyproject.toml` (escluso il file) → vuoto; nemmeno gli hook di sessione in `~/.claude/hooks` lo nominano. Stessa famiglia di `resource_monitor.py` (round 13 del 27/05: P2a hot-reload, P2b throttle): scritti, testati, mai collegati. Il docstring è onesto sul limite (il riavvio del server MCP ospitato in Claude Code non si può fare da dentro: questo è la metà «osservazione»), ma la metà osservazione non la avvia nessuno. Il README non lo nomina.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/hot_reload.py:48` `FileChange` | un cambiamento osservato (percorso, hash, quando) | `diff_snapshots`, il watcher | `tests/test_hot_reload.py` | - | MAI CHIAMATA (l'intero modulo non ha chiamanti nel prodotto; il codice funziona: 9 test verdi) | grep sopra; pytest 9 passed |
| 2 | `verimem/hot_reload.py:55` `_hash_file` | sha256 del contenuto (16 hex), `''` se illeggibile | `_scan_py_files`, il watcher | via i test | - | MAI CHIAMATA (come sopra) | grep sopra |
| 3 | `verimem/hot_reload.py:70` `_scan_py_files` | `{percorso: hash}` di ogni `.py` sotto le radici, saltando `.git`, `__pycache__`, `.venv`, `node_modules`, `.pytest_cache` | `collect_pending_changes`, `HotReloadWatcher.start` | `::test_skips_pycache` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 4 | `verimem/hot_reload.py:88` `diff_snapshots` | i file aggiunti, cambiati nel contenuto o cancellati (hash vuoto) | `collect_pending_changes` | `::test_no_changes_returns_empty`, `::test_added_file_detected`, `::test_modified_file_detected`, `::test_deleted_file_detected` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 5 | `verimem/hot_reload.py:105` `collect_pending_changes` | confronta il filesystem con l'ultimo snapshot in `pending.json`, lo aggiorna, rende i cambiamenti; la prima chiamata stabilisce la base | nessuno nel prodotto | `::test_first_call_establishes_baseline`, `::test_second_call_detects_only_changes` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 6 | `verimem/hot_reload.py:131` `HotReloadWatcher` | watcher vivo con `watchdog` (opzionale): a ogni cambio vero chiama `on_change` e accoda; senza `watchdog` è degradato (si fa polling) | nessuno nel prodotto | `::test_watcher_constructs`, `::test_drain_pending_clears` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 7 | `verimem/hot_reload.py:143` `HotReloadWatcher.start` | avvia l'osservatore o, senza `watchdog`, non fa niente | nessuno nel prodotto | `::test_watcher_constructs` (non lo avvia: letto) | - | MAI CHIAMATA (come sopra; l'avvio con `watchdog` non ha un test) | grep sopra |
| 8 | `verimem/hot_reload.py:156` `HotReloadWatcher.start._Handler` | il gestore degli eventi del filesystem | `start` (187) | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 9 | `verimem/hot_reload.py:157` `HotReloadWatcher.start._Handler.on_modified` | inoltra | il watchdog | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 10 | `verimem/hot_reload.py:160` `HotReloadWatcher.start._Handler.on_created` | inoltra | il watchdog | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 11 | `verimem/hot_reload.py:163` `HotReloadWatcher.start._Handler.on_deleted` | inoltra | il watchdog | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 12 | `verimem/hot_reload.py:166` `HotReloadWatcher.start._Handler._maybe_emit` | ignora i non-`.py` e i cambi cosmetici (stesso hash); accoda e chiama `on_change` senza far cadere l'osservatore | i tre `on_*` | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 13 | `verimem/hot_reload.py:192` `HotReloadWatcher.stop` | ferma e attende l'osservatore | nessuno nel prodotto | nessuno | - | MAI CHIAMATA (come sopra) | grep sopra |
| 14 | `verimem/hot_reload.py:202` `HotReloadWatcher.drain_pending` | rende e svuota la coda | nessuno nel prodotto | `::test_drain_pending_clears` | - | MAI CHIAMATA (come sopra) | grep sopra |

Reperti: (a) modulo intero mai collegato, come `resource_monitor.py`: il «silent drift» che dichiara di curare (il server MCP che gira su codice vecchio mentre credi di provare il nuovo) è esattamente la trappola che abbiamo pagato oggi con le scratchpad che importano l'albero sbagliato (post di ws1, 21:17); (b) la classe `_Handler` e i suoi metodi non hanno nessun test (solo la parte senza `watchdog` è provata). Decisione da prendere: collegarlo a chi può avvisare (il doctor? l'hook di sessione?) o toglierlo. Nessun P0.
