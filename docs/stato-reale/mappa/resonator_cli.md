# Mappa di `verimem/resonator_cli.py` — 9 righe, 179 righe di codice (lead, 08/09 22:43)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_resonator_cli.py tests/test_resonator_cli_cross_process.py` → `7 passed in 23.85s`, EXIT=0. **Nessun modulo del prodotto lo importa e nessuno lo lancia**: `git grep -nE "resonator_cli" -- verimem/ scripts/ .github/ pyproject.toml` (escluso il file) → vuoto; nessun entry point, nessun comando `verimem`, nessun hook. È una CLI a sé (`python -m verimem.resonator_cli remember|recall|stats|reset`) sopra `ResonatorMemory` (ws3), con stato in `~/.engram/resonator/memory.npz` e un indice di testo a parte (`index.jsonl`), fuori dallo store di verimem. I tre contratti falsificabili del docstring (round trip, save/load, reset) hanno i loro test. Il README non nomina il resonator.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/resonator_cli.py:41` `_load_state` | lo stato dal file `.npz` se c'è, altrimenti una memoria nuova (K=3, M=32, d=4096) | `cmd_remember`, `cmd_recall`, `cmd_stats` | `tests/test_resonator_cli.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 2 | `verimem/resonator_cli.py:53` `_save_state` | salva il `.npz` creando la cartella | `cmd_remember` (91) | `tests/test_resonator_cli.py` (contratto b: load(save)) | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 3 | `verimem/resonator_cli.py:60` `_load_text_index` | la mappa indici → testo dal JSONL | `cmd_recall`, `cmd_stats` | `tests/test_resonator_cli.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 4 | `verimem/resonator_cli.py:75` `_append_text_index` | appende una riga `{indices, text}` | `cmd_remember` (90) | `tests/test_resonator_cli.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 5 | `verimem/resonator_cli.py:85` `cmd_remember` | testo → atomi (hash) → aggregato; salva stato e indice; rende `n_facts` e la norma | `main` (165) | `tests/test_resonator_cli.py` (contratto a), `tests/test_resonator_cli_cross_process.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 6 | `verimem/resonator_cli.py:101` `cmd_recall` | matching pursuit su tutto l'aggregato e mappa inversa al testo; gli atomi non indicizzati vanno in `unknown_atoms` | `main` (167) | `tests/test_resonator_cli.py` (contratto a), `tests/test_resonator_cli_cross_process.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 7 | `verimem/resonator_cli.py:125` `cmd_stats` | lo stato più i percorsi e se esistono | `main` (169) | `tests/test_resonator_cli.py` | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 8 | `verimem/resonator_cli.py:139` `cmd_reset` | cancella stato e indice | `main` (171) | `tests/test_resonator_cli.py` (contratto c) | - | FUNZIONA COME PROMESSO | pytest 7 passed |
| 9 | `verimem/resonator_cli.py:148` `main` | la CLI con i quattro sottocomandi e i due percorsi | **nessuno**: solo `python -m verimem.resonator_cli` a mano (grep sopra) | `tests/test_resonator_cli.py`, `tests/test_resonator_cli_cross_process.py` | - | MAI CHIAMATA (dal prodotto: raggiungibile solo con `python -m`, non documentata) | grep sopra |

Reperti: (a) una memoria parallela a quella di verimem (aggregato olografico + indice di testo in `~/.engram/resonator/`), non passa dal gate e non appare in nessuna superficie: è un laboratorio dentro il pacchetto; se resta, va detto (README o `docs/ricerca`), altrimenti si toglie con `resonator_memory.py` (ws3); (b) `~/.engram` è il percorso del nostro ambiente. Nessun P0.
