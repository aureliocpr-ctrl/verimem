# Mappa di `verimem/llm_keywords_augment.py` — 3 righe, 161 righe di codice (lead, 09/09 03:10)

Letto per intero. Prova sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_batch_predicate_derivation.py tests/test_lab_longmemeval_adapter.py tests/test_llm_keywords_augment.py tests/test_llm_keywords_batch.py tests/test_momentum_macro.py tests/test_predicate_derivation.py tests/test_residual_copies.py tests/test_self_curation.py tests/test_smart_pruning.py tests/test_temporal_narrative.py tests/test_trajectory_diff.py tests/test_trajectory_render.py` → `105 passed, 1 warning in 31.36s`, EXIT=0 (lotto F: dieci moduli). Chiamante letto: `verimem/llm_keywords_batch.py:50` (il batch sui fatti con `trigger_keywords` vuote: 277 vuoti e 1.388 superficiali nell'audit del 22/05). La bozza attribuiva a `_normalise` chiamanti in `content_pin.py`: **falsi positivi del nome**. Il modello è INIETTATO (`llm_callable`): nessun client API nel modulo, per il vincolo O4 (subscription-only). Claim README: nessuna riga (grep su «keyword» → riga 218, altro senso).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/llm_keywords_augment.py:73` `_normalise` | minuscolo, spazi → un trattino | `extract_keywords` (151) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 105 passed |
| 2 | `verimem/llm_keywords_augment.py:79` `_strip_markdown_fences` | toglie SOLO le recinzioni markdown in testa e in coda (il modello le mette nonostante il prompt: osservato il 22/05 sul fatto `a232e5c15c76`) | `extract_keywords` (138) | via il test | - | FUNZIONA COME PROMESSO | pytest 105 passed |
| 3 | `verimem/llm_keywords_augment.py:103` `extract_keywords` | fino a `n_max` concetti normalizzati e deduplicati dal JSON del modello; testo vuoto → nessuna chiamata; qualsiasi fallimento → `[]` senza eccezioni | `verimem/llm_keywords_batch.py:50` | `tests/test_llm_keywords_augment.py`, `tests/test_llm_keywords_batch.py` | - | FUNZIONA COME PROMESSO | pytest 105 passed |

Reperti: (a) «every failure path returns `[]` and silently swallows the cause» è dichiarato: il batch non può distinguere «il modello non ha risposto» da «nessun concetto»: classe «l'assenza non ha un canale»; (b) `n_min` è un suggerimento al modello, non un vincolo (dichiarato). Nessun P0.
