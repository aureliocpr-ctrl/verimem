# Mappa di `verimem/query_intent.py` — 4 righe, 113 righe di codice (lead, 09/09 02:20)

Letto per intero. Prova: pytest del lotto D sul tip `20257636` (`73 passed, 6 xfailed in 72.21s`, EXIT=0, con `tests/test_query_intent.py` e `tests/test_quanti_giorni_non_e_una_domanda_di_conteggio.py`; lo nominano anche `tests/test_query_dispatch.py`, `tests/test_il_conteggio_a_zero_non_diceva_perche.py`, `tests/test_il_conteggio_non_promette_l_intero_corpus.py`). Chiamanti letti: `verimem/client.py:2063-2083` (il dispatcher del read-path: `classify_query_intent`, `content_terms`) e `:2189-2190` (`split_exclude`); `is_set_operation`: **nessun chiamante** (`git grep` per nome su `verimem/ scripts/ benchmark/` → solo il modulo). È il gemello del `gate_router` sul read-path: classifica l'OPERAZIONE (find/count/exclude/list_all), il chiamante smista; FIND resta il default sicuro. Misura citata nel docstring: recall k=5 vedeva 5/12 menzioni (sottoconteggio 58%). Claim README: nessuna riga nomina il routing per intento (grep su «intent» → riga 46, altro senso).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/query_intent.py:47` `classify_query_intent` | `count` > `exclude` > `list_all` > `find`, su pattern ancorati EN+IT; vuoto → find | `verimem/client.py:2068,2071` | `tests/test_query_intent.py`, `tests/test_quanti_giorni_non_e_una_domanda_di_conteggio.py` | - | FUNZIONA COME PROMESSO (la regex prende anche «quanti giorni»: la guardia che lo tiene fuori dal conteggio sta nel chiamante, mappa `client.py` di ws3) | pytest 73 passed |
| 2 | `verimem/query_intent.py:65` `is_set_operation` | il bit «serve una scansione dell'insieme» | nessuno | `tests/test_query_intent.py` | - | MAI CHIAMATA (il dispatcher confronta direttamente con FIND) | pytest 73 passed |
| 3 | `verimem/query_intent.py:88` `content_terms` | via i pattern d'intento e le parole funzionali, restano i termini di contenuto (ordine conservato) | `verimem/client.py:2069,2073,2083`, `split_exclude` (107-108) | `tests/test_query_dispatch.py`, `tests/test_il_conteggio_a_zero_non_diceva_perche.py` | - | FUNZIONA COME PROMESSO | pytest 73 passed |
| 4 | `verimem/query_intent.py:99` `split_exclude` | `(soggetto, escluso)` spezzando al pivot di negazione | `verimem/client.py:2189-2190` | nessun test la nomina (esercitata dal dispatcher) | - | FUNZIONA COME PROMESSO (per effetto: nessun test diretto) | pytest 73 passed |

Reperti: (a) `_STOP` è la QUARTA lista bilingue di parole funzionali del prodotto (con `soggetto_valore._FUNZIONALI`, `bm25_rank._QUERY_STOPWORDS`, `document_index._PAROLE_VUOTE`): classe «una copia invece della superficie unica»; (b) `_EXCLUDE` contiene `senza` e `che non`: «fatti senza fonte» viene instradato a EXCLUDE e spezzato in («fatti», «fonte»), cioè il contrario dell'intento — letto, non misurato; (c) `_COUNT` prende `quant[ei]` anche in «quanti giorni», «quante ore»: durata, non cardinalità — il test dice che la porta non conta, quindi il rimedio sta a valle. Nessun P0.
