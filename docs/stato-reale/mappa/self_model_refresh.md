# Mappa di `verimem/self_model_refresh.py` — 5 righe, 153 righe di codice (lead, 09/09 12:28)

Letto per intero. Prova: pytest del lotto H sul tip `20257636` (`81 passed in 88.10s`, EXIT=0, con `tests/test_self_model_refresh.py`). Chiamante letto: `verimem/mcp_server.py:11492-11534` (`hippo_self_model_refresh`, nome dall'elenco dei tool: `propose_refresh` e `compute_diff`). La bozza attribuiva a `_truncate` chiamanti in `skill_drafter.py` e `trajectory_render.py`: **falsi positivi del nome** (terza `_truncate` locale). Ciclo #68: aggiornamento DETERMINISTICO del self-model (progetti attivi dai topic `project/<nome>/…` degli ultimi episodi, `recent_focus` dall'ultimo episodio); i campi che richiedono interpretazione (`current_goals`, `open_decisions`, `collab_style`, `notes`) sono preservati verbatim. Claim README: nessuna riga (grep su «self-model» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/self_model_refresh.py:28` `_extract_project_name` | il secondo segmento di `project/<nome>/…`, altrimenti None | `propose_refresh` (103) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 81 passed |
| 2 | `verimem/self_model_refresh.py:41` `_topic_for_episode` | il topic da `topic_hint`, `topic`, o dal prefisso `[…]` del `task_text` | `propose_refresh` (102) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 81 passed |
| 3 | `verimem/self_model_refresh.py:60` `_truncate` | taglio a 280 con «…» | `propose_refresh` (116) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 81 passed |
| 4 | `verimem/self_model_refresh.py:67` `propose_refresh` | il contenuto proposto: `active_projects` (top-6 per frequenza) e `recent_focus` (episodio più recente per `created_at`) ricalcolati, gli altri quattro campi preservati; senza episodi → l'attuale invariato o i default | `verimem/mcp_server.py:11510` | `tests/test_self_model_refresh.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 5 | `verimem/self_model_refresh.py:141` `compute_diff` | i campi di primo livello che cambiano fra due dict, ordinati | `verimem/mcp_server.py:11534` | `tests/test_self_model_refresh.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |

Reperti: (a) il self-model di casa (mostrato a ogni avvio dal hook: «version 7, focus 2026-05-19 sera…») è fermo da maggio: la funzione che lo aggiorna esiste ed è testata, e il campo `recent_focus` che vediamo ogni mattina è di quattro mesi fa — la classe «capacità pronta e mai esercitata» misurata sulla nostra stessa sessione (il valore letto stamattina nell'hook di avvio: `focus ora: 2026-05-19 sera`); (b) i topic canonici con scope (`user:<u>/…/project/x`) non iniziano con `project` e non contano come progetto attivo (letto). Nessun P0.
