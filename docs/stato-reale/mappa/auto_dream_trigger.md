# Mappa di `verimem/auto_dream_trigger.py` — 7 righe, 284 righe di codice (lead, 08/09 23:35)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_auto_dream_trigger.py tests/test_il_doctor_diceva_assente_a_un_file_che_c_era.py` → `39 passed, 1 warning in 23.19s`, EXIT=0. Chiamante letto: `verimem/auto_dream_worker.py:470-478` (`main()` del worker: `maybe_trigger_dream` con l'ora e la proposta via engram); il worker lo lancia il nostro hook di sessione (il blocco «Auto-Dream worker (cycle #69): spawned» all'avvio). Il README non nomina il Dream. Il docstring dichiara tutto: default ON dal 16/05, opt-out con la variabile, soglia 5 elementi, cooldown 30 min, SQL in sola lettura, la chiamata al Dream iniettata, mai un'eccezione a chi avvia la sessione; e il controesempio del critico (un timestamp nel futuro bloccava per sempre) con la cura.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/auto_dream_trigger.py:58` `should_trigger` | la decisione pura: abilitato, abbastanza elementi nuovi, cooldown rispettato (o prima volta) | **nessuno nel prodotto**: `maybe_trigger_dream` rifà i tre controlli in linea (righe 226-260) invece di chiamarla (`git grep -n "should_trigger(" -- verimem/` → solo qui) | `::test_no_trigger_when_disabled`, `::test_no_trigger_when_few_items`, `::test_trigger_on_first_run_when_enough_items`, `::test_cooldown_blocks_trigger`, `::test_trigger_when_cooldown_elapsed`, `::test_zero_items_never_triggers_even_after_long_idle` | - | MAI CHIAMATA (candidata: la «funzione pura da testare esaustivamente» è testata e poi non usata; l'orchestratore ne tiene una copia in linea, con un ordine diverso: prima il cooldown, poi il conteggio) | comando sopra |
| 2 | `verimem/auto_dream_trigger.py:89` `_count_table` | le righe con `created_at > since` in una tabella; DB o tabella mancanti → 0 | `count_new_items` (125-126) | `::test_count_handles_missing_dbs_gracefully` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 3 | `verimem/auto_dream_trigger.py:112` `count_new_items` | episodi + fatti nuovi dopo `since`; con `since=None` tutto il corpus | `maybe_trigger_dream` (250) | `::test_count_zero_when_no_new`, `::test_count_sums_episodes_and_facts_since_ts`, `::test_count_since_ts_none_counts_everything` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 4 | `verimem/auto_dream_trigger.py:135` `load_last_trigger_ts` | l'ultimo timestamp; None se manca, è corrotto o sta nel FUTURO oltre 60 s (l'orologio che torna indietro non deve bloccare per sempre) | `maybe_trigger_dream` (243) | `::test_load_returns_none_when_state_missing`, `::test_load_handles_corrupt_state_gracefully`, `::test_load_clamps_future_timestamp_to_none`, `::test_load_accepts_timestamps_at_or_before_now`, `::test_maybe_trigger_recovers_from_future_state_file` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 5 | `verimem/auto_dream_trigger.py:156` `save_last_trigger_ts` | scrive il timestamp creando la cartella | `maybe_trigger_dream` (270) | `::test_save_then_load_roundtrip`, `::test_maybe_trigger_persists_state_after_firing` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 6 | `verimem/auto_dream_trigger.py:162` `_resolve_db_paths` | i percorsi di `episodes.db` e `semantic.db`: prima il layout annidato, poi quello piatto | `maybe_trigger_dream` (249) | `::test_maybe_trigger_prefers_nested_db_layout`, `tests/test_il_doctor_diceva_assente_a_un_file_che_c_era.py` (lo cita alle righe 177-178) | - | FUNZIONA COME PROMESSO (letto: se il file annidato esiste ma è VUOTO lo sceglie lo stesso, il commento «avoids picking an empty stub» non è implementato: la trappola dei due DB della memoria di casa) | pytest 39 passed |
| 7 | `verimem/auto_dream_trigger.py:192` `maybe_trigger_dream` | un solo punto d'ingresso per l'hook: variabile (allowlist di valori veri, default ON), cooldown PRIMA di aprire il DB, conteggio, chiamata al Dream con ogni eccezione catturata, stato persistito solo dopo un successo | `verimem/auto_dream_worker.py:474` | `::test_maybe_trigger_returns_status_dict_when_disabled`, `::test_maybe_trigger_default_is_enabled`, `::test_maybe_trigger_non_truthy_values_still_disable`, `::test_maybe_trigger_skips_when_not_enough_items`, `::test_maybe_trigger_invokes_dream_when_all_conditions_met`, `::test_maybe_trigger_respects_cooldown_across_calls`, `::test_maybe_trigger_handles_dream_exception_gracefully` | - | FUNZIONA COME PROMESSO | pytest 39 passed |

Reperti: (a) `should_trigger` è la copia pura dell'orchestratore e nessuno la chiama: due implementazioni della stessa decisione, una testata e ferma, una in linea e viva, con un ordine di controlli diverso; (b) `_resolve_db_paths` promette nel commento di evitare uno stub vuoto e non lo fa (righe 179-183): sceglie il file annidato se esiste, punto; è la stessa trappola dei «due DB, quello alla radice è vuoto» della memoria di casa, qui nel verso opposto. Nessun P0.
