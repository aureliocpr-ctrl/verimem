# Mappa di `verimem/trajectory.py` — 8 righe, 105 righe di codice (lead, 08/09 23:12)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_trajectory.py tests/test_causal_extract.py` → `17 passed in 9.12s`, EXIT=0. Chiamanti letti: `verimem/mcp_server.py:9793-9812` (i tool `hippo_trajectory_render`, `hippo_trajectory_fork`, e per nome `hippo_trajectory_diff`, `hippo_trajectory_summary`, righe 4603-4658: leggono `trajectory_json` con `trajectory_from_json`), `verimem/causal_extract.py:15,72-73` (l'estrazione causale confronta due traiettorie). I moduli `trajectory_render/fork/diff` sono altre righe della mia parte. Il README non nomina le traiettorie; le espone solo la porta MCP. Il docstring promette la compatibilità all'indietro (`Episode.trajectory` opzionale).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/trajectory.py:27` `TrajectoryStep` | un passo (pensiero, azione con strumento, osservazione, decisione con ramo) | `from_dict`, `trajectory_from_json`, `causal_extract.py`, `mcp_server.py:9808` | `tests/test_trajectory.py::test_step_kinds`, `::test_step_with_tool` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 2 | `verimem/trajectory.py:39` `TrajectoryStep.__post_init__` | rifiuta un `kind` fuori dai quattro e uno `step_idx` negativo | Python, alla costruzione | `::test_step_validation_kind`, `::test_step_validation_step_idx_negative` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 3 | `verimem/trajectory.py:47` `TrajectoryStep.to_dict` | il dict del passo | `trajectory_to_json` (66) | `::test_to_dict_roundtrip`, `::test_step_dict_returns_clean_dict` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 4 | `verimem/trajectory.py:51` `TrajectoryStep.from_dict` | ricostruisce, robusto ai campi opzionali mancanti | `trajectory_from_json` (74) | `::test_from_json_robust_to_missing_optional` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 5 | `verimem/trajectory.py:64` `trajectory_to_json` | la lista in JSON (`default=str` per i valori non serializzabili) | nessuno nel prodotto (`git grep -n trajectory_to_json -- verimem/` → solo qui e `__all__`) | `::test_to_dict_roundtrip`, `::test_empty_trajectory_serialization` | - | MAI CHIAMATA (candidata: le porte ricevono il JSON dal chiamante e non lo producono; chi scrive una traiettoria in un episodio non passa da qui) | comando sopra |
| 6 | `verimem/trajectory.py:69` `trajectory_from_json` | la lista dal JSON; vuoto → `[]` | `verimem/mcp_server.py:9793-9809` (i quattro tool) | `::test_to_dict_roundtrip`, `::test_empty_trajectory_serialization`, `::test_from_json_robust_to_missing_optional` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 7 | `verimem/trajectory.py:77` `trajectory_normalize` | i passi ordinati per `step_idx` | nessuno nel prodotto (`git grep -n trajectory_normalize -- verimem/` → solo qui) | `::test_step_sorting` | - | MAI CHIAMATA (candidata) | comando sopra |
| 8 | `verimem/trajectory.py:84` `trajectory_branches` | i passi raggruppati per `branch_id` (i None saltati), ogni ramo ordinato | nessuno nel prodotto (`git grep -n trajectory_branches -- verimem/` → solo qui) | `::test_branch_ids_grouping` | - | MAI CHIAMATA (candidata: `trajectory_fork` fa il suo raggruppamento) | comando sopra |

Reperti: tre funzioni di libreria senza chiamanti nel prodotto (serializzazione in uscita, ordinamento, rami); la porta MCP usa solo la lettura. Nessun P0.
