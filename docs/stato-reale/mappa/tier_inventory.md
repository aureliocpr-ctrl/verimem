# Mappa di `verimem/tier_inventory.py` — 5 righe, 188 righe di codice (lead, 09/09 12:24)

Letto per intero. Prova: pytest del lotto H sul tip `20257636` (`81 passed in 88.10s`, EXIT=0, con `tests/test_inventario_dei_tier.py`). Chiamanti letti: `tier_inventory` ← `verimem/cli.py:688-689`, `verimem/client.py:4029`, `verimem/gateway.py:1343`, `verimem/mcp_server.py:14902` (`hippo_tier_inventory`, nome dall'elenco dei tool): quattro porte. Nato dalla buca del 05/08 (contare le tabelle delle entità dentro `semantic.db` dava 0 mentre il tier vero, `entity_kg/entity_kg.db`, aveva 9.078 entità e 87.387 archi; nella radice della data dir c'è un doppione vuoto per quasi ogni tier). Tre scelte dichiarate: percorsi presi dal PRODOTTO (`CONFIG` e i risolutori dei moduli), uno store assente vale `"unavailable"` mai `0`, i doppioni si NOMINANO. Claim README: nessuna riga (grep su «tier» → 184, 211-215: altro senso, i tier del giudice e della confidenza).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/tier_inventory.py:57` `_conta` | righe della tabella in sola lettura (`mode=ro`), oppure `"unavailable"` per file assente, non apribile o senza la tabella; uno zero vero resta zero | `_esche_di` (146), `tier_inventory` (173) | `tests/test_inventario_dei_tier.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 2 | `verimem/tier_inventory.py:80` `_percorsi` | i nove tier (facts, entities, episodes, skills, documents, decisions, adjudications, transcripts, self_model) con lo store che il prodotto apre davvero, derivato da `CONFIG` relativo a `data_dir`; i quattro «che l'inventario saltava» aggiunti il 06/08 | `_esche_di` (145), `tier_inventory` (170) | via il test | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 3 | `verimem/tier_inventory.py:92` `_percorsi._come_config` | il percorso di `CONFIG` ricollocato sotto `root`; fuori da `data_dir` → il default | `_percorsi` (99, 107, 110) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 81 passed |
| 4 | `verimem/tier_inventory.py:138` `_esche_di` | i file vicini col nome ovvio del tier (`semantic.db`, `episodes.db`, …) con righe e MB, escluso lo store vero | `tier_inventory` (175) | via il test | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 5 | `verimem/tier_inventory.py:151` `tier_inventory` | per tier: store, tabella contata, righe o `"unavailable"`, esche; e la `note` che spiega perché mai 0 | quattro porte (sopra) | `tests/test_inventario_dei_tier.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |

Reperti: (a) `_esche_di` richiama `_percorsi(root)` per ogni esca (riga 145): ricalcola l'elenco dei tier a ogni file esca, costo trascurabile ma è la forma «ricalcolo dentro il ciclo»; (b) il docstring misura una lezione già in memoria da luglio e dice che «nessuna superficie del prodotto la dichiarava»: ora quattro porte la dichiarano, ed è l'unico inventario che nomina le trappole invece di evitarle; (c) `documents.db` e `self_model.db` hanno il percorso scritto qui (non da `CONFIG`): due su nove non seguono la prima delle tre scelte (letto). Nessun P0.
