# Mappa di `verimem/dashboard.py` — 13 righe, 186 righe di codice (lead, 08/09 22:00)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_dashboard_api.py` → `28 passed, 1 warning in 12.11s`, EXIT=0 (altri sette file di test importano `verimem.dashboard`: `tests/test_ide*.py`, `tests/test_feedback.py`, `tests/test_eventbus_unsubscribe_leak.py`, `tests/security/test_pentest_validation.py`, `tests/security/test_secrets_redaction.py`; non eseguiti qui, sono della mappa di `ide.py` (ws1) e di `dashboard_routes/` (ws8)). Chiamanti letti: `verimem/cli.py:2721-2723` (il comando `verimem dashboard` importa `app` e lancia uvicorn su `verimem.dashboard:app`), `verimem/tui.py:177,241` (`PRESETS`), `verimem/dashboard_routes/layout.py:42`. Il README non nomina né il comando `verimem dashboard` né questa app: le righe 537-561 parlano della console `/ui` servita dal gateway, un'altra superficie. Questo file è il punto d'ingresso sottile del vecchio monolite (2.338 righe spezzate in `dashboard_routes/`, ws8) e porta gli adattatori di compatibilità per i test.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/dashboard.py:53` `_ag` | accessorio legacy dell'agente singleton, tenuto per i test che lo monkeypatchano; il codice delle route usa `layout.get_agent()` | nessuno nel prodotto (`git grep -nE "\b_ag\(" -- verimem/` → solo qui); i test | i test che fanno `monkeypatch.setattr(dash, "_ag", ...)` | - | MAI CHIAMATA (dichiarata: esiste per i test) | comando sopra |
| 2 | `verimem/dashboard.py:80` `__getattr__` | PEP 562: `_SESSION_TOKEN` letto dal modulo auth, altrimenti `AttributeError` | Python, all'accesso a un nome mancante | i test legacy che leggono `dash._SESSION_TOKEN` | - | FUNZIONA COME PROMESSO (compatibilità per i test, letto) | pytest 28 passed |
| 3 | `verimem/dashboard.py:90` `_DashboardModule` | sottoclasse di `ModuleType` installata sul modulo stesso (riga 109) perché `monkeypatch.setattr(dash, "_SESSION_TOKEN", v)` cambi il binding vero in `dashboard_routes.auth` invece di un attributo morto | riga 109, all'import | i test che monkeypatchano il token | - | FUNZIONA COME PROMESSO (compatibilità per i test) | pytest 28 passed |
| 4 | `verimem/dashboard.py:97` `_DashboardModule.__getattribute__` | rilegge il token dal modulo auth | Python | come sopra | - | FUNZIONA COME PROMESSO | pytest 28 passed |
| 5 | `verimem/dashboard.py:102` `_DashboardModule.__setattr__` | scrive il token nel modulo auth | Python | come sopra | - | FUNZIONA COME PROMESSO | pytest 28 passed |
| 6 | `verimem/dashboard.py:113` `_lifespan` | all'avvio dell'app crea il token di sessione su disco (`bootstrap_token`) prima di qualunque richiesta | FastAPI (riga 118) | `tests/test_dashboard_api.py` (il client di test avvia l'app) | - | FUNZIONA COME PROMESSO | pytest 28 passed |
| 7 | `verimem/dashboard.py:133` `auth_info` | `GET /api/auth/info`: se l'autenticazione è richiesta e dove sta il file del token; mai il token | FastAPI | `git grep -n "auth/info" -- tests/` → da verificare nella mappa di `dashboard_routes` (ws8); qui non eseguito per nome | - | NON MISURATO (la route esiste, un test con quel percorso non l'ho trovato eseguendo il solo `test_dashboard_api.py`) | - |
| 8 | `verimem/dashboard.py:156` `_read_static` | legge un file da `verimem/static/` | le tre route sotto | via le route | - | FUNZIONA COME PROMESSO (plumbing) | pytest 28 passed |
| 9 | `verimem/dashboard.py:160` `ide_page` | `GET /ide` → l'HTML dell'IDE | FastAPI | `tests/test_ide.py` (ws1, non eseguito qui) | - | NON MISURATO (qui) | - |
| 10 | `verimem/dashboard.py:165` `ide_js_route` | `GET /static/ide.js` | FastAPI | `tests/test_ide.py` (ws1) | - | NON MISURATO (qui) | - |
| 11 | `verimem/dashboard.py:170` `chat_js` | `GET /static/chat.js` dal file in `static/` (esiste: `ls verimem/static` → chat.js) | FastAPI | `tests/test_dashboard_api.py` (28 passed; la route per nome non è verificata) | - | FUNZIONA COME PROMESSO (il file c'è, la route è registrata; letto) | pytest 28 passed; `ls verimem/static` |
| 12 | `verimem/dashboard.py:175` `settings_js` | `GET /static/settings.js` (il file esiste) | FastAPI | come sopra | - | FUNZIONA COME PROMESSO (letto) | `ls verimem/static` |
| 13 | `verimem/dashboard.py:180` `events_js` | `GET /static/events.js` (il file esiste) | FastAPI | come sopra | - | FUNZIONA COME PROMESSO (letto) | `ls verimem/static` |

Reperti: (a) il comando `verimem dashboard` e questa app (porta 8765, CORS su loopback) non compaiono nel README: due superfici web (dashboard legacy e console `/ui` del gateway) e una sola raccontata; (b) un terzo del file è macchinario di compatibilità per i test (`_ag`, `__getattr__`, `_DashboardModule`): vive nel prodotto per non rompere i test. Nessun P0.
