# Mappa di `verimem/trust_ledger.py` — 8 righe, 221 righe di codice (lead, 08/09 21:12)

Letto per intero. Prova eseguita sul checkout a `7b9e8ca1` (i 16 commit fino a `20257636` non toccano questo file né i test eseguiti: `git diff --stat 7b9e8ca1 20257636` vuoto su di essi): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_trust_ledger.py tests/test_trust_ledger_honesty.py tests/test_trust_ledger_scale.py tests/test_stats_declares_the_ledger_window.py` → `23 passed, 22 warnings in 60.32s`, EXIT=0. Chiamanti letti: `verimem/client.py:599-600` (il registro nasce con la Memory, sullo stesso file SQLite), `verimem/client.py:3156-3168` (l'ingest di conversazione conta a lotti con `record_many`), `verimem/client.py:3170-3242` (`trust_stats` lo legge e pubblica `ledger_write_failures`), `verimem/cli.py:2079` (`stats` legge `MIN(ts)` direttamente dalla tabella). Il README lo chiama «Trust odometer» (riga 284: `m.trust_stats()` / `verimem stats`, «persistent …»; righe 573, 588, 592).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/trust_ledger.py:59` `TrustLedger` | contatore persistente di cosa il gate ha FATTO (admitted / quarantined / rejected / abstained): fail-open (mai rompe una scrittura), nessun testo di proposizione (no PII), stesso file DB dello store (backup e cancellazione del tenant lo coprono gratis) | `verimem/client.py:599-600` | `tests/test_trust_ledger.py` (11), `tests/test_trust_ledger_honesty.py` (4) | `README.md:284` («Trust odometer»), `:588` | FUNZIONA COME PROMESSO | pytest 23 passed; `::test_ledger_is_fail_open`, `::test_no_proposition_text_in_ledger`, `::test_ledger_persists_across_reopen` |
| 2 | `verimem/trust_ledger.py:62` `TrustLedger.__init__` | tiene il percorso e il contatore degli eventi PERSI in questo processo (`write_failures`), reso visibile da `Memory.trust_stats` come `ledger_write_failures` invece di zeri mascherati | `verimem/client.py:600` | `tests/test_trust_ledger_honesty.py::test_ledger_write_failures_are_visible`, `tests/test_trust_ledger_scale.py::test_failopen_still_counts_failures` | - | FUNZIONA COME PROMESSO | pytest 23 passed |
| 3 | `verimem/trust_ledger.py:70` `TrustLedger._connect` | apre la connessione, crea lo schema (eventi + totali) e fa il backfill una volta | `record_many` (165), `stats` (190) | tutti i test del modulo | - | FUNZIONA COME PROMESSO (plumbing esercitato da ogni test) | pytest 23 passed |
| 4 | `verimem/trust_ledger.py:77` `TrustLedger._ensure_backfill` | migrazione una tantum per gli store scritti prima dei totali: deriva i totali dalle righe grezze in una scansione sola, marca fatto; idempotente anche con due processi in corsa (DO NOTHING + chiave primaria) | `_connect` (74) | `tests/test_trust_ledger_scale.py::test_backfill_from_preexisting_rows`, `::test_backfill_is_idempotent_for_layers` | - | FUNZIONA COME PROMESSO | pytest 23 passed |
| 5 | `verimem/trust_ledger.py:129` `TrustLedger._bump_totals` | aggiorna i totali per azione, per layer e per giorno NELLA STESSA transazione dell'insert: i contatori non possono divergere dalle righe che riassumono | `record_many` (172) | `tests/test_trust_ledger_scale.py::test_totals_match_raw_after_mixed_writes` | - | FUNZIONA COME PROMESSO | pytest 23 passed |
| 6 | `verimem/trust_ledger.py:149` `TrustLedger.record` | un evento del gate; non alza mai | `verimem/client.py` (le scritture singole: admitted/quarantined/rejected/abstained, letto in `trust_stats` e nei test del gate) | `tests/test_trust_ledger.py::test_clean_add_counts_admitted`, `::test_unsupported_selfclaim_counts_quarantined`, `::test_reject_counts_rejected`, `::test_abstention_counts` | `README.md:284` | FUNZIONA COME PROMESSO | pytest 23 passed |
| 7 | `verimem/trust_ledger.py:155` `TrustLedger.record_many` | `n` eventi identici in una transazione (l'ingest conta lotti interi); stesso contratto fail-open | `verimem/client.py:3159,3162,3165` | `tests/test_trust_ledger_honesty.py::test_conversation_ingest_is_ledgered`, `tests/test_trust_ledger_scale.py` | - | FUNZIONA COME PROMESSO | pytest 23 passed |
| 8 | `verimem/trust_ledger.py:176` `TrustLedger.stats` | totali per azione e per layer in O(1) righe (mai una scansione della tabella eventi: 2213 ms a 1M misurati), la serie giornaliera degli ultimi `daily_days` giorni UTC in O(giorni), `since`; zeri e non errore su uno store nuovo o illeggibile | `verimem/client.py:3170` (`trust_stats`), da lì CLI `stats`, `GET /v1/stats`, la console | `tests/test_trust_ledger.py::test_daily_series_buckets_by_day`, `::test_daily_series_is_capped_and_fail_open`, `::test_gateway_tenant_stats_endpoint`, `tests/test_trust_ledger_scale.py::test_stats_is_o1_reads_not_full_scan`, `::test_daily_window_and_since_survive`, `tests/test_stats_declares_the_ledger_window.py` (2) | `README.md:284,573,588` | FUNZIONA COME PROMESSO | pytest 23 passed |

Reperti: nessuno nuovo. Il numero «2213 ms a 1M eventi» del commento (riga 35) è una misura storica del 17/07, non un test; il test di scala prova solo che `stats` non fa la scansione completa (`test_stats_is_o1_reads_not_full_scan`). Nessun P0.
