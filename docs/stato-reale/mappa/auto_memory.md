# Mappa di `verimem/auto_memory.py` — 6 righe, 93 righe di codice (lead, 08/09 21:25)

Letto per intero. Prova eseguita su origin/main 20257636: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_auto_memory.py` → `4 passed, 22 warnings in 55.02s`, EXIT=0. Chiamante letto: `verimem/__init__.py:76-78` (export pigro `verimem.AutoMemory`, API pubblica: `tests/test_nessun_modulo_nasce_irraggiungibile.py:170-177` la tiene nella lista dei moduli VIVI). Nessun altro modulo del prodotto la istanzia: è opt-in per costruzione, come promette il README alla riga 280 («Opt-in auto-memory — `AutoMemory(memory).observe(role, text)` … through the SAME gated pipeline as explicit writes … if you don't instantiate it, it doesn't exist»).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/auto_memory.py:27` `AutoMemory` | osserva i turni di una conversazione e li ingerisce a soglia attraverso la STESSA pipeline dell'ingest esplicito (estrazione → consolidate → gate → provenienza → redazione); opt-in: se l'app non la istanzia non esiste; nessun thread | `verimem/__init__.py:77` (export), l'applicazione dell'utente | `tests/test_auto_memory.py` (4) | `README.md:280-283` | FUNZIONA COME PROMESSO | pytest 4 passed; letto: `_do_flush` chiama `self.memory.add(window, ...)`, cioè la porta dell'ingest esplicito, nessun canale privilegiato |
| 2 | `verimem/auto_memory.py:41` `AutoMemory.__init__` | pretende una Memory con l'`llm` di estrazione (`ValueError` altrimenti); `min_turns` ≥ 1, `max_buffer_turns` ≥ `min_turns` | l'applicazione dell'utente | i quattro test costruiscono l'oggetto; il `ValueError` senza llm non ha un test con il nome (letto) | `README.md:280` | FUNZIONA COME PROMESSO (il rifiuto senza `llm` è letto, non provato: NON MISURATO) | pytest 4 passed |
| 3 | `verimem/auto_memory.py:59` `AutoMemory.pending_turns` | quanti turni osservati non sono ancora ingeriti | nessuno nel prodotto (`git grep -n pending_turns -- verimem/` → solo qui) | `tests/test_auto_memory.py::test_no_flush_below_buffer_threshold` (letto: legge `pending` dal dict di ritorno, non la proprietà) | - | MAI CHIAMATA (candidata: la stessa informazione viaggia già nel dict `pending` di `observe`/`flush`) | comando sopra |
| 4 | `verimem/auto_memory.py:63` `AutoMemory.observe` | registra un turno (ignora i vuoti) e flusha da solo alla soglia; rende `{flushed, stored, rejected, pending}` | l'applicazione dell'utente | `::test_no_flush_below_buffer_threshold`, `::test_flush_fires_at_buffer_threshold_and_stores_gated`, `::test_empty_and_blank_messages_are_ignored` | `README.md:280` | FUNZIONA COME PROMESSO | pytest 4 passed |
| 5 | `verimem/auto_memory.py:74` `AutoMemory.flush` | ingest esplicito del buffer; sotto `min_turns` è un no-op DICHIARATO (`flushed=False` col conteggio) | l'applicazione dell'utente (fine sessione) | `::test_manual_flush_respects_min_turns` | `README.md:281` | FUNZIONA COME PROMESSO | pytest 4 passed |
| 6 | `verimem/auto_memory.py:83` `AutoMemory._do_flush` | svuota il buffer e lo passa a `memory.add` con `conversation_id` numerato (`<id>#n`) così il TrustReport distingue le finestre; rende i conteggi dell'ingest | `observe` (70), `flush` (81) | `::test_flush_fires_at_buffer_threshold_and_stores_gated` | `README.md:282` («through the SAME gated pipeline») | FUNZIONA COME PROMESSO | pytest 4 passed; letto: nessun bypass del gate, la finestra entra da `Memory.add(messages)` |

Reperti: il claim del README (righe 280-283) è vero riga per riga e presidiato; `pending_turns` è un accessorio doppione. Nessun P0.
