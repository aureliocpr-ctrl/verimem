# Mappa — `verimem/gateway.py`

*ws3 Galileo. 1.798 righe, **87 fra funzioni, classi e metodi** (il conteggio è
quello del righello del lead, `scripts/mappa_completa.py`, che include i metodi
qualificati e le dunder). Il gateway è la **porta HTTP**: quello che promette
sta scritto per esteso nell'intestazione del modulo, ed è la parte del prodotto
dove una promessa sbagliata non è un difetto di comodità ma di sicurezza.*

**Metodo.** I claim dell'intestazione si provano **attaccandoli**, non
rileggendoli: il tenant sbagliato viene dichiarato nel corpo della richiesta, la
chiave viene cercata **in chiaro dentro i byte del database**, i nomi di tenant
pericolosi vengono creati davvero. Ogni controllo negativo ha accanto il suo
**controllo positivo**: un rifiuto che rifiuta tutto non prova niente.
Banco: `ws3-mappa-prova-gateway-auth-e-tenant.py` (09/09 12:15), avviato con
`fastapi.testclient.TestClient` — nessuna porta aperta sulla macchina.

## Autenticazione, tenant, chiavi (gateway.py 62-360, 792-960)

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/gateway.py:792` `create_app` | «costruisce l'app FastAPI del gateway; `keys` iniettabile (test)… `admin_key` (None = default): SENZA, gli endpoint `/admin/*` **non esistono** — un gateway senza control plane è byte-identico a prima» | **FUNZIONA COME PROMESSO** | `create_app(data_dir=tmp, keys=keys)` costruisce l'app e serve; con `TestClient` tutti gli endpoint `/v1/*` rispondono. Il ramo `admin_key` è **NON MISURATO** (prossimo blocco) |
| 2 | `verimem/gateway.py:925` `create_app._tenant` | il cuore del claim: «il tenant deriva **SOLO** dalla chiave presentata, mai da un campo della richiesta — niente path traversal, niente confused deputy» | **FUNZIONA COME PROMESSO — attaccato, regge** | `POST /v1/memories` con la chiave di **acme** e `"tenant_id": "globex"` **nel corpo** → 200, `stored: True`. Poi: `GET /v1/search` con la chiave di acme → **1 risultato**; con la chiave di globex → **0**. E su disco nascono due DB distinti (`tenants/acme/memory.db`, `tenants/globex/memory.db`, cartelle `['acme','globex']`). Il campo del corpo **non ha spostato niente** |
| 3 | `verimem/gateway.py:956` `create_app.health` | liveness, e «`/v1/health` non è mai limitato» | **FUNZIONA COME PROMESSO** | `GET /v1/health` **senza chiave** → `200 {'ok': True, 'version': '0.7.7'}`; ed è l'unico: `GET /v1/search` senza chiave → **401** `{'detail': 'invalid or missing API key'}` |
| 4 | `verimem/gateway.py:251` `GatewayKeys` · `verimem/gateway.py:254` `GatewayKeys.__init__` · `verimem/gateway.py:266` `GatewayKeys._connect` | il registro delle chiavi su `gateway_keys.db` | **FUNZIONA COME PROMESSO** | il file nasce al primo `create`; tabelle `['gateway_keys','gateway_usage']`, colonne `['key_id','key_hash','tenant_id','name','plan','created_at','revoked_at']` |
| 5 | `verimem/gateway.py:275` `GatewayKeys.create` | «crea una chiave e la ritorna **IN CHIARO** — l'unica volta che esiste fuori dallo sha256»; `tenant_id` è «uno slug validato (finisce in un path di filesystem)» | **FUNZIONA COME PROMESSO**, e la validazione è provata **su sette nomi** | chiave `vm_` + 40 esadecimali (lunghezza **43**, prefisso `vm_` → `True`). Respinti con `ValueError` parlante: `'../evil'` e `'acme/../x'` (slug) · `'acme.'` («Windows strips trailing dots, collapsing it onto another tenant's directory») · `'con'` («reserved device name on Windows») · `'ACME'` (maiuscole) · 65 caratteri · stringa vuota. **Controllo positivo**: `'acme-2'` → creato. Un rifiuto che rifiuta tutto non proverebbe niente |
| 6 | `verimem/gateway.py:272` `GatewayKeys._hash` | «a riposo solo lo sha256 (`gateway_keys.db`)» | **FUNZIONA COME PROMESSO — misurato sui BYTE, non sulla struttura** | ho cercato la chiave in chiaro **dentro i byte del file**: `k_acme.encode() in gateway_keys.db (bytes)` → **False**. Nella riga c'è `key_hash` (`43cf7e9947011b6a0601449a03d5b66e95…`), non la chiave |
| 7 | `verimem/gateway.py:320` `GatewayKeys.resolve` | risolve la chiave presentata nel suo tenant, «confronto sull'hash via `secrets.compare_digest`» | **FUNZIONA COME PROMESSO** | chiave vera in `X-API-Key` → **200**; la stessa in `Authorization: Bearer` → **200**; una chiave inventata della forma giusta (`vm_` + 40 zeri) → **401** |
| 8 | `verimem/gateway.py:340` `GatewayKeys.revoke` | «revoca senza cancellare (audit)» | **FUNZIONA COME PROMESSO, ed è chirurgica** | `revoke('62fde42659db…')` → `True`; **quella** chiave → **401**, l'altra chiave (altro tenant) → **200**. La riga resta nel DB con `revoked_at` valorizzato |
| 9 | `verimem/gateway.py:350` `GatewayKeys.list` | elenca le chiavi con i loro metadati | **FUNZIONA COME PROMESSO** | usata per prendere il `key_id` da revocare: la riga porta `key_id`, `tenant_id`, `plan`, `created_at` |
| 10 | `verimem/gateway.py:310` `GatewayKeys.plan_for_tenant` | il piano commerciale del tenant, che decide il tetto di richieste | **NON MISURATO** | il piano è `free` di default e il tetto (`rate_limit_per_minute`) è 0 in questa app: il ramo che conta — il 429 con `Retry-After` — si misura con un'app costruita apposta, prossimo blocco |

## Il write path HTTP: gate, LLM, corpi malformati (gateway.py 1004-1180)

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 11 | `verimem/gateway.py:1048` `create_app.add_memory` | «stessa semantica dell'SDK — **ogni write passa il gate anti-confab**» | **FUNZIONA COME PROMESSO** | `POST /v1/memories` con un claim che la fonte non sostiene → **200** con `{'moat': 'failed', 'quarantined_by': 'moat', 'stored': True, 'status': 'quarantined'}`. Il fatto entra ma **non è servito**: la porta HTTP dà la stessa risposta dell'SDK, compreso il campo che dice **chi** ha fermato |
| 12 | `verimem/gateway.py:1069` `create_app._add_memory_impl` | «validazione dell'input al bordo: un tipo malformato è un errore del client (400), **mai un 500** — un 500 non gestito su input costruito è un endpoint che si può far cadere = un DoS» | **FUNZIONA COME PROMESSO — quattro corpi, quattro 400** | corpo vuoto `{}` → **400** (la guardia contro lo scarto silenzioso) · `{'content': 123}` → **400** · `{'messages': 'non una lista'}` → **400** · `topic` come dizionario → **400**. Nessun 500 |
| 13 | `verimem/gateway.py:1048` `create_app.add_memory` (ramo `messages`) | «**niente LLM implicito** (O4): l'ingest conversazionale è disponibile solo se l'operatore costruisce l'app con un `llm`; senza, **400 onesto**» | **FUNZIONA COME PROMESSO, e l'errore insegna** | `POST /v1/memories` con `messages` su un'app senza `llm` → **400**: «conversation ingest needs a server-side extraction llm: start the gateway with one (`create_app(llm=...)`); single verified facts work witho…». Dice cosa manca, come rimediare, e **cosa funziona lo stesso** |
| 14 | `verimem/gateway.py:1178` `create_app.search` | la lettura per tenant | **FUNZIONA COME PROMESSO** (isolamento provato alla riga 2) | `GET /v1/search?q=canone capannone` → 200 con i risultati del **proprio** tenant; il `k` di default di questa porta è **4** (letto nel codice, riga 621 per la variante non-tenant) |
| 15 | `verimem/gateway.py:383` `_Metering` · `verimem/gateway.py:409` `_Metering.bump` | il contatore d'uso per tenant | **FUNZIONA COME PROMESSO** (misurato di riflesso) | dopo il giro di prove, `gateway_usage` porta `['acme', '2026-09-09', 4, 3, 1, 1, 0]`: **4 richieste, 3 letture, 1 scrittura, 1 memorizzata, 0 respinte** — i conti tornano con quello che ho fatto io in quella sessione |

## Le difese del data plane: corpo, header, tetto, idempotenza, quota (gateway.py 62-767, 900-1046)

*Banco: `ws3-mappa-prova-gateway-porte.py` (09/09 13:53). Ogni difesa è provata
**attaccandola**: un corpo troppo grande, una chiave in eccesso, un secondo
invio identico, un piano al limite.*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 16 | `verimem/gateway.py:81` `_host_only` | «toglie la porta dall'header `Host`, **correttamente per IPv6** (opus LOW-6: `"[::1]".rsplit(":",1)[0]` dava `"[:"` → il client loopback IPv6 cadeva in 401)» | **FUNZIONA COME PROMESSO — il caso del bug è il controllo positivo** | `'localhost:8000'` → `'localhost'` · `'127.0.0.1:9000'` → `'127.0.0.1'` · **`'[::1]:8000'` → `'[::1]'`** · `'[::1]'` → `'[::1]'` · `'esempio.it'` → invariato · `None` e `''` → `''`. Il caso che aveva rotto il loopback IPv6 ora torna intero |
| 17 | `verimem/gateway.py:461` `_gateway_min_relevance` | «il pavimento di astensione della porta in lettura. `ENGRAM_GATEWAY_MIN_RELEVANCE`: `auto` (default — lo store si auto-calibra per tenant), oppure un numero» | **FUNZIONA COME PROMESSO** | senza variabile → `'auto'` · `'auto'` → `'auto'` · `'0.75'` → `0.75` · **`'spazzatura'` → `'auto'`** (ripiega sul default *dichiarato*, non su un numero) · `'-1'` → `0.0`. ⚠️ Da confrontare con **T-MAP-7** su `_pavimento_avviso`, dove un valore malformato ripiega su un **terzo** valore: qui il ripiego è quello giusto, e le due funzioni sorelle si comportano in modo diverso |
| 18 | `verimem/gateway.py:502` `_parse_flow_line` | «tiene la riga solo se è un evento `flow.*` **per QUESTO tenant** (privacy). `see_untenanted` lascia vedere all'operatore locale anche l'attività senza tenant» | **FUNZIONA COME PROMESSO — la privacy provata da tutti i lati** | riga del **mio** tenant → passa · riga di **un altro tenant** → `None` · riga **senza tenant** con `see_untenanted=False` → `None`, con `True` → passa · evento che non inizia per `flow.` → `None` · JSON rotto → `None`. ⚠️ Nota mia: la forma vera è `{"name": "flow.…", "payload": {"tenant": …}}` — col mio primo formato inventato tornava `None` **su tutto**, incluso il caso che doveva passare |
| 19 | `verimem/gateway.py:566` `_quota_reserve` · `verimem/gateway.py:584` `_quota_release` | «prenota atomicamente uno slot contro il tetto del piano. **TOCCTOU-safe**: il conteggio reale e le prenotazioni in volo sono valutati insieme sotto lo stesso lock» | **FUNZIONA COME PROMESSO** | piano con `max_facts=3` e **1 fatto già presente**: prenotazione 1 → `True`, 2 → `True`, **3 → `False`**, 4 e 5 → `False`, con `pending` fermo a 2. Dopo una `_quota_release` → `pending` scende a 1. Con `max_facts=None` (illimitato) e **10.000** fatti già presenti → `True`: il piano senza tetto non finge di averne uno |
| 20 | `verimem/gateway.py:663` `_BodyLimitMiddleware` · `verimem/gateway.py:674` `_BodyLimitMiddleware.__init__` · `verimem/gateway.py:678` `_BodyLimitMiddleware.__call__` · `verimem/gateway.py:707` `_BodyLimitMiddleware._reject` | «middleware ASGI anti-DoS sul data plane. Il guard precedente si fidava del **SOLO** header `Content-Length`» | **FUNZIONA COME PROMESSO** | `max_body_bytes=2048`, corpo da ~5 KB → **413**. ⚠️ **NON MISURATO** il caso che ha motivato il middleware: una richiesta *chunked* che mente sul `Content-Length` — con `TestClient` non sono riuscito a costruirla, e lo scrivo invece di far passare il caso facile per quello difficile |
| 21 | `verimem/gateway.py:757` `_SecurityHeadersMiddleware` · `verimem/gateway.py:766` `_SecurityHeadersMiddleware.__init__` · `verimem/gateway.py:769` `_SecurityHeadersMiddleware.__call__` · `verimem/gateway.py:774` `_SecurityHeadersMiddleware.__call__.send_wrapper` | «timbra gli header difensivi su **OGNI** risposta — riuscita, errore, in streaming, o il 413 corto-circuitato dal guard sul corpo» | **FUNZIONA COME PROMESSO — provato su 200, 401 e 413** | su `/v1/health` (200), su `/v1/search` (200 e **401**) e sul **413**: `x-content-type-options: nosniff`, `x-frame-options: DENY`, `referrer-policy: strict-origin-when-cross-origin`, `permissions-policy: geolocation=(), camera=(), microphone=(), browsing-topics=()`, `content-security-policy: frame-ancestors 'none'`. È il caso in cui «ogni» significa davvero ogni |
| 22 | `verimem/gateway.py:900` `create_app._check_rate` | tetto per **chiave** su finestra scorrevole di 60 s; «oltre, 429 con `Retry-After`»; «`/v1/health` non è mai limitato» | **FUNZIONA COME PROMESSO — tutte e tre le parti** | con `rate_limit_per_minute=2`: richiesta 1 → 200 · 2 → 200 · **3 → 429 con `Retry-After: 60`** · 4 → 429 · e `/v1/health` → **200** anche a tetto superato |
| 23 | `verimem/gateway.py:915` `create_app._plan_for` · `verimem/gateway.py:310` `GatewayKeys.plan_for_tenant` | il piano del tenant decide tetti e funzioni | **FUNZIONA COME PROMESSO** | `/v1/quota` → `{'plan': 'free', 'facts_used': 2, 'facts_limit': 1000, 'facts_remaining': 998, …}`: il piano arriva fino alla risposta, coi numeri del tetto |
| 24 | `verimem/gateway.py:1004` `create_app._idem_entry_lock` · `verimem/gateway.py:1015` `create_app._idem_release` · `verimem/gateway.py:1021` `create_app._idem_get` · `verimem/gateway.py:1036` `create_app._idem_put` | «UN lock su check→execute→store per una data chiave: un ritentativo simultaneo **si blocca qui e poi rigioca la ricevuta originale**, invece di correre nel varco e memorizzare un gemello (audit F6)» | **FUNZIONA COME PROMESSO** | due `POST /v1/memories` identici con lo stesso `Idempotency-Key`: **stessa ricevuta**, `id` `c7182c6e1cd8` in entrambi (`True`), e la ricerca sul tema serve **2** fatti in tutto (quello di prova più questo), non 3. ⚠️ **NON MISURATA** la corsa *simultanea* vera (due thread nello stesso istante): qui i due invii sono in sequenza |
| 25 | `verimem/gateway.py:359` `_TenantMemories` · `verimem/gateway.py:362` `_TenantMemories.__init__` · `verimem/gateway.py:370` `_TenantMemories.get` | «cache {tenant_id → Memory} con un DB **isolato** per tenant» | **FUNZIONA COME PROMESSO** | provato alla riga 2 del primo blocco: due tenant → `tenants/acme/memory.db` e `tenants/globex/memory.db`, e il fatto scritto da uno **non** è visibile all'altro |
| 26 | `verimem/gateway.py:383` `_Metering` · `verimem/gateway.py:404` `_Metering.__init__` · `verimem/gateway.py:409` `_Metering.bump` · `verimem/gateway.py:428` `_Metering.totals` · `verimem/gateway.py:441` `_Metering.usage_for` | «contatori d'uso per tenant… il ponte *software → servizio*: senza contatori non si fattura e non si vedono gli abusi»; `usage_for` = «le voci per giorno che una fattura mensile somma» | **FUNZIONA COME PROMESSO** | `/v1/usage` → `{'tenant_id': 'acme', 'plan': 'free', 'since': None, 'days': [{'day': '2026-09-09', 'requests': …}]}`, e nel database `gateway_usage` la riga `['acme', '2026-09-09', 4, 3, 1, 1, 0]` corrisponde alle richieste che ho fatto io |
| 27 | `verimem/gateway.py:867` `create_app._bytes_safe_validation_handler` | il gestore degli errori di validazione | **FUNZIONA COME PROMESSO** (di riflesso) | è la via per cui i quattro corpi malformati del primo blocco tornano **400 e non 500**; non l'ho chiamata direttamente |
| 28 | `verimem/gateway.py:62` `mark_multi_writer` | «dichiara che **questo processo** serve una memoria condivisa: N sessioni dietro un tenant» | **NON MISURATO** | non l'ho chiamata: cambia uno stato di processo che riguarda il comportamento concorrente, e con `TestClient` in un processo solo non avrei potuto distinguere l'effetto |
| 29 | `verimem/gateway.py:479` `_flow_ctx` · `verimem/gateway.py:489` `_flow_ctx_reset` · `verimem/gateway.py:519` `_read_flow_bytes` · `verimem/gateway.py:599` `_shadow_observe` · `verimem/gateway.py:634` `_replay_receive` · `verimem/gateway.py:639` `_replay_receive.receive` · `verimem/gateway.py:649` `_prepend_message` · `verimem/gateway.py:653` `_prepend_message.receive` | il contesto di flusso per la Engine Room, il lettore incrementale del flusso, l'osservatore ombra del ledger adattivo, e i due `receive` ASGI che rigiocano il corpo | **NON MISURATE** (sei funzioni interne) | `_replay_receive` e `_prepend_message` sono esercitate **di riflesso** dal middleware del corpo (senza di loro il 413 e il 400 non funzionerebbero), ma non le ho isolate; `_read_flow_bytes` e `_shadow_observe` richiedono un file di eventi vivo e un ledger adattivo per tenant. Lo scrivo invece di dedurre |

## Le porte di lettura, gestione e amministrazione (gateway.py 1179-1780)

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 30 | `verimem/gateway.py:962` `create_app.quota` | il consumo contro il tetto del piano | **FUNZIONA COME PROMESSO** | `{'plan': 'free', 'facts_used': 2, 'facts_limit': 1000, 'facts_remaining': 998, …}` |
| 31 | `verimem/gateway.py:970` `create_app.usage` | l'uso per giorno | **FUNZIONA COME PROMESSO** | vedi riga 26 |
| 32 | `verimem/gateway.py:1194` `create_app.explain` | «`explain` è il TrustReport via HTTP» | **FUNZIONA COME PROMESSO** | `{'query': 'canone', 'as_of': None, 'deep': False, 'k': 5, 'min_relevance': 0.0, 'ranking_d…'}`: la porta espone **la soglia usata** insieme ai risultati |
| 33 | `verimem/gateway.py:1213` `create_app.answer` | la risposta verificata, che richiede un LLM lato server | **FUNZIONA COME PROMESSO** | senza `llm` → **400**: «answering needs a server-side llm: start the gateway with one (`create_app(llm=...)`); **/v1/search works without it**» — dice anche cosa continua a funzionare |
| 34 | `verimem/gateway.py:1251` `create_app.correct` | la porta della correzione | **FUNZIONA COME PROMESSO** | `{'verdict': 'ACCEPT', 'answer': "Il canone del capannone 12 e' 5900 euro.", 'served_id': …}`: **verdetto esplicito** accanto alla risposta |
| 35 | `verimem/gateway.py:1278` `create_app.get_memory` | un fatto per id | **FUNZIONA COME PROMESSO** | `{'id': '474dc75d28a1', 'text': "Il canone…", 'status': 'model_claim', …}`; dopo la cancellazione lo stesso id → **404** |
| 36 | `verimem/gateway.py:1286` `create_app.delete_memory` | la cancellazione (privacy/GDPR) | **FUNZIONA COME PROMESSO** | `DELETE /v1/memories/<id>` → **200**, e la lettura successiva → **404**. ⚠️ **NON MISURATO** il parametro `purge_history` alla porta |
| 37 | `verimem/gateway.py:1298` `create_app.tenant_stats` | le statistiche del tenant | **FUNZIONA COME PROMESSO** | `{'tenant': 'acme', 'trust': {'ledger': {'admitted': 2, 'quarantined': 0, …}}}`: il contatore del gate arriva alla porta |
| 38 | `verimem/gateway.py:1308` `create_app.quarantine` | il registro dei claim bloccati via HTTP | **FUNZIONA COME PROMESSO** | `{'items': [], 'count': 0}` su un tenant senza quarantinati: **conteggio esplicito**, non una lista muta |
| 39 | `verimem/gateway.py:1332` `create_app.tiers` | l'inventario dei livelli | **FUNZIONA COME PROMESSO** | `{'data_dir': '…/tenants', 'tiers': …}` — e il `data_dir` mostrato è quello **del tenant**, non quello globale |
| 40 | `verimem/gateway.py:1348` `create_app.retirements` | i ritiri, coi filtri e i conteggi | **FUNZIONA COME PROMESSO** | `{'items': [], 'count': 0}`. ⚠️ **NON MISURATI** i parametri `counts`, `mismatches`, `breakdown` |
| 41 | `verimem/gateway.py:1393` `create_app.restore_memory` | il salvataggio di un fatto bloccato per errore | **FUNZIONA COME PROMESSO** (chiamata riuscita) | `POST /v1/memories/<id>/restore` → **200**. ⚠️ **NON MISURATO** il caso che conta: ripristinare un fatto **davvero quarantinato** e vederlo tornare nella ricerca — qui il fatto era già servito |
| 42 | `verimem/gateway.py:1416` `create_app.undo_operation` | annulla un'operazione per maniglia | **FUNZIONA COME PROMESSO, con una nota sul codice HTTP** | con una maniglia **inventata** → **200** e corpo `{'ok': False, 'op_id': 'op-che-non-esiste-mai', 'action': 'not_found'}`: onesto **nel corpo**, ma un 200 su una cosa che non esiste è la forma che fa credere di aver annullato qualcosa a chi guarda solo il codice. Nota, non ticket. ⚠️ **NON MISURATO** l'annullamento di una supersessione **vera** via HTTP: nella prova la seconda scrittura è stata quarantinata **senza** superare la prima (`superseded_by: None`, il primo fatto ancora servito), quindi non c'era niente da annullare — ed è anche il motivo per cui la ricevuta HTTP non portava `undo_op_id`: **non è un buco della porta**, è che non c'era stata supersessione |
| 43 | `verimem/gateway.py:1437` `create_app._kg_for` · `verimem/gateway.py:1459` `create_app.graph` · `verimem/gateway.py:1470` `create_app.graph_full` | il grafo delle entità per tenant | **FUNZIONA COME PROMESSO (forma)** | `/v1/graph` → `{'nodes': [], 'edges': []}` · `/v1/graph/full` → `{'n': [], 'e': [], 'truncated': False, 'total_entities': 0, 'total_edges': 0}`: la versione «full» dichiara **anche i totali e se ha troncato**. Vuoti perché il grafo del tenant è nuovo |
| 44 | `verimem/gateway.py:1485` `create_app.graph_dossier` | il dossier fra due entità | **NON MISURATO** | richiede due entità collegate nel grafo del tenant: qui il grafo è vuoto |
| 45 | `verimem/gateway.py:1508` `create_app.full_snapshot` | l'istantanea completa | **FUNZIONA COME PROMESSO** | `{'tenant': 'acme', 'trust': {'ledger': {'admitted': 2, …}}, …}` in una sola chiamata |
| 46 | `verimem/gateway.py:1528` `create_app.events` (+ `verimem/gateway.py:1546` `create_app.events.gen`) · `verimem/gateway.py:1570` `create_app.events_flow` · `verimem/gateway.py:1594` `create_app.events_flow.gen` | i due flussi SSE (eventi e flusso live) | **NON MISURATI** | sono flussi che restano aperti: con `TestClient` andavano consumati in streaming, e una chiamata secca avrebbe misurato solo che la porta risponde. Scritto invece di spacciare l'apertura per il funzionamento |
| 47 | `verimem/gateway.py:1625` `create_app.dashboard` · `verimem/gateway.py:1639` `create_app.root` · `verimem/gateway.py:1664` `create_app.ui_index` | le pagine | **FUNZIONA COME PROMESSO** | `/` → 200 `text/html` con **ETag** `"893077e46e60874b2352"` · `/dashboard` → 200 `text/html` · `/ui` → 200 con lo **stesso** ETag della radice (stessa risorsa) |
| 48 | `verimem/gateway.py:1649` `create_app._asset_etag` · `verimem/gateway.py:1655` `create_app._asset_response` · `verimem/gateway.py:1668` `create_app.ui_asset` | gli asset statici con ETag | **FUNZIONA COME PROMESSO** | `/ui/app.js` → 200 `application/javascript` con ETag `"ddf57981275da38c3b0b"` (**diverso** da quello della pagina: l'ETag segue il file); `/ui/non-esiste.js` → **404** in `application/json` |
| 49 | `verimem/gateway.py:1688` `create_app._admin` | la guardia del piano di controllo | **FUNZIONA COME PROMESSO — tre casi** | app **senza** `admin_key`: `/admin/tenants`, `/admin/stats`, `/admin/overview`, `/admin/audit` → **404 tutti e quattro**, cioè «non esistono» come promette `create_app`. App **con** `admin_key`: senza header → **401** · header sbagliato → **401** · header giusto → **200** |
| 50 | `verimem/gateway.py:1695` `create_app.create_tenant` | il provisioning remoto di un tenant | **FUNZIONA COME PROMESSO** | `POST /admin/tenants {'tenant_id': 'nuovo-cliente'}` con la chiave admin → **200** `{'tenant_id': 'nuovo-cliente', 'api_key': 'vm_62ef98…'}`: la chiave in chiaro **una volta sola**, come promette `GatewayKeys.create` |
| 51 | `verimem/gateway.py:1714` `create_app.stats` · `verimem/gateway.py:1738` `create_app.overview` · `verimem/gateway.py:1760` `create_app.audit_tail` | le tre viste amministrative | **FUNZIONA COME PROMESSO** (raggiungibilità e guardia) | tutte e tre rispondono **200** con la chiave admin e **401** senza. ⚠️ **NON MISURATO** il contenuto su un'installazione con più tenant e traffico vero |
| 52 | `verimem/gateway.py:1726` `create_app.admin_ui` · `verimem/gateway.py:1734` `create_app.admin_ui_js` | le due pagine dell'interfaccia admin | **NON MISURATE** | non le ho aperte: sono pagine HTML/JS servite dietro la stessa guardia della riga 49, e misurarne il 200 non direbbe niente di più |
