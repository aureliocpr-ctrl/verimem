# Mappa di `verimem/gate_router.py` — 3 righe, 137 righe di codice (lead, 09/09 02:56)

Letto per intero. Prova: pytest del lotto E sul tip `20257636` (`233 passed in 145.12s`, EXIT=0, con `tests/test_gate_router.py`, `tests/test_una_url_inventata_non_disarma_il_gate.py`, `tests/test_il_perdono_non_si_compra_riscrivendo_il_claim_come_fonte.py`, `tests/test_il_gate_sa_la_strada_e_non_la_diceva.py`, `tests/test_un_verbale_non_e_l_agente_che_si_vanta.py`). Chiamanti letti: `anti_confab_gate.py:69-71` (import delle tre), `:2066`, `:3286-3287`; `semantic.py:2913-2914, 2959-2960, 3061-3062, 3168-3169, 3224-3225` (lo store chiede DI CHI è il claim prima di applicare gli L1.x); `l1_completion_detector.py:33` (`AGENT_CLAIM`). Il router del write-path: `agent_claim` (L1.x si applicano), `external_content` e `user_input` (L1.x saltati: un libro che dice «merged» non è l'agente che si vanta), `trusted_hook`. Claim README: nessuna riga nomina la provenienza del claim come asse dei gate (le righe 25/232/331 usano «provenance» per la citazione della fonte e la firma).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/gate_router.py:64` `classify_provenance` | dal solo `writer_role` (`system_hook/trusted_hook` → trusted; `user` → user_input; `external_content/document/document_ingest` → external; altrimenti agent_claim); **`verified_by` NON decide più**: prima un ref `url:`/`doc:` inventato rendeva `external_content` un'auto-attestazione e la faceva entrare servibile senza L1 (ricostruito riga per riga nel commento) | `semantic.py` (5 punti), `anti_confab_gate.py:2066,3286` | `tests/test_gate_router.py`, `tests/test_una_url_inventata_non_disarma_il_gate.py`, `tests/test_il_perdono_non_si_compra_riscrivendo_il_claim_come_fonte.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 2 | `verimem/gate_router.py:102` `l1x_applies` | gli L1.x valgono solo per `agent_claim` e `trusted_hook` | `semantic.py:3169`, `anti_confab_gate.py:3287` | `tests/test_gate_router.py`, `tests/test_il_gate_sa_la_strada_e_non_la_diceva.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 3 | `verimem/gate_router.py:109` `attribution_question` | la riga di attribuzione che il gate allega all'evento, con la strada esplicita (`writer_role='external_content'`) per chi ingerisce documenti | `semantic.py` (4 punti), `anti_confab_gate.py:3287` | `tests/test_gate_router.py`, `tests/test_un_verbale_non_e_l_agente_che_si_vanta.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |

Reperti: (a) il commento alle righe 76-98 è la regola di casa scritta nel codice: **`writer_role` è una dichiarazione, `verified_by` è un dato, e un dato non decide i privilegi di chi lo porta** — «quarta istanza di spoofabile-ma-sicuro», la terza curata il giorno prima nello stesso file: la classe si ripete finché il presidio copre UN campo e la funzione ne legge DUE; (b) `writer_role='external_content'` resta dichiarabile dal client e salta gli L1.x: sicuro per costruzione (gli L1.x sono avvisi sull'onestà dell'agente), dichiarato nel docstring; (c) `trusted_hook` non è verificato qui (il token-gate sta in `store()`, mappa di ws2/ws5). Nessun P0.
