# Mappa di `verimem/fact_priority.py` — 6 righe, 144 righe di codice (lead, 09/09 00:20)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_fact_priority.py tests/test_due_fatti_che_si_contraddicono_non_si_confermano.py tests/test_soggetto_valore.py tests/test_l43_arriva_alla_porta.py` → `34 passed, 23 warnings in 66.12s`, EXIT=0 (i primi due file sono di questo modulo). Chiamante letto: `verimem/mcp_server.py:10009` (il tool `hippo_fact_priority`). Il README non nomina la priorità composita. Formula dichiarata: `0,5·confidenza + 0,3·freschezza + 0,2·corroborazione`; il docstring di `_corroboration` racconta il difetto curato il 04/08 (due fatti che si contraddicono prendevano corroborazione 0,2: il conto era rovesciato) e i due limiti che restano (il caso non numerico «PostgreSQL contro MySQL» e le conferme parafrasate a Jaccard basso).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/fact_priority.py:17` `_tokens` | token alfanumerici in minuscolo | `_corroboration` (76, 86) | via i test della corroborazione | - | FUNZIONA COME PROMESSO (plumbing) | pytest 34 passed |
| 2 | `verimem/fact_priority.py:21` `_jaccard` | Jaccard, 0 se un insieme è vuoto | `_corroboration` (86) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 34 passed |
| 3 | `verimem/fact_priority.py:27` `_freshness` | decadimento a emivita 180 giorni | `rank_facts_by_priority` (120) | `tests/test_fact_priority.py` | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 4 | `verimem/fact_priority.py:33` `_quantita_divergono` | vero se un'unità presente in ENTRAMBI ha valori diversi («100 euro» contro «500 euro»); un'unità da una parte sola non è un conflitto | `_corroboration` (88) | `tests/test_due_fatti_che_si_contraddicono_non_si_confermano.py` | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 5 | `verimem/fact_priority.py:53` `_corroboration` | quante ALTRE proposizioni confermano (Jaccard ≥ soglia) ESCLUDENDO quelle che si contraddicono sui numeri; 0,2 per conferma, tetto 1 | `rank_facts_by_priority` (121) | `tests/test_fact_priority.py`, `tests/test_due_fatti_che_si_contraddicono_non_si_confermano.py` | - | FUNZIONA COME PROMESSO (i due limiti dichiarati restano: non numerico e parafrasi) | pytest 34 passed |
| 6 | `verimem/fact_priority.py:94` `rank_facts_by_priority` | la classifica con le quantità estratte una volta per fatto, il contratto unico del fatto accanto (`fact_payload`) e le componenti | `verimem/mcp_server.py:10009` | `tests/test_fact_priority.py`, `tests/test_ogni_superficie_porta_il_verdetto.py` (letto per nome) | - | FUNZIONA COME PROMESSO | pytest 34 passed |

Reperti: (a) come `trust_score`, la priorità non contiene il moat (il commento alla riga 123-126 lo dice: «senza il verdetto accanto si legge come se il moat fosse dentro il conto, e non c'è»): stessa decisione di prodotto di T47; (b) il conto è O(n²) sui confronti, dichiarato. Nessun P0.
