# Mappa — `verimem/compilation.py`

*ws3 Galileo. 13 fra funzioni, classi e metodi: una skill che ha funzionato più
volte viene **distillata in una macro** che poi si esegue **senza più chiamare
l'LLM**. Banco: `ws3-mappa-prova-compilation-e-registro.py` (09/09 13:13).*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/compilation.py:40` `MacroStep` · `verimem/compilation.py:49` `MacroStep.to_dict` · `verimem/compilation.py:53` `MacroStep.from_dict` | «una chiamata a strumento in una macro compilata. Il dizionario degli argomenti può contenere segnaposto `{{TASK}}` o `{{LAST_OBSERVATION}}`» | **FUNZIONA COME PROMESSO** | round-trip: `{'tool': 'cerca', 'args': {'q': '{{TASK}}', 'extra': ['{{LAST_OBSERVATION}}']}}` → `from_dict(to_dict(x)).to_dict() == to_dict(x)` → **True**, coi segnaposto **conservati** (se il giro li espandesse, la macro si legherebbe al primo compito) |
| 2 | `verimem/compilation.py:58` `CompiledMacro` · `verimem/compilation.py:65` `CompiledMacro.to_dict` · `verimem/compilation.py:74` `CompiledMacro.from_dict` | «lo schema di esecuzione distillato di una skill» — `skill_id`, `steps`, `derived_from_episodes`, `confidence` | **FUNZIONA COME PROMESSO**, e i default reggono | round-trip identico (**True**); `from_dict({})` su un dizionario **vuoto** → `{'skill_id': '', 'steps': [], 'derived_from_episodes': [], 'confidence': 0.0}`: nessuna eccezione, nessun campo inventato |
| 3 | `verimem/compilation.py:124` `_extract_json` | «estrae un OGGETTO JSON da una risposta LLM **eventualmente incartata**» | **FUNZIONA COME PROMESSO — tre involucri, tre esiti giusti** | `'{"a": 1}'` → `{'a': 1}` · dentro un blocco ```` ```json ```` con testo prima e dopo → `{'a': 2}` · `'nessun json qui'` → **None** (non un dizionario vuoto: il «non c'è» resta distinguibile) |
| 4 | `verimem/compilation.py:135` `trajectories_to_prompt` | «rende ≤`cap` episodi riusciti come traiettorie compatte per il compilatore» | **FUNZIONA COME PROMESSO** | con 3 episodi e `cap=2` → **due** blocchi `### Trajectory 1/2` con `TASK`, `STEPS` (`1. cerca({"q": "uno"})`) e `FINAL_ANSWER`. Il tetto è rispettato: il terzo episodio non compare |
| 5 | `verimem/compilation.py:216` `_substitute` · `verimem/compilation.py:219` `_substitute.repl` | «sostituisce ricorsivamente i segnaposto in valori str / dict / list» | **FUNZIONA COME PROMESSO** | stringa → `'trova il canone -> 5900 euro'` · dizionario annidato con una lista → `{'q': 'trova il canone', 'n': [1, '5900 euro']}` · **segnaposto ignoto `{{BOH}}` → lasciato com'è**, non svuotato: un buco che resta visibile invece di diventare una stringa vuota che sembra un valore |
| 6 | `verimem/compilation.py:230` `MacroRunResult` | l'esito di una macro: `ok`, `final_answer`, `traces`, `aborted_at_step`, `reason` | **FUNZIONA COME PROMESSO** | ogni esecuzione qui sotto restituisce la struttura completa, e in ogni caso di fallimento **`reason` è pieno e `aborted_at_step` dice a quale passo** |
| 7 | `verimem/compilation.py:238` `execute_macro` | «esegue una macro compilata **in modo deterministico — nessuna chiamata all'LLM**. Il contesto dei segnaposto si costruisce progressivamente: `TASK` è fisso, `LAST_OBSERVATION` si aggiorna dopo ogni passo riuscito» | **FUNZIONA COME PROMESSO — quattro casi, incluso il positivo** | **(a)** due passi con strumenti finti che registrano: chiamate `[('cerca', {'q': "qual e' il canone?"}), ('scrivi', {'testo': '5900 euro'})]` → **il `LAST_OBSERVATION` del primo passo è arrivato al secondo** (`True`), e nessun LLM è stato toccato. **(b)** strumento **mancante** → `aborted_at_step=2` (il primo passo era già stato eseguito: l'effetto collaterale c'è, ed è onesto che la traccia lo mostri). **(c)** strumento che **solleva** → `ok=False`, `reason="tool error in cerca: il tool e' caduto"`, `aborted_at_step=1`. **(d)** **controllo positivo**: la stessa macro chiusa da `submit_solution` → **`ok=True`**, `final_answer='5900 euro'`, 2 passi. ⚠️ Senza `submit_solution` in fondo il risultato è `ok=False` con «macro completed without submit_solution» **anche se ogni passo è riuscito**: la macro non è «fallita», è **incompleta** — e il messaggio lo dice |
| 8 | `verimem/compilation.py:152` `compile_macro` | «chiede all'LLM di distillare una macro parametrica dalle traiettorie riuscite. **Restituisce None** se l'estrazione fallisce o l'uscita dell'LLM non è valida» | **FUNZIONA COME PROMESSO — tre rami su tre** | con un LLM finto che risponde JSON valido **ma senza `submit_solution` finale** → **None** + log `compile_no_terminator` (righe 192-196: «la macro deve terminare con `submit_solution`, altrimenti non può mai chiudere il ciclo»); con spazzatura → **None** + log `compile_invalid_json raw="non e' json"`; con **meno episodi del minimo** (`CONFIG.compile_min_successes`) → **None senza nemmeno chiamare l'LLM** (contatore delle chiamate fermo). ⚠️ Nota **contro di me**: il primo tentativo è morto con `AttributeError: 'str' object has no attribute 'text'` — la risposta dell'LLM viene letta con `.text` e la firma dichiara solo `llm: Any`; il contratto sta nel corpo, non nel tipo |
