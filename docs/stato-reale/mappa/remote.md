# Mappa di `verimem/remote.py` — 11 righe, 182 righe di codice (lead, 08/09 20:50)

Letto per intero. Prova eseguita sull'albero di origin/main 20257636: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_remote_memory.py tests/test_mcp_thin.py tests/test_audit_moat_and_transport.py` → `39 passed, 34 warnings in 70.19s`, EXIT=0. I chiamanti sono letti in `verimem/client.py:280-321` (l'import pigro `_remote_class` e `open_memory`, che ripiega sull'embedded se il server non risponde): i metodi di questa classe non hanno chiamanti diretti per nome, la chiama chi ha in mano l'istanza restituita da `open_memory`.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/remote.py:20` `RemoteMemory` | classe (docstring del modulo): il client SOTTILE di un server verimem condiviso: nessun modello caricato, nessun handle SQLite, solo HTTP con la chiave del tenant; errori tipizzati | `verimem/client.py:281` (`_remote_class`), `verimem/client.py:289-321` (`open_memory`: sceglie il thin client se il server è configurato, ripiega sull'embedded se è giù) | `tests/test_remote_memory.py::test_open_memory_returns_thin_client_when_server_configured`, `::test_open_memory_falls_back_embedded_when_server_down`, `tests/test_mcp_thin.py::test_hippo_remember_delegates_to_server` | `README.md:597` («Many local sessions, one memory (thin client)»), `README.md:586-591` (gli endpoint) | FUNZIONA COME PROMESSO | pytest 39 passed (sopra); il file importa solo `typing`, e `httpx` dentro `__init__`: nessun modello, nessun SQLite, letto |
| 2 | `verimem/remote.py:21` `RemoteMemory.__init__` | il budget della PROBE (health) resta breve, le richieste dati hanno il proprio (`VERIMEM_SERVER_REQUEST_TIMEOUT_S`, default 60); rifiuta di mandare la chiave in chiaro su http verso un host non loopback, salvo `VERIMEM_ALLOW_INSECURE_HTTP` | `verimem/client.py:289-321` (`open_memory`) | `tests/test_remote_memory.py::test_request_timeout_is_separate_from_probe_timeout`; il rifiuto dell'http in chiaro: `tests/test_audit_moat_and_transport.py` (nomina `ALLOW_INSECURE_HTTP`) | `README.md:572` (chiave sempre richiesta su bind non loopback) | FUNZIONA COME PROMESSO | pytest 39 passed; le due promesse hanno ciascuna il proprio test, letti i nomi |
| 3 | `verimem/remote.py:63` `RemoteMemory._req` | plumbing: `ConnectionError` se il server non risponde, `PermissionError` su 401/403, `None` su 404 quando richiesto, `RuntimeError` su ogni altro non-2xx | tutti i metodi di superficie sotto, tranne `add` che ha il proprio giro con l'idempotency key | `::test_wrong_key_raises_permission_error`, `::test_server_down_raises_connection_error`, `::test_get_and_delete` (404 → None) | - | FUNZIONA COME PROMESSO (3 promesse su 4; il ramo `RuntimeError` per un non-2xx diverso da 401/403/404 NON ha un test: NON MISURATO) | pytest 39 passed |
| 4 | `verimem/remote.py:83` `RemoteMemory.health` | True se e solo se il server risponde a `/v1/health`; non alza di default; con `raise_on_down` alza; un rifiuto della chiave NON viene inghiottito (fail-closed) | `verimem/client.py:289-321` (`open_memory`), `verimem/cli.py:243`, `verimem/agent_guide.py:158` | `::test_health_probe`, `::test_health_probe_is_public_so_a_revoked_key_still_probes_up`, `::test_health_does_not_mask_auth_rejection_when_probe_is_authenticated`, `::test_open_memory_fails_closed_on_auth_rejection`, `tests/test_mcp_thin.py::test_auth_rejection_fails_closed_not_silent_local` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 5 | `verimem/remote.py:104` `RemoteMemory.add` | scrive su `POST /v1/memories` inoltrando `verified_by`, `source`, `asserted_at`; UNA idempotency key per scrittura e UN solo tentativo in più sullo stesso key in caso di timeout (mai un gemello) | chi ha l'istanza (SDK via `open_memory`, `hippo_remember` in `mcp_server` quando delega) | `::test_add_and_search_round_trip`, `::test_add_forwards_provenance_fields`, `::test_gateway_same_idempotency_key_stores_once`, `::test_gateway_different_keys_store_twice`, `::test_remote_add_sends_idempotency_key_and_reuses_it_on_timeout` | `README.md:587` (`POST /v1/memories`) | FUNZIONA COME PROMESSO | pytest 39 passed |
| 6 | `verimem/remote.py:148` `RemoteMemory.search` | `GET /v1/search` inoltrando `deep`, `as_of`, `with_history`; rende la lista `hits` | chi ha l'istanza; `tests/test_mcp_thin.py::test_hippo_facts_search_delegates_to_server` | `::test_add_and_search_round_trip`, `tests/test_mcp_thin.py::test_hippo_facts_search_delegates_to_server` (nomina `as_of`) | `README.md:587` (`GET /v1/search`) | FUNZIONA COME PROMESSO | pytest 39 passed |
| 7 | `verimem/remote.py:156` `RemoteMemory.explain` | `GET /v1/explain` con `min_relevance` opzionale (nessun docstring) | `verimem/cli.py:982`, `verimem/client.py:134`, `verimem/agent_guide.py:153` | nessuno dei tre file nomina `explain` (`git grep -F explain` sui tre: vuoto) | `README.md:587-588` (`GET /v1/explain`) | NON MISURATO | nessun test la esercita sul thin client; l'endpoint lato server è un'altra riga (gateway.py, ws3) |
| 8 | `verimem/remote.py:163` `RemoteMemory.get` | `GET /v1/memories/{id}`, `None` su 404 come l'API embedded | chi ha l'istanza | `::test_get_and_delete` | - | FUNZIONA COME PROMESSO | pytest 39 passed |
| 9 | `verimem/remote.py:166` `RemoteMemory.delete` | `DELETE /v1/memories/{id}` con `purge_history`; rende `removed` | chi ha l'istanza | `::test_get_and_delete`; `purge_history` non è nominato in nessuno dei tre file | `README.md:591` (`DELETE /v1/memories/{id}?purge_history=true`) | FUNZIONA COME PROMESSO (la cancellazione; l'inoltro di `purge_history` NON MISURATO) | pytest 39 passed |
| 10 | `verimem/remote.py:171` `RemoteMemory.stats` | `GET /v1/stats` (nessun docstring) | `verimem/cli.py`, `verimem/agent_guide.py:158`, `verimem/anomaly_detection.py:68` (trovati per nome, la riga esatta va letta quando si mappa quel file) | nessuno dei tre file nomina `stats()` | `README.md:588` (`GET /v1/stats`) | NON MISURATO | - |
| 11 | `verimem/remote.py:174` `RemoteMemory.close` | chiude il client httpx SOLO se lo ha creato lei (un client iniettato resta di chi lo ha iniettato) | chi ha l'istanza | nessuno dei tre file nomina `close()` | - | NON MISURATO | letto: `_own_client` decide, eccezioni inghiottite |

Reperti: (a) `explain`, `stats`, `close` del thin client non hanno un test; (b) il ramo `RuntimeError` di `_req` e l'inoltro di `purge_history` non hanno un test; (c) il README nomina gli endpoint ma mai la classe: la promessa «thin client» sta alla riga 597 e il codice la mantiene (nessun modello, nessun SQLite, verificato leggendo gli import). Nessun P0.
