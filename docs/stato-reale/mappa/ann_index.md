# Mappa di `verimem/ann_index.py` — 6 righe, 90 righe di codice (lead, 08/09 21:18)

Letto per intero. Prova eseguita sul checkout a `7b9e8ca1` (i 16 commit fino a `20257636` non toccano questo file né i test eseguiti: `git diff --stat 7b9e8ca1 20257636` vuoto su di essi) con faiss 1.13.2 installato: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_ann_index.py tests/test_ann_cache.py tests/test_uno_store_piccolo_non_carica_faiss.py` → `12 passed in 67.84s`, EXIT=0. Chiamanti letti: `verimem/ann_cache.py:31` (import), `:63` e `:108` (costruisce l'indice sulla matrice della cache); `verimem/semantic.py:1839` importa solo `faiss` da qui per sapere se è disponibile. Il README (righe 675-687) promette: latenza del recall ~piatta con l'indice ANN opzionale, «1.3 ms at 1M facts vs 81 ms brute-force», installazione di default senza faiss, ANN *approssimato* con i caveat in SCALE.md.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/ann_index.py:39` `ANNIndex` | HNSW (faiss) sulla matrice della cache del corpus; `query` rende gli INDICI dei top-(k·oversample) da filtrare e riordinare a valle esattamente come il brute-force: l'ANN sostituisce solo la selezione, mai la logica di fiducia | `verimem/ann_cache.py:63,108` | `tests/test_ann_index.py`, `tests/test_ann_cache.py` | `README.md:675-687` | FUNZIONA COME PROMESSO (la classe; il NUMERO del README, 1,3 ms a 1M contro 81 ms, è un banco di SCALE.md e non è provato da nessun test: NON MISURATO qui) | pytest 12 passed |
| 2 | `verimem/ann_index.py:45` `ANNIndex.__init__` | alza `RuntimeError` senza faiss; pretende una matrice 2-D; costruisce `IndexHNSWFlat(d, M)` con `efConstruction`/`efSearch`; aggiunge i vettori se ce ne sono | `verimem/ann_cache.py:63,108` | `tests/test_ann_index.py` | - | FUNZIONA COME PROMESSO | pytest 12 passed |
| 3 | `verimem/ann_index.py:60` `ANNIndex.size` | quanti vettori sono indicizzati (cresce con `add`) | `verimem/ann_cache.py` (letto: usa `size` per decidere il sync incrementale) | `tests/test_ann_index.py`, `tests/test_ann_cache.py` | - | FUNZIONA COME PROMESSO | pytest 12 passed |
| 4 | `verimem/ann_index.py:64` `ANNIndex.add` | aggiunge vettori SENZA ricostruire; gli indici continuano dalla dimensione corrente (ordine di append della cache); `ValueError` se la dimensione non combacia | `verimem/ann_cache.py` (sync incrementale) | `tests/test_ann_index.py`, `tests/test_ann_cache.py` | `README.md:685-687` (SCALE.md: «incremental add», il pezzo difficile) | FUNZIONA COME PROMESSO | pytest 12 passed |
| 5 | `verimem/ann_index.py:76` `ANNIndex.query` | i top-(k·oversample) indici per il vettore di query; i `-1` di padding di faiss vengono tolti, così il chiamante non dereferenzia mai un indice negativo | `verimem/ann_cache.py` (il recall in pool) | `tests/test_ann_index.py` | `README.md:675` | FUNZIONA COME PROMESSO | pytest 12 passed |
| 6 | `verimem/ann_index.py:87` `should_use_ann` | ANN solo se abilitato E il corpus supera `_ANN_MIN_N` (la soglia vive in `ann_gate`, che non importa niente di pesante) | nessuno nel prodotto: `git grep -nE "should_use_ann\(" -- verimem/` → solo questo file; la decisione vera la prendono `ann_gate` e `semantic._ann_recall_enabled` | `tests/test_ann_index.py` | `README.md:685` («the default install ships no faiss») | MAI CHIAMATA (candidata: la funzione è corretta e testata, ma il prodotto decide altrove; o si usa qui o si toglie) | comando sopra |

Reperti: (a) il numero pubblico «1.3 ms at 1M facts vs 81 ms brute-force» (README:676) non ha un test: è un banco (SCALE.md) e va nel registro dei numeri pubblici, non presidiato dal codice; (b) `should_use_ann` non è chiamata dal prodotto: la soglia è applicata da `ann_gate`/`semantic`, la funzione qui è un doppione testato. Nessun P0.
