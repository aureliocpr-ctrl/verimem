# Mappa di `verimem/settings.py` — 9 righe, 202 righe di codice (lead, 08/09 21:10)

Letto per intero. Prova eseguita su origin/main 20257636: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_settings.py tests/test_preset_resets_base_url_scan68.py tests/test_presets_apply_reset_scan68.py` → `22 passed, 1 warning in 10.76s`, EXIT=0. Il modulo applica le impostazioni all'ambiente **all'import** (righe 197-202, dichiarato nel commento): chiunque lo importi scrive in `os.environ`. Il README non nomina il file né `user_settings.json`; le variabili d'ambiente che il README documenta (righe 143-411) appartengono ad altri moduli. La docstring dice da sé che le chiavi API stanno in chiaro nel file (righe 7-8: «Treat the file as a secret»).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/settings.py:25` `UserSettings` | classe dati: provider, modelli per stadio, host ollama, onboarding, permessi del sandbox (default «strict»: solo la data dir), catena di fallback dei provider | `load` (66), `verimem/dashboard_routes/settings.py:269,335,393,467-479` | `tests/test_settings.py::test_default_user_settings_round_trip`, `::test_full_round_trip_preserves_all_fields` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 2 | `verimem/settings.py:58` `UserSettings.to_dict` | il dataclass come dict | `save` (79) | `::test_full_round_trip_preserves_all_fields` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 3 | `verimem/settings.py:62` `UserSettings.from_dict` | costruisce ignorando i campi sconosciuti | `load` (71) | `::test_from_dict_ignores_unknown_fields` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 4 | `verimem/settings.py:66` `load` | legge `user_settings.json` nella data dir; default se il file manca o è corrotto | `apply_to_env` (85), `update` (179), `upsert_api_key` (188), `verimem/dashboard_routes/settings.py:335,393,467,472,479` | `::test_load_returns_defaults_when_file_missing`, `::test_load_returns_defaults_on_corrupt_file` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 5 | `verimem/settings.py:76` `save` | scrive il JSON e proietta subito sull'ambiente (`apply_to_env`) | `update` (183), `upsert_api_key` (193), `verimem/dashboard_routes/settings.py:408,474` | `::test_default_user_settings_round_trip` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 6 | `verimem/settings.py:83` `apply_to_env` | proietta le impostazioni su `os.environ`: un `HIPPO_LLM_PROVIDER` esplicito dell'operatore vince sempre; i modelli per stadio e gli host sono set-OR-POP (un valore vuoto RIMUOVE la variabile stantia, SCAN-68); il `base_url` del provider idem; i permessi diventano `HIPPO_ENABLE_*`; il filesystem `strict`/`home`/`full` diventa `HIPPO_FS_*`; poi invalida la cache di settings_v2 | `save` (80), `verimem/dashboard_routes/settings.py:282,461`, `verimem/mode.py:6`, e l'import del modulo (riga 200) | `::test_apply_to_env_projects_capability_flags`, `::test_apply_to_env_strict_filesystem_sets_strict_flag`, `::test_apply_to_env_home_filesystem_sets_home_flag`, `::test_apply_to_env_full_filesystem_sets_root_flag`, `::test_apply_to_env_sandbox_off_unleashes_everything`, `tests/test_presets_apply_reset_scan68.py::test_apply_to_env_clears_stale_per_stage_model`, `::test_apply_to_env_full_projection_clears_global_and_ollama`, `::test_preset_to_env_chain_no_claude_under_ollama`, `tests/test_preset_resets_base_url_scan68.py::test_preset_resets_base_url` | - | FUNZIONA COME PROMESSO | pytest 22 passed; ogni ramo del docstring ha un test con il suo nome |
| 7 | `verimem/settings.py:139` `apply_to_env._set` | plumbing: scrive «1»/«0» in una variabile | `apply_to_env` (142-146) | via `::test_apply_to_env_projects_capability_flags` | - | FUNZIONA COME PROMESSO (plumbing esercitato dal test dei flag) | pytest 22 passed |
| 8 | `verimem/settings.py:177` `update` | aggiorna solo i campi noti e persiste | nessuno nel prodotto: `git grep -nE "settings\.update\(|user_settings\.update\(" -- verimem/` → vuoto; la dashboard modifica i campi a mano e chiama `save` (dashboard_routes/settings.py:406-408) | `::test_update_patches_known_fields_only` | - | MAI CHIAMATA (candidata: solo i test la chiamano; da confermare che nessuno la raggiunga per nome via `getattr`) | comandi sopra |
| 9 | `verimem/settings.py:187` `upsert_api_key` | aggiunge o toglie una chiave API e persiste | nessuno nel prodotto: `git grep -n upsert_api_key -- verimem/` → solo la definizione; la dashboard scrive `cur.api_keys[env_name] = body.api_key` e chiama `save` (dashboard_routes/settings.py:406-408) | `::test_upsert_api_key_adds_and_removes` | - | MAI CHIAMATA (candidata: la dashboard duplica la logica invece di chiamarla) | comandi sopra |

Reperti: (a) `update` e `upsert_api_key` non hanno chiamanti nel prodotto: la dashboard rifà la stessa cosa a mano (dashboard_routes/settings.py:393-408); o si usa la funzione o si toglie; (b) l'import del modulo scrive in `os.environ` (riga 200): chi importa `settings` per leggere cambia l'ambiente del processo, dichiarato nel commento ma non nel README; (c) il README non racconta `user_settings.json` né che le chiavi API stanno lì in chiaro, cosa che la docstring dichiara. Nessun P0.
