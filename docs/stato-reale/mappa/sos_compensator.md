# Mappa di `verimem/sos_compensator.py` — 4 righe, 240 righe di codice (lead, 08/09 22:20)

Letto per intero. **Nessun modulo del prodotto lo importa** (`git grep -nE "sos_compensator" -- verimem/` escluso il file → vuoto) e **nessun test lo esercita**: il docstring (riga 42) dice «Falsifiable bench in tests/test_sos_compensator.py», e quel file **non esiste** (`git grep -l sos_compensator -- tests/ benchmark/` → vuoto). Il README non lo nomina. Reperto di ws1 Marie (21:30), confermato leggendo: il modulo dichiara di scegliere l'ancora che MINIMIZZA lo spostamento della partizione attaccando il fatto nuovo alla comunità GRANDE (righe 48-53, 116-128), ma il punteggio è `alpha / size` (riga 96: più alto per la comunità PICCOLA) e la selezione prende il punteggio MASSIMO (riga 199): sceglie la comunità più piccola, cioè l'ancora che massimizza la crescita relativa. Fa l'opposto di ciò che promette. **T44**.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/sos_compensator.py:65` `compute_community_sizes` | `{indice: dimensione}` di una partizione | nessuno (nemmeno dentro il file: `select_compensated_anchor` non la usa) | nessuno | - | MAI CHIAMATA | grep sopra |
| 2 | `verimem/sos_compensator.py:72` `score_anchor_for_compensation` | «più alto = ancora migliore (meno ΔJ atteso)»: dovrebbe premiare le comunità grandi | `select_compensated_anchor` (195) | nessuno | - | NON COME PROMESSO (il punteggio `alpha / size` è più ALTO per la comunità più PICCOLA: premia l'ancora peggiore secondo la sua stessa intuizione; il modulo è morto, quindi il difetto non tocca nessun utente oggi) | letto: riga 96 |
| 3 | `verimem/sos_compensator.py:110` `select_compensated_anchor` | l'ancora che minimizza lo spostamento atteso: «Return the best-scored anchor (LARGEST community)» | nessuno nel prodotto | nessuno (il banco promesso non esiste) | - | NON COME PROMESSO (prende `s["score"] > best["score"]`, riga 199, cioè la comunità più piccola: l'opposto della riga 122 del suo docstring) | letto: righe 96, 199 |
| 4 | `verimem/sos_compensator.py:216` `select_vanilla_anchor` | il confronto: un'ancora a caso | nessuno | nessuno | - | MAI CHIAMATA | grep sopra |

Reperti: (a) **T44** (owner da assegnare, proposta: rimozione): un modulo che nessuno chiama, senza test, il cui banco dichiarato non è mai stato scritto, e il cui algoritmo fa l'opposto della promessa; la «singolarità candidata» del 23/05 non è mai stata falsificata perché non è mai stata eseguita; (b) la sola cosa che si può fare senza scrivere il banco è toglierlo; se si vuole tenerlo, prima il banco (ΔJ compensato contro casuale, come promette la riga 21-26), e il segno del punteggio va invertito. Nessun P0 (codice morto).
