# Mappa di `verimem/working_memory.py` — 6 righe, 189 righe di codice (lead, 09/09 00:26)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_working_memory.py tests/test_legacy_audit.py tests/test_mcp_legacy_audit.py tests/test_legacy_cleanup.py` → `30 passed, 1 warning in 16.58s`, EXIT=0 (il primo file è di questo modulo; lo nominano anche `tests/test_wake_extra.py`, `tests/test_active_memory_integration.py`, `tests/test_rnd_active_memory.py`). Chiamante letto: `verimem/wake.py:55-58` (import) e `:1535-1545` (il ciclo di veglia pota le vecchie osservazioni degli strumenti quando la lista dei messaggi supera il budget, in tutte e due le codifiche: tool-use nativo e ReAct testuale). Il README non nomina la working memory. Un algoritmo, due strategie iniettate: il docstring dichiara che prima viveva due volte in `wake.py`.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/working_memory.py:31` `estimate_size` | conteggio di caratteri come proxy dei token: stringhe, blocchi `tool_result` (o 200 se strutturati), senza system prompt e schemi | `prune_messages` (86, 101), `verimem/wake.py` | `tests/test_working_memory.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 2 | `verimem/working_memory.py:59` `prune_messages` | il potatore generico: esce subito se dentro il budget; l'indice 0 (il compito) non si tocca; gli ultimi `keep_tail` candidati sono sacri; sostituisce i precedenti finché rientra; rende `(messaggi, n_potati)` contando solo le sostituzioni vere | `verimem/wake.py:1535-1545` | `tests/test_working_memory.py`, `tests/test_wake_extra.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 3 | `verimem/working_memory.py:114` `native_tool_is_candidate` | candidato se `role=tool` (OpenAI/Ollama) o `role=user` con blocchi `tool_result` (Anthropic) | `verimem/wake.py` (strategia nativa) | `tests/test_working_memory.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 4 | `verimem/working_memory.py:131` `native_tool_replace` | sostituisce il contenuto testuale di ogni `tool_result`; True solo se qualcosa è cambiato (mai doppio conteggio) | `verimem/wake.py` | `tests/test_working_memory.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 5 | `verimem/working_memory.py:166` `react_obs_is_candidate` | candidato se `role=user` e il testo inizia con `Observation:` | `verimem/wake.py` (strategia ReAct) | `tests/test_working_memory.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 6 | `verimem/working_memory.py:174` `react_obs_replace` | l'osservazione segnaposto con l'ancora `Continue.` conservata | `verimem/wake.py` | `tests/test_working_memory.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |

Reperti: nessuno. Modulo puro, promesse minute, tutte provate; il costo di `estimate_size` chiamata a ogni passo del ciclo (riga 101) è O(n) per potatura, dichiarato come «early exit». Nessun P0.
