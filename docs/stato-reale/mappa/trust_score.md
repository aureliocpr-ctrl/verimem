# Mappa di `verimem/trust_score.py` — 7 righe, 172 righe di codice (lead, 08/09 23:38)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_trust_score.py tests/test_trust_grounding.py tests/test_il_fattore_grounding_non_basta.py` → `14 passed in 10.56s`, EXIT=0. Chiamante letto: `verimem/mcp_server.py:10397-10404` (il tool `hippo_rank_facts_trust`, riga 5332, passa fino a 10.000 fatti a `rank_facts_by_trust`). Il README non nomina il punteggio di fiducia composito. Il docstring di `_grounding_factor` (righe 72-89) dichiara una misura che smentisce la propria promessa originale: ricalcolando l'intera classifica del corpus vivo con e senza il fattore di grounding (4.755 fatti, 35 giudicati) cambiano 362 posizioni e i giudicati restano in fondo (mediana 4418 → 4411, 0 nella top-20): il fattore resta spento perché accenderlo darebbe l'illusione del collegamento fra il moat in scrittura e la fiducia in lettura.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/trust_score.py:24` `_tokens` | i token alfanumerici in minuscolo | `_corroboration` (52, 60) | via `::test_corroboration_boost` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 14 passed |
| 2 | `verimem/trust_score.py:28` `_jaccard` | Jaccard fra due insiemi, 0 se uno è vuoto | `_corroboration` (61) | via `::test_corroboration_boost` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 14 passed |
| 3 | `verimem/trust_score.py:36` `_age_decay` | decadimento a emivita (180 giorni), 1 se l'emivita non è positiva | `compute_trust_score` (112) | `::test_low_trust_old_low_confidence`, `::test_high_trust_fresh_high_confidence` | - | FUNZIONA COME PROMESSO | pytest 14 passed |
| 4 | `verimem/trust_score.py:44` `_corroboration` | boost 0..0,3 dai fatti che si sovrappongono (Jaccard ≥ 0,4), a rendimenti decrescenti (1 → 0,15, 2 → 0,22, 3+ → 0,30) | `compute_trust_score` (113) | `::test_corroboration_boost` | - | FUNZIONA COME PROMESSO | pytest 14 passed |
| 5 | `verimem/trust_score.py:67` `_grounding_factor` | fattore moltiplicativo dal grounding di scrittura (0 → 0,5, 50 → 0,8, 100 → 1,1), neutro se spento o senza punteggio; SPENTO per default e per misura (vedi sopra) | `compute_trust_score` (115) | `tests/test_trust_grounding.py` (4: spento invariato, None neutro, acceso riordina), `tests/test_il_fattore_grounding_non_basta.py` (3: il boost massimo non recupera la confidenza, il pareggio è alto, resta spento) | - | FUNZIONA COME PROMESSO (opt-in mai acceso da nessun percorso: `git grep -n "use_grounding=True" -- verimem/` → vuoto; e il test `test_resta_spento_di_default` lo inchioda) | pytest 14 passed |
| 6 | `verimem/trust_score.py:99` `compute_trust_score` | fiducia = confidenza × decadimento × (1 + corroborazione) × fattore, in [0, 1], con componenti e motivazione | `rank_facts_by_trust` (145) | `::test_high_trust_fresh_high_confidence`, `::test_low_trust_old_low_confidence`, `::test_payload_keys`, `::test_components_in_unit_range` | - | FUNZIONA COME PROMESSO | pytest 14 passed |
| 7 | `verimem/trust_score.py:134` `rank_facts_by_trust` | la classifica per fiducia (top_k), con il contratto unico del fatto (`fact_payload`) accanto al punteggio così chi legge vede da cosa è fatta | `verimem/mcp_server.py:10398` | `::test_rank_facts_by_trust`, `::test_empty_input`, `tests/test_trust_grounding.py::test_rank_uses_grounding_to_reorder` | - | FUNZIONA COME PROMESSO | pytest 14 passed |

Reperti: (a) la classifica di fiducia NON usa il moat: i fatti giudicati hanno confidenza 0,5 (il default di `verimem save`) e le inferenze di modello 0,9-1,0 auto-assegnate, quindi i verificati stanno in fondo (misura del 30/07 nel docstring, inchiodata da `test_il_fattore_grounding_non_basta.py`); è dichiarato e spento con cognizione, ma per un utente «rank by trust» ordina per una fiducia che ignora l'unica misura di verifica del prodotto: **T47**, decisione di prodotto (ricomporre la fiducia con un A/B), non una cura di una riga; (b) `_corroboration` è O(n) per fatto → O(n²) sulla classifica di 10.000 fatti dal tool MCP: non misurato qui. Nessun P0.
