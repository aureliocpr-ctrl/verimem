# Mappa di `verimem/world_model.py` — 5 righe, 194 righe di codice (lead, 09/09 01:00)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_find_duplicates.py tests/test_memory_compaction.py tests/test_schema_abstraction.py tests/test_structural_analogy.py tests/test_un_analogia_che_non_puo_esistere_lo_dice.py tests/test_world_model.py tests/test_world_model_alternative_r3.py` → `51 passed, 1 warning in 17.79s`, EXIT=0 (lotto: world_model, analogy, find_duplicates, schema_abstraction). Chiamanti letti: `verimem/mcp_server.py:10563-10575` (`hippo_world_simulate`, episodi da `a.memory.all(limit=n_scan)`), `verimem/counterfactual_rollout.py:16,29` (il rollout di più azioni). Lavora sugli EPISODI: T49 non lo tocca. Claim README: nessuna riga (grep su «world model|simulat» → nessuna). Il docstring promette «predictive/planning layer, foundation for beam search/MCTS»: quello che c'è è un voto pesato per Jaccard sui `task_text` degli episodi simili.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/world_model.py:19` `_tokens` | token `[A-Za-z0-9_-]+` in minuscolo, senza filtro delle parole vuote | `_action_phrase` (41), `_suggest_alternative` (61), `simulate_action` (104-105, 123) | `tests/test_world_model.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 2 | `verimem/world_model.py:23` `_jaccard` | Jaccard, 0 se vuoto (copia n. 18 di 18) | `_suggest_alternative` (72), `simulate_action` (124) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 51 passed |
| 3 | `verimem/world_model.py:31` `_action_phrase` | le parole del `task_text` passato i cui token NON stanno nello stato corrente (l'azione, non lo stato dell'altro episodio) | `_suggest_alternative` (79) | `tests/test_world_model_alternative_r3.py` (per effetto: nessun test la nomina) | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 4 | `verimem/world_model.py:46` `_suggest_alternative` | il successo passato su stato simile (Jaccard ≥ 0,1) con azione DIVERSA (≤ 50% dei token d'azione in comune); rende `(frase, id)` | `simulate_action` (131, 171) | `tests/test_world_model_alternative_r3.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |
| 5 | `verimem/world_model.py:85` `simulate_action` | `p_success`/`p_failure` dal voto pesato per similarità con lisciatura di Laplace sui top-10 episodi a Jaccard ≥ 0,1; confidenza da numero e polarità; alternativa se `p_success < 0,5`; prior uniforme senza esperienza | `verimem/mcp_server.py:10563` (`hippo_world_simulate`), `verimem/counterfactual_rollout.py:29` | `tests/test_world_model.py`, `tests/test_counterfactual_threshold_r3.py` | - | FUNZIONA COME PROMESSO | pytest 51 passed |

Reperti: (a) la soglia 0,1 su token non filtrati: due `task_text` che condividono solo articoli e preposizioni superano la soglia (stessa classe della lezione dell'oracolo del 01/08, applicata solo lì); (b) la «probabilità» è un rapporto di somme di Jaccard con Laplace 1+1: con un solo episodio simile a 0,2 esce `p_success` 0,545 e confidenza «low», dichiarato nel `rationale`; (c) il nome del modulo e il docstring promettono un modello del mondo; il README non lo promette, e va bene così finché resta fuori dalla vetrina. Nessun P0.
