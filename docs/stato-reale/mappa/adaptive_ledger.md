# Mappa di `verimem/adaptive_ledger.py` — 17 righe, 196 righe di codice (lead, 08/09 21:50)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_adaptive_ledger.py tests/test_gateway_shadow_ledger.py tests/test_cosa_resta_scollegato_dell_epistemico.py` → `15 passed, 1 warning in 13.59s`, EXIT=0. Chiamante letto: `verimem/gateway.py:612-631`: dopo una search/answer del gateway (salvo `ENGRAM_SHADOW_LEDGER=0`) chiama `observe_recall` con i topic dei risultati e `decision` sul topic dominante, e scrive l'evento `shadow.ledger` nel journal; tutto in un `try` che inghiotte ogni errore (ombra, mai la risposta). **Nessuno nel prodotto chiama `observe_error` né `close_window`** (`git grep -nE "observe_error\(|close_window\(" -- verimem/` → solo questo file). Il README non lo nomina. Il docstring dichiara la fase 1 «SHADOW»: osserva e logga, non altera nessuna richiesta; la persistenza è fase 2.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/adaptive_ledger.py:53` `ConsequenceLedger` | massa degli eventi per dominio con decadimento esponenziale (emivita 14 giorni); gli eventi futuri sono invisibili | `ShadowLedger._led` (127) | `tests/test_adaptive_ledger.py::test_consequence_decay_and_future_invisible` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 2 | `verimem/adaptive_ledger.py:60` `ConsequenceLedger.__init__` | emivita > 0 (o infinita = niente decadimento), altrimenti `ValueError` | `ShadowLedger._led` (127) | `::test_consequence_decay_and_future_invisible` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 3 | `verimem/adaptive_ledger.py:67` `ConsequenceLedger.record` | registra `(t, peso)` per dominio | `observe_recall` (135), `observe_error` (144) | `::test_consequence_decay_and_future_invisible`, `::test_shadow_exposure_and_hazard_smoothing` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 4 | `verimem/adaptive_ledger.py:70` `ConsequenceLedger.mass` | la massa decaduta a `now`; il futuro non conta | `decision` (162-163) | `::test_consequence_decay_and_future_invisible` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 5 | `verimem/adaptive_ledger.py:82` `ConsequenceLedger.domains` | i domini visti | nessuno (`git grep -n "\.domains()" -- verimem/` → vuoto) | - | - | MAI CHIAMATA (candidata) | comando sopra |
| 6 | `verimem/adaptive_ledger.py:86` `AdaptiveMeta` | pesi da expert-advice sui 3 predittori: cold start uniforme, `w *= exp(eta·MRR)` per finestra, fixed-share così nessun esperto muore | `ShadowLedger.close_window` (153), `decision` (165) | `::test_meta_cold_start_uniform_weights`, `::test_meta_window_update_expeta_mrr_and_fixed_share` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 7 | `verimem/adaptive_ledger.py:94` `AdaptiveMeta.__init__` | eta 3, alpha 0,05, pesi 1/3 | come sopra | `::test_meta_cold_start_uniform_weights` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 8 | `verimem/adaptive_ledger.py:100` `AdaptiveMeta.update_window` | l'aggiornamento per finestra | `close_window` (154) | `::test_meta_window_update_expeta_mrr_and_fixed_share` | - | FUNZIONA COME PROMESSO (in produzione non viene mai raggiunta: vedi riga 12) | pytest 15 passed |
| 9 | `verimem/adaptive_ledger.py:109` `ShadowLedger` | osserva il traffico (tenant, topic) e LOGGA le decisioni; numeratore = errori confermati dall'audit, denominatore = esposizione del recall (obbligatorio); non alza mai | `get_shadow` (189) → `verimem/gateway.py:623` | `tests/test_gateway_shadow_ledger.py` (4), `tests/test_adaptive_ledger.py` (3) | - | FUNZIONA COME PROMESSO (la metà che il gateway chiama) | pytest 15 passed |
| 10 | `verimem/adaptive_ledger.py:119` `ShadowLedger.__init__` | tre libri vuoti | `get_shadow` (189) | via tutti | - | FUNZIONA COME PROMESSO (plumbing) | pytest 15 passed |
| 11 | `verimem/adaptive_ledger.py:124` `ShadowLedger._led` | il libro del tenant, creato al bisogno | `observe_recall`, `observe_error`, `decision` | via tutti | - | FUNZIONA COME PROMESSO (plumbing) | pytest 15 passed |
| 12 | `verimem/adaptive_ledger.py:131` `ShadowLedger.observe_recall` | registra l'esposizione (un evento per topic servito); inghiotte tutto | `verimem/gateway.py:624` | `::test_shadow_exposure_and_hazard_smoothing`, `::test_shadow_never_raises_from_observe_or_decide`, `tests/test_gateway_shadow_ledger.py::test_search_emits_shadow_event_with_decision` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 13 | `verimem/adaptive_ledger.py:140` `ShadowLedger.observe_error` | registra un errore confermato (peso 1) o esterno non verificato (peso 0,2, anti-avvelenamento) | **nessuno nel prodotto** (`git grep -n "observe_error(" -- verimem/` → solo qui) | `::test_shadow_anti_poisoning_external_weight` | - | MAI CHIAMATA (il numeratore non è mai alimentato in produzione) | comando sopra |
| 14 | `verimem/adaptive_ledger.py:149` `ShadowLedger.close_window` | l'aggiornamento dei pesi a fine finestra, «alimentato dall'analisi del log ombra» | **nessuno nel prodotto** (`git grep -n "close_window(" -- verimem/` → solo qui); l'analisi del log ombra che dovrebbe chiamarla non esiste nel pacchetto | `::test_meta_window_update_expeta_mrr_and_fixed_share` (via `AdaptiveMeta`) | - | MAI CHIAMATA | comando sopra |
| 15 | `verimem/adaptive_ledger.py:159` `ShadowLedger.decision` | le tre stime, i pesi meta, il tasso di rischio smussato `(num+0,25)/(den+5)` e un consiglio solo loggato (`verify` sopra 0,10); mai alza | `verimem/gateway.py:627` | `::test_shadow_exposure_and_hazard_smoothing`, `::test_shadow_never_raises_from_observe_or_decide`, `tests/test_gateway_shadow_ledger.py::test_shadow_never_changes_the_response`, `::test_kill_switch_suppresses_events` | - | NON COME PROMESSO (in produzione `num` è sempre 0 perché nessuno chiama `observe_error`: il rischio vale `0,25/(den+5)`, mai sopra 0,10, e il consiglio loggato è SEMPRE «trust»; il «self-trust learned online» del docstring non può imparare) | letto: righe 162-175 con `observe_error` mai chiamata; pytest 15 passed sul modulo isolato |
| 16 | `verimem/adaptive_ledger.py:186` `get_shadow` | l'istanza di processo (cold start uniforme a ogni riavvio, per scelta di fase 1) | `verimem/gateway.py:618` | `tests/test_gateway_shadow_ledger.py` | - | FUNZIONA COME PROMESSO | pytest 15 passed |
| 17 | `verimem/adaptive_ledger.py:193` `reset_shadow` | solo test | i test | i test | - | MAI CHIAMATA (candidata: utilità dei test nel prodotto) | `git grep -n reset_shadow -- verimem/` → solo qui |

Reperti: (a) **T43**: il registro ombra è cablato a metà: il gateway alimenta l'esposizione e logga la decisione, ma nessuno alimenta gli errori (`observe_error`) né chiude le finestre (`close_window`); così il tasso di rischio è la sola prior e il consiglio loggato è sempre «trust». Il journal `shadow.ledger` contiene numeri veri (esposizione) e un giudizio vuoto. O si collega il numeratore (gli errori confermati dall'audit: da dove? il contradiction/adjudication log è il candidato) o si dichiara che la fase 1 misura solo l'esposizione; (b) `domains` e `reset_shadow` senza chiamanti. Nessun P0.
