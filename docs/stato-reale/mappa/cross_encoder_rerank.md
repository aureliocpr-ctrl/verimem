# Mappa di `verimem/cross_encoder_rerank.py` — 2 righe, 133 righe di codice (lead, 09/09 03:46)

Letto per intero. Prova: pytest del lotto G sul tip `20257636` (`117 passed, 1 skipped in 94.44s`, EXIT=0, con `tests/test_cross_encoder_rerank.py`, `tests/test_rerank_cold_start.py`, `tests/test_rerank_fallback_order_audit3.py`; lo nomina anche `tests/test_il_reranker_vive_nel_daemon.py`). Chiamanti letti: `verimem/semantic.py:4618` (+4564: lo stadio 2 del recall), `scripts/bench_rerank_n300.py`, `scripts/bench_rerank_n300_fast.py`. Il modello NON è istanziato qui (`scorer` iniettato): il reranker vive nel daemon (mappa di ws5). Claim README: **righe 410-411** («The reranker is a third model, and it is on by default … `VERIMEM_RECALL_RERANK=0` … stage-2 recall lever, not part of the moat») e 237-238 (i campi `rerank: applied | timeout_cold` nel payload): questo modulo è la funzione pura dietro quelle righe.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/cross_encoder_rerank.py:38` `_load_propositions` | le proposizioni degli id (righe mancanti saltate in silenzio); errore SQL → `{}` | `rerank_candidates` (96) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 117 passed |
| 2 | `verimem/cross_encoder_rerank.py:63` `rerank_candidates` | ri-punteggia con lo `scorer` iniettato le coppie `(query, proposizione)` nell'ordine del chiamante; scorer che alza o rende una lunghezza sbagliata → tutti 0,0 **conservando l'ordine del primo stadio** (il tiebreak sull'indice, non sull'id: trovato da una revisione fra istanze il 09/06); top-N | `verimem/semantic.py:4618` | `tests/test_cross_encoder_rerank.py`, `tests/test_rerank_fallback_order_audit3.py`, `tests/test_rerank_cold_start.py` | README:410-411, 237-238 | FUNZIONA COME PROMESSO | pytest 117 passed |

Reperti: (a) il fallback «tutti 0,0» è indistinguibile, nel valore di ritorno, da uno scorer che ha davvero dato 0 a tutti: chi legge `applied` nel payload (README:238) lo deve dedurre dal chiamante, non da qui; (b) legge il DB per path con `sqlite3` (un'altra connessione fuori da `SemanticMemory`, come `rank_list_builders` e `temporal_narrative`). Nessun P0.
