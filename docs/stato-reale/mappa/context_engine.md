# Mappa di `verimem/context_engine.py` — 7 righe, 102 righe di codice (lead, 08/09 21:22)

Letto per intero. Prova eseguita sul checkout a `7b9e8ca1` (i 16 commit fino a `20257636` non toccano questo file né i test eseguiti: `git diff --stat 7b9e8ca1 20257636` vuoto su di essi): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_context_engine.py tests/test_wake_context_lifecycle.py tests/test_wake_cross_session_context.py` → `17 passed, 1 warning in 13.01s`, EXIT=0 (`tests/test_e2e_memory_integration.py` lo nomina ma non l'ho eseguito). Chiamanti letti in `verimem/wake.py`: `:316` (costruito con `CONFIG.embedding_dim` e `CONFIG.tcm_rho`), `:568-612` (`reset_context`, `checkpoint_context`, `restore_context`), `:718-724` (il richiamo pesa il contesto se `CONFIG.tcm_cross_session_enabled` e `tcm_recall_context_weight > 0`). Il README non lo nomina. Il docstring (righe 35-36) dice «not stored on disk by default … persistence out of scope»: superato dai fatti, perché `wake.py` salva e ripristina lo stato (`checkpoint_context`/`restore_context`, test `test_checkpoint_restore_roundtrip` e `test_retrieve_uses_cross_session_context`). ⚠️ `wake.py` cambia di 10 righe in `20257636` (la cura di T26a alla riga 307): i numeri di riga di `wake.py` citati qui sono quelli di `7b9e8ca1`.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/context_engine.py:43` `ContextEngine` | il vettore di contesto che deriva (Howard & Kahana 2002): `c_t = ρ·c_{t-1} + (1-ρ)·obs`; non thread-safe per scelta | `verimem/wake.py:316,607` | `tests/test_context_engine.py` (7), `tests/test_wake_cross_session_context.py::test_wake_has_context_engine_at_init` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 2 | `verimem/context_engine.py:51` `ContextEngine.__init__` | `rho` in [0, 1] e `dim` positiva, altrimenti `ValueError`; stato iniziale a zero (mai casuale) | `verimem/wake.py:316,607` | `::test_initial_state_is_zero_norm_or_seeded` | - | FUNZIONA COME PROMESSO (la validazione di `rho`/`dim` non ha un test con il nome: letta) | pytest 17 passed |
| 3 | `verimem/context_engine.py:67` `ContextEngine.state` | una COPIA dello stato: chi la modifica non tocca il motore | `verimem/wake.py:596` (`checkpoint_context`), `observe` (94) | `tests/test_wake_context_lifecycle.py::test_checkpoint_returns_independent_copy` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 4 | `verimem/context_engine.py:73` `ContextEngine.rho` | il ρ configurato | nessuno nel prodotto oltre ai test (`git grep -nE "\.rho\b" -- verimem/` → vuoto fuori dal file) | `tests/test_context_engine.py::test_rho_one_freezes_context`, `::test_rho_zero_makes_context_equal_latest` (leggono il comportamento, non la proprietà) | - | MAI CHIAMATA (candidata: accessorio di lettura inutilizzato dal prodotto) | comando sopra |
| 5 | `verimem/context_engine.py:77` `ContextEngine.dim` | la dimensione | nessuno nel prodotto (`git grep -nE "\.dim\b" -- verimem/wake.py` → vuoto: wake usa `CONFIG.embedding_dim`) | - | - | MAI CHIAMATA (candidata) | comando sopra |
| 6 | `verimem/context_engine.py:80` `ContextEngine.observe` | applica il passo di deriva e rende la copia dello stato; un'osservazione di dimensione sbagliata alza invece di corrompere | `verimem/wake.py` (il ciclo di veglia osserva ogni compito: `test_context_engine_drifts_under_observation`) | `::test_drift_moves_context_toward_recent_obs`, `::test_steady_state_with_repeated_observation`, `::test_rho_one_freezes_context`, `::test_rho_zero_makes_context_equal_latest`, `::test_observation_with_wrong_dim_raises`, `tests/test_wake_cross_session_context.py::test_context_engine_drifts_under_observation` | - | FUNZIONA COME PROMESSO | pytest 17 passed |
| 7 | `verimem/context_engine.py:96` `ContextEngine.reset` | torna allo stato zero (nuovo compito, nuovo regime di contesto) | `verimem/wake.py:575` (`reset_context`) | `::test_reset_returns_to_zero_state`, `tests/test_wake_context_lifecycle.py::test_reset_context_returns_engine_to_zero` | - | FUNZIONA COME PROMESSO | pytest 17 passed |

Reperti: (a) il docstring del modulo dichiara la persistenza fuori scopo, ma `wake.py` la fa (checkpoint/restore, cross-session): il commento è vecchio, il codice no; (b) `rho` e `dim` sono accessori mai letti dal prodotto. Il README non racconta il contesto temporale del richiamo (`tcm_cross_session_enabled`): capacità presente, provata, non promessa. Nessun P0.
