# Mappa di `verimem/recall_usage.py` — 9 righe, 190 righe di codice (lead, 08/09 22:33)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_recall_usage.py` → `7 passed in 9.34s`, EXIT=0 (`tests/test_sqlite_busy_timeout.py` lo importa per il timeout di 60 s). **Nessun modulo del prodotto lo importa**: `git grep -nE "RecallUsageStore|recall_usage" -- verimem/ scripts/` (escluso il file) → vuoto; nessun tool MCP, nessun comando CLI, nessun metodo dell'SDK gli passa una dichiarazione. Il docstring (ciclo 120, 17/05) promette che «dopo aver recuperato i fatti, l'AI dichiara quali ha usato e quali ha ignorato, con un motivo», e da lì un `usage_ratio` per fatto «segnale di obsolescenza più forte dell'età»: la tabella e le query esistono, la dichiarazione non ha una porta. Il README non lo nomina.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/recall_usage.py:52` `RecallUsageRow` | una riga di dichiarazione | `all_for_fact` (129) | `tests/test_recall_usage.py` | - | MAI CHIAMATA (l'intero modulo non ha chiamanti nel prodotto; il codice funziona: 7 test verdi) | grep sopra; pytest 7 passed |
| 2 | `verimem/recall_usage.py:61` `RecallUsageStore` | store SQLite delle dichiarazioni d'uso, schema idempotente | nessuno nel prodotto | `::test_init_creates_table` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 3 | `verimem/recall_usage.py:64` `RecallUsageStore.__init__` | crea cartella e schema | nessuno | `::test_init_creates_table` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 4 | `verimem/recall_usage.py:71` `RecallUsageStore._connect` | connessione WAL con `busy_timeout` 60 s, commit alla chiusura | i metodi sotto | `tests/test_sqlite_busy_timeout.py::test_recall_usage_uses_60s_busy_timeout` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 5 | `verimem/recall_usage.py:85` `RecallUsageStore.record` | una dichiarazione (query, fatto, usato, motivo) | nessuno | `::test_record_persists_row`, `::test_multiple_records_same_fact` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 6 | `verimem/recall_usage.py:99` `RecallUsageStore.record_batch` | molte dichiarazioni in una transazione | nessuno | `::test_record_batch` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 7 | `verimem/recall_usage.py:120` `RecallUsageStore.all_for_fact` | le dichiarazioni di un fatto, dalla più recente | nessuno | via `::test_multiple_records_same_fact` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 8 | `verimem/recall_usage.py:140` `RecallUsageStore.usage_stats` | `{n_recalled, n_used, ratio}` per fatto | nessuno | `::test_zero_when_no_records`, `::test_ratio_computation` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 9 | `verimem/recall_usage.py:158` `RecallUsageStore.low_usage_facts` | i fatti con almeno `min_recalls` dichiarazioni e ratio ≤ `max_ratio`, i più deboli prima | nessuno | `::test_low_usage_filter` | - | MAI CHIAMATA (come sopra) | grep sopra |

Reperti: (a) quarto modulo intero mai collegato (dopo `resource_monitor`, `hot_reload`, `sos_compensator`): qui la parte mancante è la PORTA (un tool `hippo_recall_usage`/un metodo dell'SDK che raccolga la dichiarazione dell'agente); senza, il «segnale di obsolescenza» non nasce mai; (b) il commento del test `test_sqlite_busy_timeout.py:21` cita ancora `engram/recall_usage.py:76`, il nome vecchio del pacchetto. Decisione da prendere: dare una porta (è coerente con il contratto: «cosa può fare l'utente») o toglierlo. Nessun P0.
