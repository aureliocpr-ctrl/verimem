# Mappa di `verimem/file_extract.py` — 8 righe, 212 righe di codice (lead, 08/09 23:00)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_file_extract.py tests/test_file_extract_epub.py` → `16 passed in 11.77s`, EXIT=0. Chiamante letto: `verimem/document_index.py:41` (import) e `:394` (`extract_text(p)` all'indicizzazione di un documento), da lì `verimem index` (CLI), `hippo_document_index_file` (MCP) e l'SDK. Claim README: righe 267-273 («Document memory, cited on the indexed text — index PDF/DOCX/HTML/EPUB/text … Reachable from all three surfaces») e 503 (`verimem index contract.pdf`). I parser opzionali (PyMuPDF, python-docx, BeautifulSoup) sono importati pigramente: senza, un `RuntimeError` che dice cosa installare, mai un crash all'import per tutti; due guardie contro gli archivi ostili (dimensione dichiarata prima di python-docx; byte decompressi limitati per l'EPUB).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/file_extract.py:30` `_assert_zip_within_limits` | rifiuta un archivio la cui dimensione DICHIARATA non compressa supera 25 MB per membro o 200 MB in tutto, PRIMA che una libreria lo decomprima | `_extract_docx` (99) | `tests/test_file_extract.py::test_docx_zip_bomb_is_refused` | - | FUNZIONA COME PROMESSO | pytest 16 passed |
| 2 | `verimem/file_extract.py:58` `extract_text` | testo da un file per estensione: `.txt/.md/.rst/.log` nativi, `.pdf`, `.docx`, `.html`, `.epub`; `FileNotFoundError` se manca, `ValueError` se non supportato, `RuntimeError` se manca il parser | `verimem/document_index.py:394` | `::test_txt_and_md`, `::test_unsupported_extension_raises`, `::test_missing_file_raises`, `::test_extract_feeds_chunker_with_provenance`, `tests/test_file_extract_epub.py::test_unsupported_extension_error_unchanged` | `README.md:267-273, 503` | FUNZIONA COME PROMESSO | pytest 16 passed |
| 3 | `verimem/file_extract.py:82` `_extract_pdf` | il testo di tutte le pagine con PyMuPDF; senza PyMuPDF un `RuntimeError` che dice `pip install pymupdf` | `extract_text` (72) | `::test_pdf` | `README.md:267` | FUNZIONA COME PROMESSO (il ramo senza PyMuPDF è `pragma: no cover`: letto) | pytest 16 passed |
| 4 | `verimem/file_extract.py:94` `_extract_docx` | i paragrafi con python-docx, dopo la guardia sull'archivio | `extract_text` (74) | `::test_docx`, `::test_docx_zip_bomb_is_refused` | `README.md:267` | FUNZIONA COME PROMESSO | pytest 16 passed |
| 5 | `verimem/file_extract.py:104` `_extract_html` | il testo con BeautifulSoup, separato da a capo | `extract_text` (76) | nessuno con il nome (`git grep -n html tests/test_file_extract.py` → da verificare; i test EPUB usano lo stesso parser) | `README.md:267` | FUNZIONA COME PROMESSO (letto; il percorso `.html` da solo non ha un test con il nome: NON MISURATO) | - |
| 6 | `verimem/file_extract.py:113` `_extract_epub` | i capitoli nell'ordine dello spine OPF; un EPUB imperfetto degrada a tutti i file di markup in ordine di nome; byte decompressi limitati per capitolo e in totale (con avviso nel log) | `extract_text` (78) | `tests/test_file_extract_epub.py` (9: ordine dello spine, senza OPF, href percent-encoded, `.xml` nel fallback, DOCTYPE non parsato come XML, capitolo bomba tagliato, budget totale, non-zip alza chiaro) | `README.md:267` | FUNZIONA COME PROMESSO | pytest 16 passed |
| 7 | `verimem/file_extract.py:135` `_extract_epub.safe_xml` | i metadati XML letti con un tetto di 5 MB e rifiutati se contengono `DOCTYPE`/`ENTITY` (gli OPF veri non ne hanno): il rifiuto manda al fallback non-XML | `spine_docs` (151, 156) | `::test_epub_with_doctype_xml_is_not_parsed_as_xml` | - | FUNZIONA COME PROMESSO | pytest 16 passed |
| 8 | `verimem/file_extract.py:150` `_extract_epub.spine_docs` | l'ordine di lettura da container.xml → rootfile → manifest + spine, con gli href decodificati (`Chapter%20one.xhtml`) | `_extract_epub` (178) | `::test_epub_extracts_text_in_spine_order`, `::test_epub_percent_encoded_href_still_resolves`, `::test_epub_without_opf_falls_back_to_zip_scan` | - | FUNZIONA COME PROMESSO | pytest 16 passed |

Reperti: (a) il percorso `.html`/`.htm` non ha un test proprio (l'EPUB prova lo stesso parser su frammenti, non un file `.html` da `extract_text`); (b) PyMuPDF, python-docx e beautifulsoup4 sono opzionali: il README promette PDF/DOCX/HTML/EPUB senza dire quali extra vanno installati (da verificare nella mappa del README, ws7: `pip install verimem[...]`). Nessun P0.
