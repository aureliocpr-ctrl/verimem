# Mappa di `verimem/graph_reasoning.py` — 3 righe, 131 righe di codice (lead, 09/09 02:58)

Letto per intero. Prova: pytest del lotto E sul tip `20257636` (`233 passed in 145.12s`, EXIT=0, con `tests/test_graph_reasoning.py` e `tests/test_rotten_hop.py`). Chiamanti letti: `reasoning_dossier` ← `verimem/gateway.py:1501-1503` (l'endpoint del grafo della console), `verimem/entity_kg.py:1027`. Il dossier del ragionamento multi-hop CON la catena di custodia: ogni salto porta la proposizione del fatto che ha generato l'arco, e la funzione SI ASTIENE quando un salto non è fondato, il fatto citato non c'è più, o è stato superseduto («rotten hop», legge di trasferimento L3). Claim README: la riga 264 nomina «the dossier» (contesto: un provider assente); da confermare da ws7 se quella riga descrive questo dossier.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/graph_reasoning.py:24` `_entity_name` | il nome canonico dell'entità o l'id se non risolvibile (eccezioni assorbite) | `_dossier_for_path` (37, 84, 85), `reasoning_dossier` (120, 123-124) | `tests/test_rotten_hop.py` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 233 passed |
| 2 | `verimem/graph_reasoning.py:33` `_dossier_for_path` | UN percorso → derivazione citata (from/to, predicato, id del fatto, peso, proposizione, catena leggibile) oppure astensione con la ragione: salto senza fatto, fatto sparito, fatto superseduto (con l'id del sostituto) | `reasoning_dossier` (126, 128) | `tests/test_rotten_hop.py`, `tests/test_graph_reasoning.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 3 | `verimem/graph_reasoning.py:102` `reasoning_dossier` | con `target`: il dossier del miglior percorso o l'astensione «irraggiungibile in N salti»; senza: un dossier per entità raggiungibile, i percorsi più corti prima (`traced_paths`) | `verimem/gateway.py:1503`, `verimem/entity_kg.py:1027` | `tests/test_graph_reasoning.py` | README:264 (da confermare) | FUNZIONA COME PROMESSO | pytest 233 passed |

Reperti: (a) l'astensione è la forma giusta e qui è completa sui tre casi; manca il quarto: un fatto citato che è **quarantenato** (presente, non superseduto) passa come fondamento della derivazione — T49/T50 nella forma «del grafo» (il grafo si popola dai fatti non quarantenati, ma un fatto quarantenato DOPO l'ingest resta un arco): letto, non misurato; (b) «none of them ABSTAINS when the chain has a gap» (docstring) è un confronto coi concorrenti senza banco (A4). Nessun P0 misurato.
