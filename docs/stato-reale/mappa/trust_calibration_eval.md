# Mappa di `verimem/trust_calibration_eval.py` — 4 righe, 163 righe di codice (lead, 09/09 01:32)

Letto per intero. Prova: pytest del lotto B sul tip `20257636` (`84 passed, 1 xfailed in 37.23s`, EXIT=0, con `tests/test_trust_calibration_eval.py`). Chiamanti: **nessuno in `verimem/`**; `scripts/bench_trust_calibration.py` e `scripts/bench_trust_scorers_compare.py` lo nominano (banchi R&D del 16/06), e `docs/TRUST_CALIBRATION.md` lo racconta. È un banco: la verità a terra viene dal MONDO simulato, non dalle proprietà del fatto («altrimenti il test è una tautologia»), e il quadrante è `unobserved_p`, la frazione di obsolescenze che la memoria non ha visto. Claim README: nessuna riga.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/trust_calibration_eval.py:49` `CalibrationResult` | `n`, Brier, ECE, tasso di sovra-fiducia (inaffidabili con p ≥ 0,7), tasso di sovra-cautela (affidabili con p ≤ 0,5), conteggi dei verdetti, tabella | `evaluate_calibration` (135) | `tests/test_trust_calibration_eval.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |
| 2 | `verimem/trust_calibration_eval.py:59` `make_calibration_dataset` | la composizione dichiarata (40% veri verificati · 20% obsoleti nel mondo, osservati con prob. 1−p · 15% contestati · 15% vecchi ma veri · 10% `model_claim` a bassa confidenza metà veri) con seme fisso | `scripts/bench_trust_calibration.py` (per nome) | `tests/test_trust_calibration_eval.py` | - | MAI CHIAMATA dal prodotto (banco R&D) | pytest 84 passed |
| 3 | `verimem/trust_calibration_eval.py:100` `evaluate_calibration` | per ogni fatto la P(affidabile) implicita dal verdetto (`VERDICT_TO_PROB`: trusted 0,90 · stale 0,50 · unverified 0,40 · contested 0,20 · obsolete 0,05) o da uno `scorer` alternativo sullo STESSO dataset; poi le metriche | `scripts/bench_trust_scorers_compare.py` (per nome) | `tests/test_trust_calibration_eval.py` | - | MAI CHIAMATA dal prodotto (banco R&D) | pytest 84 passed |
| 4 | `verimem/trust_calibration_eval.py:146` `register_contradictions` | registra una contraddizione `negation` per ogni fatto contestato del dataset, così il ramo `contested` si accende davvero | i banchi (per nome) | `tests/test_trust_calibration_eval.py` | - | MAI CHIAMATA dal prodotto (banco R&D) | pytest 84 passed |

Reperti: (a) il dataset non contiene nessun fatto `quarantined`, `orphaned`, `user_belief` o `provisional`: T50 (trust_signal dà `trusted` a quei quattro status) è invisibile a questo banco per costruzione della popolazione — un banco senza quei casi non può smentire il verdetto su di essi; (b) `VERDICT_TO_PROB` è «l'ipotesi che il banco calibra», dichiarato; (c) modulo vivo solo nei banchi: da tenere nel pacchetto o spostarlo sotto `benchmark/` è una scelta di rilascio (ws8). Nessun P0.
