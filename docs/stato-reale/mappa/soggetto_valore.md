# Mappa di `verimem/soggetto_valore.py` — 6 righe, 213 righe di codice (lead, 09/09 00:23)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_soggetto_valore.py tests/test_l43_arriva_alla_porta.py` (insieme ai due di `fact_priority`) → `34 passed, 23 warnings in 66.12s`, EXIT=0. Chiamante letto: `verimem/anti_confab_gate.py:2807-2810` (il layer `L4.3` aggiunge un AVVISO alla ricevuta, mai un veto: «una cura che rompe un presidio verde scritto da un altro non si consegna», e la coda di revisione è già a 1057 contro 500). Copre lo SCAMBIO DI ATTRIBUZIONE: il valore c'è nella fonte ma è di un'altra entità, il caso che `L4.1` non vede per costruzione e sul quale il giudice neurale «si sgretola con la lunghezza della fonte» (7/12 ammessi a 453 caratteri, 10/12 a 930, misura del 27-28/08). Le tre esclusioni (omissione, numerali a parole, percentuali) sono dichiarate. Claim README: da collegare alla mappa del README di ws7 (il moat, righe 100-160); questa mappa non lo ha cercato.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/soggetto_valore.py:80` `_norm` | minuscolo senza diacritici | `ancore` (91), `_e_identificativo` (109) | via tutti | - | FUNZIONA COME PROMESSO (plumbing) | pytest 34 passed |
| 2 | `verimem/soggetto_valore.py:85` `_frasi` | segmentazione su `.;!?` E a capo (A/B sul corpus: falsi allarmi 65,7% → 31,2% con scambi colti invariati 15/16) | `avviso_soggetto_valore` (137) | `tests/test_soggetto_valore.py` | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 3 | `verimem/soggetto_valore.py:89` `ancore` | i token che possono identificare un soggetto: né funzionali (lista IT+EN locale, dichiarata incompleta) né unità | `avviso_soggetto_valore` (138, 165, 178, 189) | `tests/test_soggetto_valore.py` | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 4 | `verimem/soggetto_valore.py:95` `_valori` | le quantità della fonte via `quantity_match.extract_quantities(come_fonte=True)` | `avviso_soggetto_valore` (133, 136, 162, 178, 191) | via tutti | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 5 | `verimem/soggetto_valore.py:101` `_e_identificativo` | un identificativo SEGUE il sostantivo («ordine 77»), una quantità lo PRECEDE («3 anni»): regola posizionale, già del prodotto (`vicinato_del_valore`) | `avviso_soggetto_valore` (154) | `tests/test_soggetto_valore.py` | - | FUNZIONA COME PROMESSO | pytest 34 passed |
| 6 | `verimem/soggetto_valore.py:124` `avviso_soggetto_valore` | l'avviso `L4.3` o None: sei passi con tre guardie (G1 senza unità non si accoppia, G2 se il claim cita anche l'altro valore non scambia, G3 stesso numero a precisione diversa) e l'astensione dove non può distinguere i soggetti | `verimem/anti_confab_gate.py:2808` | `tests/test_soggetto_valore.py`, `tests/test_l43_arriva_alla_porta.py` (l'avviso arriva alla porta) | - | FUNZIONA COME PROMESSO | pytest 34 passed |

Reperti: (a) i numeri del docstring (0/12 per `L4.1`, 7/12 e 10/12 per il giudice, 61,8% e 27,6% dei falsi allarmi) sono misure del 27-28/08 sul corpus, non test; (b) la lista `_FUNZIONALI` è la TERZA lista di stopword del prodotto (con `bm25_rank._QUERY_STOPWORDS` e `query_intent._STOP`), e il commento spiega perché non ne usa un'altra: la classe «liste monolingue» resta aperta; (c) è un avviso e non un veto per una scelta collegiale dichiarata (la coda di revisione che nessuno drena). Nessun P0.
