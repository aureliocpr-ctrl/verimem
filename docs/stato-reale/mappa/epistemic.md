# Mappa di `verimem/epistemic.py` — 7 righe, 125 righe di codice (lead, 08/09 23:28)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_epistemic.py tests/test_cosa_resta_scollegato_dell_epistemico.py tests/test_guardian.py` → `18 passed, 1 warning in 13.68s`, EXIT=0. Chiamanti letti (13 moduli lo nominano): `verimem/client.py:3689` (l'SDK costruisce le etichette), `verimem/mcp_server.py:14681` (il tool MCP), `verimem/composer.py:408` (la composizione produce `proven`), `verimem/active_probe.py:37` (la sonda attiva produce `unbeaten`/`refuted` e confronta col rango), `verimem/guardian.py:33` (il guardiano confronta col rango), `verimem/semantic.py` (persistenza: colonna `epistemic`, schema v14). Claim README: righe 300-303 («Epistemic labels — proven / unbeaten (the bound only grows) / refuted (absorbing). "Held to 10^6" and "proven" are never conflated»). Logica pura, nessun I/O.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/epistemic.py:37` `guarantee_rank` | la forza di un'etichetta in un posto solo: refuted < non etichettato < unbeaten < proven; un tipo ignoto vale 0, mai un `KeyError` (due lettori giudicavano in versi opposti, 28/07) | `verimem/guardian.py:33`, `verimem/active_probe.py:37` | `tests/test_guardian.py`, `tests/test_cosa_resta_scollegato_dell_epistemico.py` | `README.md:303` («never conflated») | FUNZIONA COME PROMESSO | pytest 18 passed |
| 2 | `verimem/epistemic.py:53` `make_proven` | una prova è un riferimento non vuoto verificabile da una macchina | `verimem/client.py:3689`, `verimem/mcp_server.py:14681`, `verimem/composer.py:408`, `parse` (88) | `tests/test_epistemic.py::test_constructors_validate` | `README.md:301` | FUNZIONA COME PROMESSO | pytest 18 passed |
| 3 | `verimem/epistemic.py:60` `make_unbeaten` | un `bound` numerico positivo (non bool) | `client.py:3689`, `mcp_server.py:14681`, `active_probe.py:37`, `parse` (90) | `::test_constructors_validate` | `README.md:301-302` | FUNZIONA COME PROMESSO | pytest 18 passed |
| 4 | `verimem/epistemic.py:67` `make_refuted` | un controesempio NOMINATO, non «un conflitto» | `client.py:3689`, `mcp_server.py:14681`, `active_probe.py:37`, `parse` (91) | `::test_constructors_validate` | `README.md:302` | FUNZIONA COME PROMESSO | pytest 18 passed |
| 5 | `verimem/epistemic.py:75` `parse` | decodifica robusta di un'etichetta salvata; qualunque cosa malformata → None (fail-open: un'etichetta guasta non rompe il recall); rivalida coi costruttori | `verimem/semantic.py` (lettura della colonna), `guardian`, `active_probe` | `::test_parse_serialize_roundtrip_and_garbage` | - | FUNZIONA COME PROMESSO | pytest 18 passed |
| 6 | `verimem/epistemic.py:96` `serialize` | JSON con chiavi ordinate | `verimem/semantic.py` (scrittura della colonna) | `::test_parse_serialize_roundtrip_and_garbage`, `::test_store_roundtrip_and_rejected_transition_not_persisted` | - | FUNZIONA COME PROMESSO | pytest 18 passed |
| 7 | `verimem/epistemic.py:100` `can_transition` | le regole monotone: prima etichetta libera; `unbeaten` cresce solo; `unbeaten` → `proven`/`refuted`; `proven` → `proven`/`refuted`; `refuted` → solo `refuted` (assorbente); vietati `refuted` → altro e `proven` → `unbeaten` (correzione per supersessione, mai per rietichettatura) | `verimem/semantic.py` (al salvataggio: una transizione rifiutata non si persiste) | `::test_transitions_monotone`, `::test_refuted_is_absorbing`, `::test_store_roundtrip_and_rejected_transition_not_persisted` | `README.md:301-303` | FUNZIONA COME PROMESSO | pytest 18 passed |

Reperti: nessuno. Il claim del README è vero riga per riga e le regole del docstring hanno un test per riga. `tests/test_cosa_resta_scollegato_dell_epistemico.py` presidia che l'unico pezzo scollegato dell'epistemico sia `adaptive_ledger` (già mappato: T43). Nessun P0.
