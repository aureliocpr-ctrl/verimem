# Mappa di `verimem/residual_copies.py` — 3 righe, 120 righe di codice (lead, 09/09 03:16)

Letto per intero. Prova: pytest del lotto F sul tip `20257636` (`105 passed in 31.36s`, EXIT=0, con `tests/test_residual_copies.py`; lo nominano anche `tests/test_flow_forget.py` e `tests/test_ogni_porta_di_lettura_porta_la_provenienza.py`). Chiamanti letti: `forget_with_report` ← `verimem/cli.py:3702`, `verimem/client.py:4169`, `verimem/mcp_server.py:14826` (`hippo_forget_with_report`): **una definizione, tre porte** (il docstring racconta il primo tentativo che l'aveva ricopiata nell'handler MCP). «La metà onesta di "dimenticato"»: misurato il 05/08, il worker Auto-Dream copia `semantic.db` in `dreams/auto-<ts>/` ogni ~35 minuti e ne tiene 3; le copie manuali `dream_<ts>` non ruotano mai (quella del 12/05 aveva 60 fatti che lo store vivo non ha più). Claim README: nessuna riga nomina le copie residue (grep su «residual» → riga 156, altro senso): il README promette `forget` senza dire dove il dato resta.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/residual_copies.py:34` `forget_with_report` | cancella E dice dove il fatto resta leggibile; il referto non blocca mai la cancellazione (scansione fallita → elenco vuoto, atto eseguito) | `cli.py:3702`, `client.py:4169`, `mcp_server.py:14826` | `tests/test_residual_copies.py`, `tests/test_flow_forget.py` | - | FUNZIONA COME PROMESSO | pytest 105 passed |
| 2 | `verimem/residual_copies.py:63` `dreams_root_for` | `<data_dir>/dreams` accanto allo store vivo | `residual_copies_for` (86) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 105 passed |
| 3 | `verimem/residual_copies.py:73` `residual_copies_for` | le copie che CONTENGONO davvero il fatto (una lookup per chiave primaria per copia, in sola lettura, timeout 1 s), le più recenti prima, `kind` auto/manual e `rotates`; copie illeggibili saltate, mai un'eccezione | `forget_with_report` (49) | `tests/test_residual_copies.py` | - | FUNZIONA COME PROMESSO | pytest 105 passed |

Reperti: (a) l'ordine delle operazioni è referto PRIMA della cancellazione (riga 49 poi 52): se la scansione dura (50 copie × timeout 1 s), l'utente aspetta il referto prima che il fatto sparisca — non misurato; (b) le copie sotto `backups/` (`~/.engram/backups/daily`, `manual`: viste il 08/09 con 32 e 8.476 fatti) NON sono nel perimetro: `dreams_root_for` guarda solo `dreams/`, quindi «dove resta leggibile» è una risposta parziale che si presenta come completa — la classe che il modulo stesso denuncia («l'azione riesce, l'effetto è parziale, nessuna superficie lo dice»). Candidato ticket: T52, referto delle copie residue che ignora `backups/`. Nessun P0.
