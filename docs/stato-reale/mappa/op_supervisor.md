# Mappa di `verimem/op_supervisor.py` — 15 righe, 281 righe di codice (lead, 08/09 21:30)

Letto per intero. Prova eseguita su origin/main 20257636: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_op_supervisor.py tests/test_dashboard_widget.py` → `11 passed in 11.01s`, EXIT=0. Chiamanti letti: `verimem/syscall_bridge.py:276-337` (prima di ogni operazione del bridge `check(op)`, poi `record_failure` sull'eccezione o `record_success`/`record_failure` sull'esito), `verimem/dashboard_widget.py:47,68` (`snapshot_all` per il pannello), `verimem/engram_syscall_mcp.py:4` (lo cita come tool MCP). Il README non nomina il circuit breaker (la riga 239 parla del budget a orologio degli stadi del recall, un'altra cosa). Il contratto falsificabile (a)-(d) del docstring ha un test per lettera.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/op_supervisor.py:53` `CircuitState` | i tre stati: closed / open / half_open | `OpSupervisor` (tutto il file), `dashboard_widget.py` (legge `circuit` nello snapshot) | `tests/test_op_supervisor.py` (tutti) | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 2 | `verimem/op_supervisor.py:59` `OpSupervisor` | circuit breaker per operazione, in processo, thread-safe: N fallimenti consecutivi nella finestra aprono il circuito, dopo `reset_window` un solo tentativo (half-open) | `get_default_supervisor` (274) → `syscall_bridge.py:279,314,333`, `dashboard_widget.py:47` | `::test_circuit_opens_after_threshold`, `::test_per_op_isolation`, `::test_reset_window_transitions_to_half_open`, `::test_half_open_success_closes_circuit`, `::test_half_open_failure_reopens`, `::test_syscall_bridge_supervisor_integration_blocks_after_3_exceptions` | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 3 | `verimem/op_supervisor.py:65` `OpSupervisor.__init__` | soglie: 3 fallimenti, finestra 30 s, riapertura dopo 60 s, 1 sonda in half-open | `get_default_supervisor` (274) | i sei test sopra costruiscono l'oggetto con soglie proprie | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 4 | `verimem/op_supervisor.py:81` `OpSupervisor._initial_state` | plumbing: lo stato vergine di un'operazione | `__init__` (77, default del dict), `reset_op` (193) | via tutti i test | - | FUNZIONA COME PROMESSO (plumbing esercitato da ogni test) | pytest 11 passed |
| 5 | `verimem/op_supervisor.py:93` `OpSupervisor._trim_window` | plumbing: butta i fallimenti più vecchi della finestra | `check` (106), `record_failure` (167) | `::test_circuit_opens_after_threshold` (la finestra decide l'apertura) | - | FUNZIONA COME PROMESSO (plumbing esercitato) | pytest 11 passed |
| 6 | `verimem/op_supervisor.py:98` `OpSupervisor.check` | `{allowed, blocked_by, state}`: chiuso → sempre permesso; aperto → bloccato (`circuit_breaker_open`) finché non scade la finestra, poi half-open con una sonda; half-open senza sonde → bloccato; stato ignoto → bloccato | `verimem/syscall_bridge.py:281` | `::test_reset_window_transitions_to_half_open`, `::test_syscall_bridge_supervisor_integration_blocks_after_3_exceptions` | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 7 | `verimem/op_supervisor.py:142` `OpSupervisor.record_success` | azzera i fallimenti consecutivi; in half-open chiude il circuito | `verimem/syscall_bridge.py:335` | `::test_half_open_success_closes_circuit` | - | FUNZIONA COME PROMESSO | pytest 11 passed; letto: la riga 147 `time.time()` è una chiamata senza effetto (residuo) |
| 8 | `verimem/op_supervisor.py:159` `OpSupervisor.record_failure` | conta il fallimento; alla soglia nella finestra apre; in half-open riapre senza indulgenza | `verimem/syscall_bridge.py:315,337` | `::test_circuit_opens_after_threshold`, `::test_half_open_failure_reopens` | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 9 | `verimem/op_supervisor.py:184` `OpSupervisor.reset_all` | azzera tutto (test e amministrazione) | nessuno nel prodotto né nei test (`git grep -nE "\.reset_all\(" -- verimem/ tests/` → vuoto) | - | - | MAI CHIAMATA (candidata) | comando sopra |
| 10 | `verimem/op_supervisor.py:189` `OpSupervisor.reset_op` | azzera un'operazione | nessuno (`git grep -nE "\.reset_op\(" -- verimem/ tests/` → vuoto) | - | - | MAI CHIAMATA (candidata: il pannello mostra i circuiti aperti ma nessuna superficie li richiude a mano) | comando sopra |
| 11 | `verimem/op_supervisor.py:195` `OpSupervisor.escalate_open_circuits` | per i circuiti aperti da più di `min_open_sec` pubblica un allarme su `clp.agentos.vec_bus`; «best-effort» se il bus manca | nessuno nel prodotto (`git grep -n escalate_open_circuits -- verimem/` → solo qui; il docstring dice «da chiamare da un daemon ambientale», che non c'è nel pacchetto) | `::test_escalate_open_circuits_cycle377` (con `vec_bus` finto via monkeypatch) | - | NON COME PROMESSO (l'allarme dipende da `clp.agentos`, che NON è una dipendenza del pacchetto: `grep -nE "clp|agentos" pyproject.toml` → vuoto; per un utente l'`ImportError` viene inghiottito e l'allarme non parte MAI; e nessuno nel prodotto la chiama) | comando sopra; pytest 11 passed col bus finto |
| 12 | `verimem/op_supervisor.py:243` `OpSupervisor.snapshot_all` | lo stato di tutte le operazioni (diagnostica) | `verimem/dashboard_widget.py:68`, `escalate_open_circuits` (209) | `tests/test_dashboard_widget.py` | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 13 | `verimem/op_supervisor.py:249` `OpSupervisor._snapshot` | plumbing: lo stato di un'operazione in dict serializzabile | `check`, `record_*`, `snapshot_all` | via tutti i test | - | FUNZIONA COME PROMESSO (plumbing esercitato) | pytest 11 passed |
| 14 | `verimem/op_supervisor.py:270` `get_default_supervisor` | il supervisore di processo, creato alla prima chiamata | `verimem/syscall_bridge.py:279,314,333`, `verimem/dashboard_widget.py:47` | `::test_syscall_bridge_supervisor_integration_blocks_after_3_exceptions`, `tests/test_dashboard_widget.py` | - | FUNZIONA COME PROMESSO | pytest 11 passed |
| 15 | `verimem/op_supervisor.py:278` `reset_default` | azzera il singleton (fixture dei test) | solo i test (`git grep -n "reset_default(" -- verimem/` → solo qui) | `tests/test_op_supervisor.py` (fixture) | - | MAI CHIAMATA (candidata: utilità dei test dentro il prodotto) | comando sopra |

Reperti: (a) **T42**: il pacchetto importa `clp.agentos` in tre punti (`op_supervisor.py:226`, `syscall_bridge.py:103`, `mesh_memory.py:76`) e `clp` non è una dipendenza: qui l'allarme è «best-effort» e per un utente non parte mai; in `syscall_bridge` l'import è guardato e risponde «unavailable» (riga 103-106, letto): quanto vale la promessa senza clp lo scrive ws1 nella mappa di `syscall_bridge`; (b) `reset_all`, `reset_op`, `reset_default` senza chiamanti: un circuito aperto si richiude solo col tempo, nessuna superficie lo richiude a mano. Nessun P0.
