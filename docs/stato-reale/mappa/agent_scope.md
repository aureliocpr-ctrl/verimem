# Mappa di `verimem/agent_scope.py` — 4 righe, 108 righe di codice (lead, 09/09 01:50)

Letto per intero. Prova sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_agent_scope.py tests/test_agent_scope_canonical_audit3.py tests/test_causal_extract.py tests/test_coding_reflection.py tests/test_correction_semantic_relevance.py tests/test_correction_velocity.py tests/test_hopfield_completion.py tests/test_predicate_graph_check.py` → `62 passed, 1 warning in 20.70s`, EXIT=0 (lotto C: sei moduli). Chiamanti letti: `agent_id_from_topic` ← `agent_specialization.py:19`, `agent_workload.py:13`, `cross_agent_consensus.py:15`; `count_by_agent` ← `mcp_server.py:10619` (`hippo_count_by_agent`); `tag_for_agent` e `filter_facts_by_agent` ← **solo `scripts/agent_scope_demo.py` e `scripts/full_pipeline_demo.py`** (`git grep` per nome su `verimem/ scripts/ benchmark/`). Dal 2026 (audit#3 R14) l'estrazione delega a `scope.parse_scope`, così anche la forma canonica `user:<u>/agent:<id>/…` è riconosciuta (prima ogni fatto canonico finiva in «(shared)»). Claim README: le righe 191 e 616 parlano di `agent_id` come dimensione di scope (mappa `scope.md`), non di questo modulo.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/agent_scope.py:25` `tag_for_agent` | il topic sotto `agent:<id>/`; idempotente; riavvolge se già di un altro agente; rifiuta id con `/` o `:` | nessuno nel prodotto (due script demo) | `tests/test_agent_scope.py` | - | MAI CHIAMATA dal prodotto (la scrittura scoped passa da `scope.scoped_topic`) | pytest 62 passed |
| 2 | `verimem/agent_scope.py:46` `agent_id_from_topic` | l'`agent_id` di un topic (forma legacy `agent:` o canonica), via `scope.parse_scope` | `agent_specialization.py:19`, `agent_workload.py:13`, `cross_agent_consensus.py:15`, le tre funzioni qui | `tests/test_agent_scope.py`, `tests/test_agent_scope_canonical_audit3.py` | - | FUNZIONA COME PROMESSO | pytest 62 passed |
| 3 | `verimem/agent_scope.py:63` `filter_facts_by_agent` | i fatti di un agente, con o senza i non prefissati; `agent_id=None` → solo i non prefissati | nessuno nel prodotto (script demo) | `tests/test_agent_scope.py` | - | MAI CHIAMATA dal prodotto (il filtro vivo è `scope.matches_scope`) | pytest 62 passed |
| 4 | `verimem/agent_scope.py:93` `count_by_agent` | conteggio per agente, non prefissati in «(shared)» | `verimem/mcp_server.py:10619` (`hippo_count_by_agent`) | `tests/test_agent_scope.py`, `tests/test_agent_scope_canonical_audit3.py` | - | FUNZIONA COME PROMESSO | pytest 62 passed |

Reperti: (a) due superfici per lo stesso scoping: `agent_scope` (R4, prefisso `agent:`) e `scope` (B-1, `user/agent/run`); la seconda ha assorbito la prima per la lettura (`parse_scope`) ma `tag_for_agent` e `filter_facts_by_agent` restano come API pubblica testata e mai usata — candidato a una sola superficie; (b) `tag_for_agent` con un altro agente già presente «riavvolge»: un fatto di `pentester` ritaggato per `reviewer` cambia proprietario senza traccia (dichiarato «rare but defensible»). Nessun P0.
