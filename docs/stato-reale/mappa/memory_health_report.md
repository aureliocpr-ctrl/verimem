# Mappa di `verimem/memory_health_report.py` — 4 righe, 122 righe di codice (lead, 09/09 02:15)

Letto per intero. Prova sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_anomaly_detection.py tests/test_betweenness_cache.py tests/test_dashboard_widget.py tests/test_engram_stack_e2e.py tests/test_healthy_dichiara_la_sua_scala.py tests/test_hippo_reason_non_esplode_sulle_skill.py tests/test_memory_health_report.py tests/test_quanti_giorni_non_e_una_domanda_di_conteggio.py tests/test_query_intent.py tests/test_reasoning.py` → `73 passed, 6 xfailed, 15 warnings in 72.21s`, EXIT=0 (lotto D: sei moduli). Chiamante letto: `verimem/mcp_server.py:10154-10161` (`hippo_health_report`), che passa episodi, skill e `list_facts(limit=10000)` (vedi `oracle.md`: senza `hide_low_trust`, quindi i quarantenati entrano nella media di confidenza). Claim README: nessuna riga (grep su «health report» → nessuna). Il commento alle righe 95-101 documenta la scelta: «Healthy» è ≥ 80 qui e ≥ 75 in `corpus_health_score`, **non allineati di proposito** (misurato il 06/08), e il payload porta `thresholds`, `formula` e `verdict_scale` così chi legge sa su quale scala è il verdetto (`tests/test_healthy_dichiara_la_sua_scala.py`).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/memory_health_report.py:19` `_episodes_score` | percentuale di episodi `success`; vuoto → 0 | `generate_health_report` (56) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 73 passed |
| 2 | `verimem/memory_health_report.py:26` `_skills_score` | percentuale di `promoted` sulle skill non `retired`; vuoto → 0 | `generate_health_report` (57) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 73 passed |
| 3 | `verimem/memory_health_report.py:37` `_facts_score` | media delle `confidence` × 100 | `generate_health_report` (58) | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 73 passed |
| 4 | `verimem/memory_health_report.py:46` `generate_health_report` | `0,40·episodi + 0,35·skill + 0,25·fatti`, verdetto a soglie 80/60/40 («Empty» se tutto vuoto), raccomandazioni per componente, e la scala dichiarata nel payload | `verimem/mcp_server.py:10161` (`hippo_health_report`) | `tests/test_memory_health_report.py`, `tests/test_healthy_dichiara_la_sua_scala.py` | - | FUNZIONA COME PROMESSO | pytest 73 passed |

Reperti: (a) la «salute dei fatti» è la media della confidenza DICHIARATA da chi scrive (11.271 `model_claim` vivi nel corpus di casa): il moat (`grounding_score`) non entra, e i quarantenati ci entrano (T49, forma «senza etichetta»): un corpus con molti fatti rifiutati ma scritti a confidenza 0,9 risulta «Healthy»; (b) `now` è calcolato e mai usato (riga 54-55): parametro morto. Nessun P0.
