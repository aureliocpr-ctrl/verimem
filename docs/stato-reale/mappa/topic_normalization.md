# Mappa di `verimem/topic_normalization.py` — 3 righe, 142 righe di codice (lead, 09/09 01:28)

Letto per intero. Prova: pytest del lotto B sul tip `20257636` (`84 passed, 1 xfailed in 37.23s`, EXIT=0, con `tests/test_topic_normalization.py`). Chiamanti letti: `verimem/skill_emergence_detector.py:52,324` (le comunità per famiglia di topic), `verimem/dream_emergence_hook.py:9` (import). `topic_similarity` e `group_by_topic_family`: **nessun chiamante nel prodotto** (`git grep` per nome in `verimem/`, `scripts/`, `benchmark/` → solo il modulo stesso e i test). Claim README: nessuna riga (grep su «topic famil» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/topic_normalization.py:48` `normalize_topic` | la «chiave di famiglia»: minuscolo, via le date in coda, separatori → `/`, via i segmenti numerici e gli scope comuni, via le cifre finali dei segmenti, segmenti lunghi ridotti ai primi due pezzi, al più due segmenti | `verimem/skill_emergence_detector.py:324`, `group_by_topic_family` (122) | `tests/test_topic_normalization.py`, `tests/test_skill_drafter.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |
| 2 | `verimem/topic_normalization.py:90` `topic_similarity` | Jaccard sui segmenti normalizzati; entrambi vuoti → 1,0 | `group_by_topic_family` (128) | `tests/test_topic_normalization.py` | - | FUNZIONA COME PROMESSO (solo chiamante interno) | pytest 84 passed |
| 3 | `verimem/topic_normalization.py:107` `group_by_topic_family` | famiglie greedy a similarità ≥ 0,5 col rappresentante | nessuno nel prodotto | `tests/test_topic_normalization.py` | - | MAI CHIAMATA (testata; il prodotto usa solo `normalize_topic`) | pytest 84 passed |

Reperti: (a) `_DROP_SCOPES` contiene `lessons`, `decisions`, `preferences`: i topic di casa `lessons/errors/X` e `preferences/aurelio/Y` perdono il primo segmento e si fondono per il secondo (`errors/x`, `aurelio/y`): voluto per le comunità, da sapere per chi conta per famiglia; (b) «entrambi vuoti → 1,0» in `topic_similarity` è la convenzione opposta a `analogy.structural_jaccard` (entrambi vuoti → 0,0): due Jaccard, due convenzioni; (c) `group_by_topic_family` e `topic_similarity` sono la metà del modulo scritta per un chiamante mai arrivato (ciclo 214, 23/05). Nessun P0.
