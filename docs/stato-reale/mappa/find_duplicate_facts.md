# Mappa di `verimem/find_duplicate_facts.py` — 3 righe, 70 righe di codice (lead, 09/09 00:48)

Letto per intero. Prove: le stesse due di `oracle.md` (pytest del lotto `61 passed in 82.08s`, EXIT=0, sul tip `20257636`, con `tests/test_find_duplicate_facts.py`, `tests/test_facts_merge.py`, `tests/test_il_duplicato_identico_passava_in_silenzio.py`; prova alla porta MCP con store isolato). Chiamante letto: `verimem/mcp_server.py:11851-11866` — il tool si chiama **`hippo_facts_find_duplicates`** (mentre `hippo_find_duplicate_facts`, riga 10214, usa `memory_compaction.find_duplicates`: **due tool, due moduli, stesso mestiere, nomi incrociati**). `client.py:244,1034-1039,1089` lo cita nei commenti come lo strumento BATCH da usare DOPO la scrittura (il duplicato identico alla scrittura è un altro mestiere). Claim README: la riga 615 («cold-start write is de-duplicated») riguarda la scrittura, non questo tool.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/find_duplicate_facts.py:17` `_tokens` | token `[a-z0-9]+` sul testo in minuscolo (senza `_` e `-`, a differenza delle altre copie) | `find_duplicate_facts` (50) | `tests/test_find_duplicate_facts.py` | - | FUNZIONA COME PROMESSO | pytest 61 passed |
| 2 | `verimem/find_duplicate_facts.py:21` `_jaccard` | Jaccard con doppia guardia sull'unione vuota (copia n. 8 di 18) | `find_duplicate_facts` (53) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 61 passed |
| 3 | `verimem/find_duplicate_facts.py:28` `find_duplicate_facts` | tutte le coppie a Jaccard ≥ soglia (0,7), filtro opzionale per topic, ordinate, tetto `top_k` | `verimem/mcp_server.py:11858` (`hippo_facts_find_duplicates`) | `tests/test_find_duplicate_facts.py`, `tests/test_facts_merge.py` | - | FUNZIONA COME PROMESSO (le coppie escono senza lo `status` dei membri: un quarantenato e un vivo sembrano uguali, T49) | porta MCP 08/09 22:33 + pytest 61 passed |

Reperti: (a) `combinations(sigs, 2)` è O(n²): sui 10.000 fatti che `list_facts` rende sono 50 milioni di confronti, senza tetto e senza indicazione nel payload (letto, non misurato); (b) doppione di `memory_compaction.find_duplicates` (clustering greedy invece di coppie) e nomi dei tool incrociati: candidato a una sola superficie; (c) T49 nella forma «senza etichetta»: la cura è un campo `status` per membro, non nascondere i quarantenati (un tool di pulizia deve vederli).
