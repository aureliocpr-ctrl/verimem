# Mappa di `verimem/codebase_ingest.py` — 8 righe, 283 righe di codice (lead, 08/09 22:53)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_codebase_ingest.py` → `10 passed, 1 warning in 9.67s`, EXIT=0. **Nessun modulo del prodotto lo importa**: `git grep -nE "codebase_ingest|ingest_codebase|extract_patterns_from_file" -- verimem/` (escluso il file) → vuoto; lo usa solo `scripts/bench_codebase_ingest.py` (un banco del 18/05). Nessun comando CLI, nessun tool MCP, niente nel README. Il docstring promette il «lato learning» del coding (ciclo 143): scansiona un repo Python, estrae pattern via AST e li salva come fatti `model_claim` con `verified_by = file:<path>:<riga>`, idempotente sulla coppia `(topic, verified_by)`. Quinto modulo intero mai collegato.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/codebase_ingest.py:53` `_extract_try_except` | ogni `except X` diventa un fatto «questo codice gestisce X con <azione>» | `extract_patterns_from_file` (156) | `tests/test_codebase_ingest.py::test_extract_try_except_pattern` | - | MAI CHIAMATA (l'intero modulo non ha chiamanti nel prodotto; il codice funziona: 10 test verdi) | grep sopra; pytest 10 passed |
| 2 | `verimem/codebase_ingest.py:89` `_extract_function_docstrings` | ogni funzione con docstring → fatto `api` | `extract_patterns_from_file` (157) | `::test_extract_function_docstring` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 3 | `verimem/codebase_ingest.py:113` `_extract_class_docstrings` | ogni classe con docstring → fatto `types` | `extract_patterns_from_file` (158) | `::test_extract_class_docstring` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 4 | `verimem/codebase_ingest.py:136` `extract_patterns_from_file` | i pattern di un file; `[]` su errore di sintassi o decodifica, mai un'eccezione | `ingest_codebase` (253), `scripts/bench_codebase_ingest.py` | `::test_verified_by_includes_file_and_line`, `::test_syntax_error_returns_empty_not_raises` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 5 | `verimem/codebase_ingest.py:165` `_iter_python_files` | al più `max_files` file `.py`, saltando le cartelle di scarto | `ingest_codebase` (240) | `::test_skip_pycache_dir`, `::test_max_files_cap_enforced` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 6 | `verimem/codebase_ingest.py:184` `_verified_by_key` | la chiave canonica del dedup | `ingest_codebase` (264), `_already_persisted` (211) | `::test_idempotent_no_duplicates_on_rerun` | - | MAI CHIAMATA (come sopra) | grep sopra |
| 7 | `verimem/codebase_ingest.py:189` `_already_persisted` | vero se un fatto vivo con lo stesso topic e lo stesso `verified_by` esiste già (legge `facts` con una connessione interna di `sm`) | `ingest_codebase` (265) | `::test_idempotent_no_duplicates_on_rerun` | - | MAI CHIAMATA (come sopra; letto: scansione di tutte le righe del topic a ogni pattern, O(n) per fatto) | grep sopra |
| 8 | `verimem/codebase_ingest.py:218` `ingest_codebase` | scorre il repo, estrae, persiste i non-duplicati con `sm.store` (confidenza 0,7, `model_claim`); `dry_run` non scrive; rende il riepilogo | `scripts/bench_codebase_ingest.py:25` | `::test_returns_summary_with_expected_keys`, `::test_dry_run_extracts_but_does_not_persist`, `::test_idempotent_no_duplicates_on_rerun` | - | MAI CHIAMATA (come sopra) | grep sopra |

Reperti: (a) quinto modulo intero senza chiamanti (dopo `resource_monitor`, `hot_reload`, `sos_compensator`, `recall_usage`): 283 righe e 10 test per una capacità che nessuna porta offre; (b) `sm.store(fact)` alla riga 274 scrive direttamente nello store SENZA passare dal gate (`Memory.add`): se qualcuno lo collegasse così com'è, i pattern entrerebbero come `model_claim` senza il giudizio, il che è coerente con «AST-derived, not verified by run» ma va detto. Decisione: una porta (`verimem ingest-repo`) o la rimozione. Nessun P0.
