# Mappa di `verimem/fact_type.py` — 3 righe, 80 righe di codice (lead, 09/09 02:52)

Letto per intero. Prova: pytest del lotto E sul tip `20257636` (`233 passed in 145.12s`, EXIT=0, con `tests/test_fact_type.py` e `tests/test_trust_report.py`). Chiamanti letti: tutte e tre ← `verimem/trust_report.py:26` (import), `:97` (`classify_fact_type`), `:332` (`evidence_type_summary`), `:333` (`causal_answerable`). Il tipo «guadagnato» dalla provenienza: `interventional` (un do(X)→Y registrato), `derived` (una derivazione multi-hop), `observational` (il default). Tesi P38/P49 del vivarium: la fiducia certifica CHI l'ha detto, ortogonale alla verità causale; «typed_trust 0.654 = oracle vs blind 0.495». Claim README: `evidence_class` (righe 210/216) è il campo del gate, non questo tipo; nessuna riga del README nomina il tipo osservazionale/interventivo.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/fact_type.py:43` `classify_fact_type` | il ruolo dichiarato vince (`experiment/trial/rct/ab_test…` → interventional; `derived/reasoning/inference…` → derived), poi il primo ref tipizzante in `verified_by` (`experiment:`, `do:`, `derived-from:`…), altrimenti observational | `verimem/trust_report.py:97` | `tests/test_fact_type.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 2 | `verimem/fact_type.py:63` `causal_answerable` | vero solo se almeno un fatto è interventional | `verimem/trust_report.py:333` | `tests/test_fact_type.py`, `tests/test_trust_report.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |
| 3 | `verimem/fact_type.py:70` `evidence_type_summary` | conteggi per tipo (tre chiavi sempre presenti) | `verimem/trust_report.py:332` | `tests/test_fact_type.py` | - | FUNZIONA COME PROMESSO | pytest 233 passed |

Reperti: (a) il tipo dipende da `writer_role` e dai prefissi di `verified_by`, entrambi DICHIARATI dal client (il docstring di `gate_router` dice: `writer_role` è una dichiarazione, `verified_by` è un dato): un fatto diventa «interventional» scrivendo `writer_role="rct"`, senza alcuna verifica che un esperimento esista — coerente con «earned from provenance», ma il nome `earned` promette più di una dichiarazione; (b) «No memory vendor in 2026 ships this» è un'affermazione di mercato dentro un docstring, non misurata (A4); (c) i numeri del vivarium (0,654 contro 0,495) sono citati senza il file del banco. Nessun P0.
