# verimem/sleep.py — mappa

**Owner**: ws4 (Nadia Ferro) · **1.348 righe · 37 fra funzioni, metodi e classi** · file 4 di 22.

È il ciclo di consolidamento: NREM → REM → Pruning → Curator.

## Stato (2026-09-08, 21:40)

```
  voci elencate con l'AST                                    37 / 37
  con un verdetto                                            28
     «i test che la nominano passano»                        28
  restano NON MISURATO                                        9
```

Batch dei **42 file di test** che questo file nomina, in un colpo:
**382 passed, EXIT=0, 411,29 s, zero rossi.**

## 🔴 Le nove scoperte sono le FASI del ciclo, e non sono «MAI CHIAMATA»

```
  SleepEngine._stage_nrem · _stage_rem · _stage_curator · _stage_compilation
  _is_duplicate_skill · _generate_counterfactual · _cluster_already_covered
  _synthesize_schema · _write_practice_prompts
```

Le prime quattro **sono le fasi del ciclo di sonno**, cioè il cuore di questo
file. Chiamanti **letti** (non grepati): `sleep.py:325-328`, dentro
`SleepEngine.cycle`. ⇒ **hanno un chiamante**, non sono codice morto, e cercarle
col nome nudo — non con `nome(` — lo conferma per tutte e nove.

⚠️ **Ma la copertura indiretta NON è verificata, ed è la parte onesta.** Il loro
chiamante si chiama `cycle`: un nome troppo comune perché il grep dica qualcosa.
La sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che
è puro rumore. Sapere se un test esercita davvero il ciclo — e quindi le fasi —
**richiede di leggere quei test, e non l'ho fatto**.

🔑 È il limite che la nota di metodo dichiara: **il grep serve a trovare**. Su un
nome raro dà una pista; su `cycle` dà 583 righe e nessuna informazione. Scriverlo
è meglio che ereditare un verde.

## ⚠️ I DUE LIVELLI

Tutte e 28 le righe con verdetto stanno al livello **«i test che la nominano
passano»**, scritto per esteso in ognuna. **Nessuna funzione di `sleep.py` è
ancora verificata contro la propria promessa** con casi scelti sul docstring.


## Nota di metodo (aggiornata alle 20:58)

Le prime due versioni di questo file usavano uno scheletro mio. **Sono state
rigenerate con `scripts/mappa_bozza.py`**, lo strumento comune pubblicato dal
lead alle 20:52: dà più informazione (chiamanti con `file:riga`, test che
nominano il simbolo, righe del README) ed è il formato che
`scripts/mappa_completa.py` sa leggere. Un formato mio avrebbe fatto una
tabella che l'aggregatore non vede.

**Le misure già fatte sono state RIPORTATE, non rifatte**, iniettandole **per
nome** e non per indice: se lo strumento cambia l'ordine delle righe, una
sostituzione per numero metterebbe la prova sulla funzione sbagliata.

⚠️ **I chiamanti in questa tabella vengono da `git grep` e sono CANDIDATI, non
conferme.** Vanno letti uno per uno prima di trasformarli in verdetto: il grep
serve a trovare. E prima di scrivere **MAI CHIAMATA** — che secondo il mandato
porta a proporre una rimozione — va cercato il **nome nudo**, non `nome(`: un
riferimento passato come callback (`head_at=sm.audit_head_at`) non ha parentesi
e non compare. È la trappola che @ws6 ha segnalato alle 20:45 dopo averla quasi
calpestata; su 2.973 funzioni produrrebbe proposte di rimuovere codice vivo.

# Mappa di `verimem/sleep.py` — 37 righe (bozza da confermare leggendo ed eseguendo)

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/sleep.py:53` `replay_priority` | funzione: Score 0..1 — failures get priority, then recency, diversity, and surprise. | `verimem/dream.py:298`; `verimem/sleep.py:129`; `verimem/sleep.py:437` | `tests/test_replay_surprise.py`; `tests/test_salience_recall.py`; `tests/test_sleep.py` (+1) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 2 | `verimem/sleep.py:123` `compute_skill_avg_steps` | funzione: Map each skill_id to the mean num_steps across its past episodes. | `verimem/sleep.py:431` | `tests/test_replay_surprise.py`; `tests/test_scaling_perf_regression.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 3 | `verimem/sleep.py:180` `_extract_json` | funzione: Extract a JSON OBJECT from a possibly-wrapped LLM response. | `verimem/compilation.py:183`; `verimem/jsonutil.py:4`; `verimem/sleep.py:480` (+5) | `tests/test_compilation.py`; `tests/test_sleep.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 4 | `verimem/sleep.py:195` `SleepReport` | classe: (nessun docstring) | `verimem/agent.py:15`; `verimem/agent.py:96`; `verimem/config.py:326` (+22) | `tests/test_counterfactual.py`; `tests/test_mcp_server.py`; `tests/test_practice.py` (+12) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 5 | `verimem/sleep.py:241` `SleepEngine` | classe: (nessun docstring) | `verimem/agent.py:15`; `verimem/agent.py:52`; `verimem/agent.py:89` (+17) | `tests/test_active_memory_integration.py`; `tests/test_agent_smoke.py`; `tests/test_anche_il_fatto_derivato_passa_dal_gate.py` (+29) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 6 | `verimem/sleep.py:242` `SleepEngine.__init__` | funzione: (nessun docstring) | `verimem/_compat.py:27`; `verimem/client.py:355`; `verimem/document_index.py:255` (+11) | `tests/perf/seed_data.py`; `tests/security/test_pentest_validation.py`; `tests/test_abstention_hybrid.py` (+265) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 7 | `verimem/sleep.py:256` `SleepEngine.cycle_light` | funzione: CYCLE #12 — light sleep pass: NO LLM call, only promote/retire | `verimem/auto_dream_worker.py:354`; `verimem/auto_dream_worker.py:379`; `verimem/auto_dream_worker.py:380` (+7) | `tests/test_audit_summary_integration.py`; `tests/test_dream_shadow.py`; `tests/test_flow_manutenzione_notturna.py` (+1) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 8 | `verimem/sleep.py:286` `SleepEngine.cycle` | funzione: Run a full sleep cycle: NREM → REM → Pruning → Curator. | `verimem/_call_telemetry.py:10`; `verimem/_compat.py:1`; `verimem/_compat.py:79` (+583) | `tests/conftest.py`; `tests/perf/bench_briefing_proactive.py`; `tests/perf/bench_briefing_proactive_v2.py` (+189) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 9 | `verimem/sleep.py:307` `SleepEngine.cycle._CountingLLM` | classe: (nessun docstring) | `verimem/sleep.py:301`; `verimem/sleep.py:303`; `verimem/sleep.py:322` | `tests/test_sleep_counting_llm_leak.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 10 | `verimem/sleep.py:308` `SleepEngine.cycle._CountingLLM.__init__` | funzione: (nessun docstring) | `verimem/_compat.py:27`; `verimem/client.py:355`; `verimem/document_index.py:255` (+11) | `tests/perf/seed_data.py`; `tests/security/test_pentest_validation.py`; `tests/test_abstention_hybrid.py` (+265) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 11 | `verimem/sleep.py:311` `SleepEngine.cycle._CountingLLM.supports_tools` | funzione: (nessun docstring) | `verimem/llm.py:180`; `verimem/llm.py:336`; `verimem/llm.py:1541` (+29) | `tests/test_claude_cli_llm.py`; `tests/test_compilation.py`; `tests/test_fallback.py` (+7) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 12 | `verimem/sleep.py:314` `SleepEngine.cycle._CountingLLM.complete` | funzione: (nessun docstring) | `verimem/anti_confab_gate.py:84`; `verimem/anti_confab_gate.py:86`; `verimem/anti_confab_gate.py:2551` (+83) | `tests/perf/bench_self_model_ab.py`; `tests/swarm/test_bridge.py`; `tests/test_abstention_hybrid.py` (+113) | `README.md:448` | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 13 | `verimem/sleep.py:318` `SleepEngine.cycle._CountingLLM.complete_with_tools` | funzione: (nessun docstring) | `verimem/llm.py:1528`; `verimem/llm.py:1593`; `verimem/sleep.py:320` (+4) | `tests/test_compilation.py`; `tests/test_fallback.py`; `tests/test_llm_client.py` (+6) | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 14 | `verimem/sleep.py:416` `SleepEngine._stage_nrem` | funzione: (nessun docstring) | `verimem/sleep.py:325` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 15 | `verimem/sleep.py:462` `SleepEngine._synthesize_from_cluster` | funzione: (nessun docstring) | `verimem/sleep.py:448` | `tests/test_anche_il_fatto_derivato_passa_dal_gate.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 16 | `verimem/sleep.py:537` `SleepEngine._stage_rem` | funzione: (nessun docstring) | `verimem/sleep.py:326` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 17 | `verimem/sleep.py:567` `SleepEngine._recombine` | funzione: (nessun docstring) | `verimem/sleep.py:556`; `verimem/sleep.py:641` | `tests/test_curator_merge_resets_fitness_audit3.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 18 | `verimem/sleep.py:594` `SleepEngine._stage_curator` | funzione: (nessun docstring) | `verimem/sleep.py:327` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 19 | `verimem/sleep.py:616` `SleepEngine._merge` | funzione: (nessun docstring) | `verimem/sleep.py:602` | `tests/test_curator_merge_resets_fitness_audit3.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 20 | `verimem/sleep.py:649` `SleepEngine._stage_compilation` | funzione: Distil successful skill traces into deterministic macros. | `verimem/sleep.py:328` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 21 | `verimem/sleep.py:702` `SleepEngine._stage_counterfactual` | funzione: Generate alternative strategies for skills that consistently fail. | `verimem/sleep.py:330` | `tests/test_counterfactual.py`; `tests/test_rnd_active_memory.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 22 | `verimem/sleep.py:753` `SleepEngine._is_duplicate_skill` | funzione: True if `candidate` is a near-duplicate of an existing non-retired skill. | `verimem/sleep.py:741` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 23 | `verimem/sleep.py:786` `SleepEngine._generate_counterfactual` | funzione: (nessun docstring) | `verimem/sleep.py:737` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 24 | `verimem/sleep.py:819` `SleepEngine._stage_schema` | funzione: Cluster semantically-close skills, write meta-skills as 'schemas'. | `verimem/sleep.py:332` | `tests/test_rnd_active_memory.py`; `tests/test_schema.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 25 | `verimem/sleep.py:868` `SleepEngine._cluster_already_covered` | funzione: True if some existing schema's `specialises` children form a | `verimem/sleep.py:844` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 26 | `verimem/sleep.py:885` `SleepEngine._synthesize_schema` | funzione: (nessun docstring) | `verimem/sleep.py:853` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 27 | `verimem/sleep.py:922` `SleepEngine._stage_practice` | funzione: Write practice prompts for skills in the uncertain fitness zone. | `verimem/sleep.py:334` | `tests/test_practice.py`; `tests/test_rnd_active_memory.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 28 | `verimem/sleep.py:970` `SleepEngine._write_practice_prompts` | funzione: (nessun docstring) | `verimem/sleep.py:957` | nessuno | - | **NON MISURATO** | chiamante LETTO, non grepato: le quattro fasi sono invocate da `SleepEngine.cycle` a `sleep.py:325-328` ⇒ **NON è MAI CHIAMATA** (cercato col NOME NUDO, non con `nome(`). Nessun test la nomina. ⚠️ **La copertura indiretta NON è verificata**: il chiamante si chiama `cycle`, un nome troppo comune perché il grep dica qualcosa — la sua colonna «chiamata da» porta **583 candidati**, fra cui `_compat.py:1`, che è rumore. Per sapere se un test esercita davvero il ciclo bisogna leggerlo, e non l'ho fatto |
| 29 | `verimem/sleep.py:994` `SleepEngine._stage_spontaneous_reactivation` | funzione: Default-mode-network style: replay a few old skills to keep them fresh. | `verimem/sleep.py:339` | `tests/test_spontaneous_reactivation.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 30 | `verimem/sleep.py:1085` `SleepEngine._stage_episode_decay` | funzione: Prune episodes whose Ebbinghaus retention fell below threshold. | `verimem/sleep.py:344` | `tests/test_sleep_episode_decay.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 31 | `verimem/sleep.py:1117` `SleepEngine._stage_tier2_triage` | funzione: Consolidation-time Tier-2 anti-confab triage: quarantine specific-unsourced | `verimem/sleep.py:393` | `tests/test_sleep_tier2_triage.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 32 | `verimem/sleep.py:1127` `SleepEngine._stage_pruning` | funzione: (nessun docstring) | `verimem/sleep.py:396` | `tests/test_skill_dormant_retire.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 33 | `verimem/sleep.py:1145` `SleepEngine._stage_bundle_discovery` | funzione: FORGIA pezzo #163: surface skill-bundle candidates on report. | `verimem/sleep.py:349` | `tests/test_sleep_bundle_discovery.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 34 | `verimem/sleep.py:1169` `SleepEngine._stage_crossover` | funzione: FORGIA pezzo #178: engram-crossover hybrid generation. | `verimem/sleep.py:382` | `tests/test_sleep_crossover.py`; `tests/test_sleep_crossover_wiring.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 35 | `verimem/sleep.py:1217` `SleepEngine._stage_synaptic_tagging` | funzione: FORGIA pezzo #175: synaptic-tag salience boost. | `verimem/sleep.py:371` | `tests/test_sleep_synaptic_tag_wiring.py`; `tests/test_sleep_synaptic_tagging.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 36 | `verimem/sleep.py:1255` `SleepEngine._stage_negative_bundles` | funzione: FORGIA pezzo #170: lateral inhibition tagging. | `verimem/sleep.py:360` | `tests/test_sleep_negative_bundle_wiring.py`; `tests/test_sleep_negative_bundles.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
| 37 | `verimem/sleep.py:1299` `SleepEngine._stage_abstract_bundles` | funzione: FORGIA pezzo #165: synthesize compound-macro candidate skills. | `verimem/sleep.py:356` | `tests/test_sleep_bundle_abstraction.py`; `tests/test_sleep_bundle_abstraction_wiring.py` | - | **FUNZIONA COME PROMESSO** *(al livello: i test passano)* | batch 08/09 21:15, 48 file eseguiti in un colpo: **382 passed, 25 warnings in 411.29s (0:06:51)**, EXIT=0, zero rossi. ⚠️ **Livello dichiarato**: questo prova che i test che la nominano passano, NON che la funzione faccia ciò che il suo docstring promette — la seconda cosa richiede casi scelti sulla promessa, e per questa riga non l'ho fatta |
