# Mappa di `verimem/local_relation.py` — 13 righe, 260 righe di codice (lead, 08/09 22:05)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_local_relation.py tests/test_ogni_import_pesante_passa_dal_lock.py tests/test_supersede_enforce_sdk.py` → `27 passed, 22 warnings in 106.17s`, EXIT=0 (i test del giudice usano un classificatore finto, come dichiara il docstring: il modello vero, `MoritzLaurer/DeBERTa-v3-large-mnli-fever-anli-ling-wanli`, qui non è stato caricato). Chiamanti letti: `verimem/anti_confab_gate.py:468` (`local_nli_available` decide l'auto-abilitazione del rilevatore semantico quando il modello è già su disco), `:2221-2222` (`get_local_relation_judge` come giudice quando non c'è un LLM), `verimem/agent.py:37` (`ENGRAM…=local` cabla `LocalRelationJudge` come giudice di riconciliazione). Claim README: riga 184 («Entity swaps (one CEO→another) need the semantic NLI tier»); le righe 111 e 704 (AUROC 0,96-0,97) sono del giudice di grounding, un altro modulo. È il giudice NLI locale che toglie la dipendenza da `claude -p` per il livello semantico: gemello di `local_grounding` (ws5).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/local_relation.py:62` `_build_label_mapper` | dalla riga di probabilità al dict `{contradiction, entailment, neutral}` leggendo l'`id2label` del modello, mai per posizione (due modelli in cache hanno ordini OPPOSTI) | `make_nli_classifier` (108) | `::test_make_classifier_maps_by_id2label` | - | FUNZIONA COME PROMESSO | pytest 27 passed |
| 2 | `verimem/local_relation.py:85` `make_nli_classifier` | il classificatore di produzione: import di torch/transformers SOTTO il lock degli import (e solo l'import: i pesi fuori, 19,1 s), softmax dei logit mappato per etichetta, CUDA se c'è | `LocalRelationJudge._ensure_classifier` (153) | `::test_make_classifier_maps_by_id2label` (monkeypatch di transformers), `tests/test_ogni_import_pesante_passa_dal_lock.py` (4: l'import passa dal lock) | - | FUNZIONA COME PROMESSO (col modello finto; il caricamento del modello vero NON è stato eseguito qui) | pytest 27 passed |
| 3 | `verimem/local_relation.py:113` `make_nli_classifier.classifier` | a lotti di 32, entrambe le direzioni come le passa il chiamante, `no_grad` | `LocalRelationJudge.classify`/`classify_batch` | via `::test_make_classifier_maps_by_id2label` | - | FUNZIONA COME PROMESSO (col modello finto) | pytest 27 passed |
| 4 | `verimem/local_relation.py:129` `LocalRelationJudge` | un `RelationJudge` su un cross-encoder NLI locale: niente `claude -p`; simmetrico e a precisione (CONTRADICTION se una direzione supera la soglia, ENTAILMENT solo se entrambe) | `get_local_relation_judge` (245), `verimem/agent.py:38` | `::test_is_a_relation_judge`, `::test_default_model_is_the_precise_one`, `::test_contradiction_detected`, `::test_entailment_needs_both_directions`, `::test_one_way_entailment_is_not_duplicate`, `::test_complementary_same_subject_is_neutral`, `::test_contradiction_is_symmetric_recall`, `::test_threshold_respected` | `README.md:184` | FUNZIONA COME PROMESSO | pytest 27 passed |
| 5 | `verimem/local_relation.py:132` `LocalRelationJudge.__init__` | modello da argomento, poi da `ENGRAM_LOCAL_NLI_MODEL`, poi il default preciso; soglie 0,5; classificatore iniettabile | come sopra | `::test_default_model_is_the_precise_one` | - | FUNZIONA COME PROMESSO | pytest 27 passed |
| 6 | `verimem/local_relation.py:146` `LocalRelationJudge._ensure_classifier` | carica una volta sotto lock; dopo un fallimento non riprova (`RuntimeError`) | `classify` (173), `classify_batch` (193) | `::test_classifier_error_fails_safe_to_neutral` (l'errore del classificatore, non del caricamento) | - | FUNZIONA COME PROMESSO (il ramo «non riprovare dopo un fallimento» è letto, non provato: NON MISURATO) | pytest 27 passed |
| 7 | `verimem/local_relation.py:160` `LocalRelationJudge._decide` | la regola: max delle contraddizioni ≥ soglia → CONTRADICTION; min delle implicazioni ≥ soglia → ENTAILMENT; altrimenti NEUTRAL | `classify` (176), `classify_batch` (200) | `::test_contradiction_is_symmetric_recall`, `::test_entailment_needs_both_directions`, `::test_threshold_respected` | - | FUNZIONA COME PROMESSO | pytest 27 passed |
| 8 | `verimem/local_relation.py:169` `LocalRelationJudge.classify` | vuoto → NEUTRAL senza modello; entrambe le direzioni; un errore del modello → NEUTRAL (mai una contraddizione inventata) | il gate (`detect_semantic_conflicts`), `semantic.reconcile_new_fact` | `::test_empty_inputs_are_neutral_without_calling_model`, `::test_classifier_error_fails_safe_to_neutral`, `tests/test_supersede_enforce_sdk.py` (11, end-to-end sull'SDK) | `README.md:184` | FUNZIONA COME PROMESSO | pytest 27 passed |
| 9 | `verimem/local_relation.py:178` `LocalRelationJudge.classify_batch` | a lotti, ordine conservato, vuoti → NEUTRAL senza modello | nessuno nel prodotto (`git grep -n "classify_batch(" -- verimem/` → solo qui) | `::test_batch_classify_preserves_order` | - | MAI CHIAMATA (candidata: testata, ma il gate chiama `classify` una coppia per volta) | comando sopra |
| 10 | `verimem/local_relation.py:209` `local_nli_available` | vero se il modello è GIÀ nella cache HF (solo filesystem: niente torch, microsecondi); rispetta `HF_HUB_CACHE`/`HF_HOME`; `lru_cache` per processo | `verimem/anti_confab_gate.py:468` | `tests/test_nli_auto_enable.py` (5, eseguiti nella mappa di `semantic_conflict.py`) | `README.md:188` (il rilevatore «costs nothing» perché si accende solo col modello su disco) | FUNZIONA COME PROMESSO | pytest (semantic_conflict) 36 passed |
| 11 | `verimem/local_relation.py:239` `get_local_relation_judge` | singleton di processo, caricato una volta | `verimem/anti_confab_gate.py:2222` | `tests/test_gate_semantic_conflict_wire.py::test_no_llm_falls_back_to_local_nli_judge`, `::test_llm_free_path_passes_a_local_relation_judge` | - | FUNZIONA COME PROMESSO | pytest (semantic_conflict) 36 passed |
| 12 | `verimem/local_relation.py:249` `set_local_relation_judge` | sostituisce il singleton | nessuno nel prodotto (`git grep -n set_local_relation_judge -- verimem/` → solo qui); i test del gate | `tests/test_gate_semantic_conflict_wire.py` | - | MAI CHIAMATA (candidata: utilità dei test nel prodotto) | comando sopra |
| 13 | `verimem/local_relation.py:260` (riga `_ = (Path, Any)`) | — non è una funzione: tiene vivi due import inutilizzati «per stabilità della superficie» | - | - | - | MAI CHIAMATA (riga morta: due import tenuti apposta; non conta nel righello perché non è una definizione) | letto |

Reperti: (a) il modello vero non è stato caricato in nessuno dei 27 test: il docstring lo dichiara per scelta; l'unica prova del giudice locale con i pesi veri sta nel banco della riconciliazione (HaluMem, n=60: 0,196 → 0,054 di falsi supersede, citato nel commento), che non è un test; (b) `classify_batch` e `set_local_relation_judge` senza chiamanti nel prodotto. Nessun P0.
