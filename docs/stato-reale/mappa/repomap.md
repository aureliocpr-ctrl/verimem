# Mappa — `verimem/repomap.py`

*ws3 Ricerca. 13 fra funzioni e classi: la **mappa del repository** che entra nel
contesto di un agente che scrive codice — file, simboli, e un budget di
caratteri da rispettare. Banco: `ws3-mappa-prova-repomap-e-selfmodel.py`
(09/09 13:18). Repository di prova costruito apposta: `pkg/grande.py` (30
funzioni), `pkg/piccolo.py` (1), `app.js` (2), `note.txt` (che non è codice).*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/repomap.py:128` `_walk_files` | «rende le triple (path, mtime, size) via `os.scandir`… lo `stat()` è già in cache su Windows, quindi si decide cache hit/miss **senza un giro in più**» | **FUNZIONA COME PROMESSO**, e filtra | `['app.js', 'grande.py', 'piccolo.py']`: **`note.txt` non c'è** — la selezione dei file di codice si accende e si spegne dove deve |
| 2 | `verimem/repomap.py:94` `_scan_file` | estrae i simboli di un file (Python/JS/TS/Rust/Go) | **FUNZIONA COME PROMESSO** | su `grande.py` (30 `def`) → **30 simboli**, i primi tre `['funzione_0', 'funzione_1', 'funzione_2']`. ⚠️ Vuole un `Path`, non una stringa — ed è **dichiarato nell'annotazione** (`path: Path, root: Path`): il mio `AttributeError: 'str' object has no attribute 'suffix'` era colpa mia |
| 3 | `verimem/repomap.py:77` `Symbol` · `verimem/repomap.py:85` `FileEntry` | le due strutture dei dati | **FUNZIONA COME PROMESSO** | `FileEntry.path` è il path **relativo alla radice con le barre in avanti** (`'pkg/piccolo.py'`), e questo conta per la riga 6 |
| 4 | `verimem/repomap.py:161` `_entry_to_cache` · `verimem/repomap.py:168` `_entry_from_cache` | serializzazione per la cache su disco | **FUNZIONA COME PROMESSO** | round-trip: i 30 simboli tornano identici (`True`) |
| 5 | `verimem/repomap.py:176` `_default_cache_path` · `verimem/repomap.py:188` `_load_cache` · `verimem/repomap.py:200` `_save_cache` | «un percorso di cache **per repository** sotto `CONFIG.data_dir`. Chiavare sulla radice del workspace fa sì che più repository serviti dalla stessa installazione **non collidano**» | **FUNZIONA COME PROMESSO** | nome generato `repomap_cache_617dcfc9d0eb.json` (sha1 della radice risolta): due repository diversi danno due file diversi. Dopo la prima scansione `_load_cache` legge **3 chiavi**, una per file |
| 6 | `verimem/repomap.py:208` `scan_repo` | «cammina nel workspace… cache per file su mtime+size: ri-scansionare un file **immutato** si limita a reidratare» | **FUNZIONA COME PROMESSO** | prima passata **0,044 s**, seconda (stessi file, stessa cache) **0,015 s** — un terzo del tempo, e il file di cache esiste. I 3 file trovati sono quelli giusti |
| 7 | `verimem/repomap.py:253` `rank_files` | «+1.0 ogni ~10 simboli · +0.5 per i path corti · +1.0 se toccato nelle ultime 24h · **+1.5 se il path compare nella provenienza di una skill applicata di recente**» | **FUNZIONA COME PROMESSO — il bonus vale esattamente 1,5** | senza skill recenti: `grande.py` 4.85 · `app.js` 2.2 · `piccolo.py` 1.95. Con `pkg/piccolo.py` fra le skill recenti: `piccolo.py` passa a **3.45** (`1.95 + 1.5`, il bonus dichiarato) e **supera `app.js`**. ⚠️ **Il confronto è sul path RELATIVO** (`e.path in recent_skill_paths`, riga 277): col path assoluto i punteggi restano identici. Il mio primo giro usava l'assoluto e stavo per scrivere «il bonus non scatta»: era il mio caso a essere sbagliato, non il codice |
| 8 | `verimem/repomap.py:283` `render_repomap` | «rende una mappa compatta e leggibile **sotto un budget di token**» | **FUNZIONA COME PROMESSO — provato SUPERANDO il budget** | con `max_chars=4000` → **699** caratteri (sotto per capienza); con `max_chars=120` → **36** caratteri, cioè **il budget stretto viene rispettato tagliando**, non ignorato. L'intestazione resta leggibile: `'## REPO MAP (top files by relevance)'` |
| 9 | `verimem/repomap.py:314` `build_repomap` | «aiutante in un colpo solo: scansiona, ordina, rende» | **FUNZIONA COME PROMESSO** | `build_repomap(repo, max_files=50, max_chars=500)` → **499 caratteri** (≤ 500). Chiamante vero **letto**: `code.py:218`, dentro `VerimemCode`, con `self.workspace` |
