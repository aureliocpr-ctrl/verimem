# Mappa — `verimem/tool_registry.py`

*ws3 Ricerca. 11 fra classe, metodi e costruttore: il **registro delle capacità**
degli strumenti — chi legge, chi scrive, chi esegue comandi, chi chiede conferma.
È il punto dove si decide se un'azione passa o si ferma, quindi il caso che conta
non è quello noto: è **lo strumento sconosciuto**. Banco:
`ws3-mappa-prova-compilation-e-registro.py` (09/09 13:13).*

🟡 **Il reperto del file**: il comportamento è **fail-CLOSED** ed è quello giusto,
ma **il docstring dice il contrario** — è rimasto alla versione di prima della
cura. Riga 5 qui sotto.

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/tool_registry.py:38` `ToolCapability` | «metadati statici che descrivono cosa può fare uno strumento **e come dev'essere sorvegliato**»: `capability`, `risk_level`, `reversibility`, `requires_confirm`, `requires_sandbox`, `mandatory_log`, `writes_memory`, `executes_command`, `gating_bypass` | **FUNZIONA COME PROMESSO** | i campi ci sono tutti e sono leggibili su ogni voce: `get('hippo_anti_confab_apply')` → `capability=WRITE risk=medium reversibility=undoable confirm=False` |
| 2 | `verimem/tool_registry.py:469` `build_default_registry` | «costruisce un registro nuovo, popolato coi 15 semi critici» | **NON COME PROMESSO nel numero — ma in eccesso, e il docstring è vecchio** | `len(build_default_registry().all())` → **50** strumenti, non 15. Non è un difetto di comportamento (50 classificati sono meglio di 15), è una **frase superata** nel docstring. Ripartizione misurata: `by_capability` → READ **34**, WRITE **7**, EXECUTE **2**; `by_risk` → low **35**, medium **10**, high **5**, critical **0** |
| 3 | `verimem/tool_registry.py:427` `CapabilityRegistry.register` | «aggiunge o sovrascrive una voce. **Idempotente**» | **FUNZIONA COME PROMESSO** | `register(mio_tool WRITE/high)` → totale **51** (era 50), `get('mio_tool')` → `WRITE high`; poi `register(mio_tool READ/low)` → `get` → **READ** e totale **ancora 51**: sovrascrive senza duplicare |
| 4 | `verimem/tool_registry.py:421` `CapabilityRegistry` · `verimem/tool_registry.py:450` `CapabilityRegistry.all` | registro mutabile, popolato da `_INITIAL_CAPS` | **FUNZIONA COME PROMESSO** | `all()` restituisce le 50 voci; i nomi sono quelli veri del server MCP (`hippo_anti_confab_apply`, `hippo_chain_facts`, `hippo_consolidate`, …) |
| 5 | `verimem/tool_registry.py:431` `CapabilityRegistry.get` | il docstring dice: «cerca per nome. **Restituisce DEFAULT_CAPABILITY (READ/low)** se sconosciuto» | 🟡 **IL CODICE FA MEGLIO DI QUELLO CHE DICE — e il docstring va corretto** | `get('uno_strumento_mai_visto_che_cancella_tutto')` → **`capability=DESTRUCTIVE risk=critical reversibility=no requires_confirm=True requires_sandbox=True gating_bypass=False`**. È **fail-CLOSED**, cioè l'opposto di quello che la sua stessa riga di documentazione promette. Il perché è scritto dieci righe sopra il default (righe 57-60): *«Pre-fix ritornava {READ, low, no-confirm, no-sandbox} per tool unknown — fail-OPEN classico: un nuovo tool distruttivo non classificato veniva trattato come read-only sicuro»*, corretto dopo un audit. **La cura è entrata nel codice e non nel docstring**: chi legge la firma per decidere se fidarsi legge la versione insicura. È la nostra classe *«un docstring dice cosa credeva l'autore»* — qui a favore della sicurezza, ma un lettore che si fida della riga sbagliata prende la decisione sbagliata in entrambi i sensi |
| 6 | `verimem/tool_registry.py:453` `CapabilityRegistry.by_capability` · `verimem/tool_registry.py:456` `CapabilityRegistry.by_risk` | le viste per capacità e per rischio | **FUNZIONA COME PROMESSO** | READ 34 · WRITE 7 · EXECUTE 2 · low 35 · medium 10 · high 5 · critical 0. ⚠️ `critical` è vuoto **fra gli strumenti registrati**, ed è coerente col fail-closed: il critico è la classe in cui cade **ciò che non conosciamo**, non ciò che conosciamo |
| 7 | `verimem/tool_registry.py:459` `CapabilityRegistry.writes_memory` · `verimem/tool_registry.py:462` `CapabilityRegistry.executes_command` · `verimem/tool_registry.py:465` `CapabilityRegistry.requires_confirm` | i tre elenchi che contano per il gate | **FUNZIONA COME PROMESSO** | `writes_memory` → **14** (`hippo_remember`, `hippo_record_episode`, `hippo_skill_promote`, `hippo_fact_forget`, …) · `executes_command` → **2** (`hippo_run_task`, `sandbox_exec`) · `requires_confirm` → **5** (`hippo_fact_forget`, `hippo_forget_scope`, `hippo_forget`, `hippo_decay_run`, …). ⚠️ Da notare, e non è un verdetto: **14 scrivono in memoria ma solo 5 chiedono conferma** — la differenza è una scelta di prodotto, non un difetto, ma è la coppia di numeri da mettere davanti a chi la decide |
