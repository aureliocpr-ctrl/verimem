# Mappa — `verimem/self_model.py`

*ws3 Ricerca. 11 fra classi, metodi e funzioni: il **modello di sé** che ogni
istanza si porta dietro fra una sessione e l'altra — versionato, con un audit, e
con un tetto di byte. Banco: `ws3-mappa-prova-repomap-e-selfmodel.py`
(09/09 13:18).*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/self_model.py:51` `SelfModelStore` · `verimem/self_model.py:59` `SelfModelStore.__init__` · `verimem/self_model.py:70` `SelfModelStore._connect` · `verimem/self_model.py:75` `SelfModelStore._init_schema` | «archivio del modello di sé su SQLite. Sicuro fra thread per istanza… istanze concorrenti sullo stesso file si serializzano tramite SQLite» | **FUNZIONA COME PROMESSO** (costruzione e schema) | `SelfModelStore(<tmp>/self.db, max_bytes=2000)` crea il file e lo schema al primo uso; le scritture successive vanno a buon fine. ⚠️ **NON MISURATA** la concorrenza vera fra due processi: è la parte che il docstring promette e che un banco a un processo solo non può provare |
| 2 | `verimem/self_model.py:95` `SelfModelStore.get` | «il modello corrente come `{version, updated_at, content, actor}`, **o None se non è mai stato scritto**» | **FUNZIONA COME PROMESSO** | su uno store nuovo → **None** (non un dizionario vuoto: «mai scritto» resta distinguibile da «scritto vuoto»). Dopo due aggiornamenti → `{'version': '2', 'updated_at': '1788952659.9', 'content': "{'focus': 'gateway.py', …}", 'actor': 'ws3'}` |
| 3 | `verimem/self_model.py:113` `SelfModelStore.update` | «sostituisce il modello corrente e **accoda il vecchio record alla tabella di audit**. Restituisce il nuovo record» | **FUNZIONA COME PROMESSO** | update #1 → `version 1`, `actor 'ws3'`; update #2 → `version 2`. Il numero di versione cresce e l'attore è registrato: chi ha cambiato il modello di sé resta scritto |
| 4 | `verimem/self_model.py:47` `SelfModelTooLarge` | «sollevata quando un aggiornamento proposto supera il tetto di byte» | **FUNZIONA COME PROMESSO — e ho provato ENTRAMBI i lati** | con un contenuto da 5000 byte e `max_bytes=2000` → **`SelfModelTooLarge: content 5012 bytes exceeds limit 2000`** (il messaggio porta **la misura e il limite**, non un «troppo grande»); subito dopo, con un contenuto corto → **versione 3**, cioè il tetto **non** ha bloccato una scrittura legittima. Un limite che rifiuta tutto non è un limite |
| 5 | `verimem/self_model.py:173` `SelfModelStore.history` | «tutte le versioni (**dalla più vecchia**) dalla tabella di audit» | **FUNZIONA COME PROMESSO** | `[(1, "{'focus': 'la mappa di client.py'…"), (2, "{'focus': 'gateway.py'…")]`, e l'ordine è crescente (verificato con `== sorted(...)`): la storia si legge nel verso in cui è accaduta |
| 6 | `verimem/self_model.py:191` `render_for_injection` | «rende il modello di sé come blocco compatto leggibile, adatto all'iniezione a inizio sessione. **Restituisce "" se il record è None**» | **FUNZIONA COME PROMESSO** | con `None` → **`''`** esatto (non «None», non un blocco vuoto con l'intestazione); con un record → `'=== SELF MODEL (continuity layer, cycle #67) ===\nversion: 3 (updated_at epoch 1788952660)…'` |
| 7 | `verimem/self_model.py:224` `render_anchor_block` · `verimem/self_model.py:299` `render_anchor_block._line_bytes` | «rende il richiamo delle àncore come blocco Markdown ≤ `max_bytes`, dalle àncore di `EntityStore.list_anchors()` pesate dal decadimento» | **FUNZIONA COME DICHIARATO sul ramo senza EntityStore; il ramo con le àncore è NON MISURATO** | con `sem=None` → `{'markdown': '', 'n_anchors': 0, 'truncated': False}`: **non solleva** e dice **quante àncore ha visto (zero)** invece di restituire un blocco vuoto muto. Il ramo che conta — àncore vere, taglio a `max_bytes`, `truncated: True` — richiede un `EntityStore` popolato: **non l'ho costruito**, e lo scrivo invece di indovinarlo |
