# Mappa di `verimem/decision_chain.py` — 10 righe, 166 righe di codice (lead, 08/09 21:05)

Letto per intero. Prova eseguita sul checkout a `7b9e8ca1` (i 16 commit fino a `20257636` non toccano questo file né i test eseguiti: `git diff --stat 7b9e8ca1 20257636` vuoto su di essi): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_decision_chain.py tests/test_decision_chain_wiring.py` → `9 passed in 9.95s`, EXIT=0. Chiamanti letti in `verimem/client.py:2580-2822`: `Memory._decisions()` (store pigro su `decisions.db` accanto a `semantic.db`, creato alla prima SCRITTURA), `_decisions_ro()` (lettura che non crea il file), `decision_outcome()`, `why_decision()`. Il README non nomina mai la catena delle decisioni (le righe 213 e 523 usano «decision» in un altro senso); il design sta in `docs/DECISION_CHAIN_DESIGN.md` (esiste). La CLI e il server MCP non la espongono (`git grep DecisionStore|why_decision|record_outcome -- verimem/cli.py verimem/mcp_server.py verimem/tools_extra.py`: vuoto): è raggiungibile solo dall'SDK.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/decision_chain.py:35` `Decision` | classe dati: una decisione con alternative scartate, evidenze CITATE (id di fatti), esito atteso, esito misurato con chi lo verifica | `DecisionStore._row` (riga 117), `verimem/client.py` (`why_decision` la converte in dict) | `tests/test_decision_chain.py::test_record_and_get_roundtrip` | - (il README non la nomina) | FUNZIONA COME PROMESSO | pytest 9 passed |
| 2 | `verimem/decision_chain.py:64` `DecisionStore` | store ISOLATO per DB: non tocca mai `semantic.db` | `verimem/client.py:2590` (`_decisions`), `verimem/client.py:2596` (`_decisions_ro`) | `tests/test_decision_chain_wiring.py::test_decision_store_is_a_sibling_db` | - | FUNZIONA COME PROMESSO | pytest 9 passed; letto: usa `sqlite3.connect(self.db_path)` sul proprio file, nessun import di semantic |
| 3 | `verimem/decision_chain.py:67` `DecisionStore.__init__` | crea la cartella e la tabella `decisions` se mancano | `verimem/client.py:2590` | `tests/test_decision_chain.py::test_persists_across_instances` | - | FUNZIONA COME PROMESSO | pytest 9 passed |
| 4 | `verimem/decision_chain.py:73` `DecisionStore._conn` | plumbing: una connessione sqlite con `row_factory=Row` | tutti i metodi di questo file | esercitata da ogni test dei due file | - | FUNZIONA COME PROMESSO (plumbing esercitato da tutti i test) | pytest 9 passed |
| 5 | `verimem/decision_chain.py:78` `DecisionStore.record` | salva una decisione e rende l'id; `evidence` sono id di fatti citati al momento della decisione | `verimem/client.py` (`record_decision`, riga ~2805) | `::test_record_and_get_roundtrip`, `tests/test_decision_chain_wiring.py::test_record_and_why_through_memory` | - | FUNZIONA COME PROMESSO | pytest 9 passed |
| 6 | `verimem/decision_chain.py:98` `DecisionStore.record_outcome` | aggiunge l'esito misurato; ESIGE `verified_by` (un esito senza evidenza è un model_claim) e aggiorna SOLO il record, mai la reputazione delle fonti citate | `verimem/client.py:2818` (`decision_outcome`) | `::test_record_outcome_requires_evidence`, `tests/test_decision_chain_wiring.py::test_outcome_requires_evidence_through_memory` | - | FUNZIONA COME PROMESSO | pytest 9 passed; letto: `UPDATE decisions ... WHERE id=?` e nessun accesso a `source_trust` |
| 7 | `verimem/decision_chain.py:117` `DecisionStore._row` | plumbing: da riga sqlite a `Decision` (json dei campi lista) | `get`, `list` | esercitata dai round trip | - | FUNZIONA COME PROMESSO (plumbing esercitato dai round trip) | pytest 9 passed |
| 8 | `verimem/decision_chain.py:127` `DecisionStore.get` | la decisione per id, `None` se non c'è | `verimem/client.py` (via `_decisions_ro`) | `::test_record_and_get_roundtrip` | - | FUNZIONA COME PROMESSO | pytest 9 passed |
| 9 | `verimem/decision_chain.py:133` `DecisionStore.list` | le decisioni dalla più recente, filtro per topic, `limit` | `why` (riga 160), `verimem/client.py` | `::test_list_by_topic_newest_first` | - | FUNZIONA COME PROMESSO | pytest 9 passed |
| 10 | `verimem/decision_chain.py:147` `DecisionStore.why` | «perché abbiamo scelto X?»: sovrapposizione di parole (lessicale per scelta dichiarata) sulle decisioni, le migliori prima; il punto è la catena di evidenze citate, non il ranking | `verimem/client.py` (`why_decision`) | `::test_why_explains_with_cited_evidence`, `tests/test_decision_chain_wiring.py::test_record_and_why_through_memory`, `::test_why_decision_empty_when_none_recorded` | - | FUNZIONA COME PROMESSO | pytest 9 passed; letto: legge al più 1000 decisioni (`self.list(limit=1000)`): oltre, le più vecchie non vengono cercate, e nessun test lo copre |

Reperti: (a) la catena delle decisioni è raggiungibile SOLO dall'SDK: né la CLI né il server MCP la espongono, e il README non la nomina: una capacità che esiste, è provata (9 test) e non è raccontata a nessuno; (b) `why` guarda solo le ultime 1000 decisioni: limite non dichiarato, nessun test. Nessun P0.
