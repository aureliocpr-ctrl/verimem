# Mappa di `verimem/scope.py` — 6 righe, 162 righe di codice (lead, 09/09 00:08)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_agent_scope.py tests/test_agent_scope_canonical_audit3.py tests/test_cli_facts_scope.py tests/test_fact_forget_scope_r3.py tests/test_lo_scope_vuoto_non_e_un_via_libera.py tests/test_scope.py` → `67 passed, 2 warnings in 22.27s`, EXIT=0 (restano `tests/test_scope_fetch_limit.py` e `tests/test_scope_index_scan.py`, non eseguiti qui). Chiamanti: 52 moduli del prodotto (`git grep -lE "\bscope\b" -- verimem/`), fra cui `semantic.py` (il filtro al recall), `client.py`, `mcp_server.py`, `cli.py` (i parametri `user_id`/`agent_id`/`run_id`). Il README **non documenta** le dimensioni `user_id`/`agent_id`/`run_id` né `include_shared` (`grep -nEi "agent_id|run_id|user_id|scope|tenant" README.md` → solo la nota sui tenant nella supersessione, righe 190-191): la multi-tenancy «mem0-parity» del docstring è una capacità viva su tutte le porte e non raccontata. Zero schema: lo scope vive nel prefisso del topic `user:<u>/agent:<a>/run:<r>/…`, filtrato al recall; il caveat (un topic che inizia davvero con `user:`) è dichiarato.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/scope.py:30` `_validate` | un identificatore semplice `[A-Za-z0-9_-.]`, altrimenti `ValueError` | `scoped_topic` (72) | `tests/test_scope.py` | - | FUNZIONA COME PROMESSO | pytest 67 passed |
| 2 | `verimem/scope.py:37` `parse_scope` | i segmenti di scope in testa al topic, consumati da sinistra; una dimensione ripetuta ferma il parsing (il resto è il topic base) | `scoped_topic` (69), `matches_scope` (146), `semantic.py` | `tests/test_scope.py`, `tests/test_agent_scope.py` | - | FUNZIONA COME PROMESSO | pytest 67 passed |
| 3 | `verimem/scope.py:59` `scoped_topic` | il prefisso canonico (user → agent → run) davanti al topic; idempotente e fondente (mai `user:a/user:a/`); senza dimensioni non cambia niente | `client.py`, `mcp_server.py`, `cli.py` (alla scrittura) | `tests/test_scope.py`, `tests/test_agent_scope_canonical_audit3.py`, `tests/test_cli_facts_scope.py` | - | FUNZIONA COME PROMESSO | pytest 67 passed |
| 4 | `verimem/scope.py:80` `lead_prefix` | il prefisso CONTIGUO in testa per un `LIKE` SQL (completo su scala); `None` senza `user_id`; si ferma al primo buco (`user` + `run` senza `agent` → solo `user:<u>/`) | `semantic.py` (il recall scoped) | `tests/test_scope_index_scan.py` (non eseguito qui), `tests/test_scope.py` | - | FUNZIONA COME PROMESSO (il test dedicato alla scansione con indice non è stato eseguito: letto) | pytest 67 passed |
| 5 | `verimem/scope.py:104` `scoped_fetch_limit` | il limite di pesca: `base` se il prefisso copre tutte le dimensioni chieste, altrimenti sovracampiona (`base × 8`, con tetto) così il post-filtro non sotto-restituisce (il buco: `run_id` senza `agent_id`) | `semantic.py` (il recall scoped) | `tests/test_scope_fetch_limit.py` (non eseguito qui) | - | FUNZIONA COME PROMESSO (letto; il test dedicato non eseguito: NON MISURATO qui) | letto |
| 6 | `verimem/scope.py:134` `matches_scope` | per ogni dimensione CHIESTA il fatto deve avere lo stesso valore (isolamento stretto); le non chieste sono jolly; un fatto senza quella dimensione passa solo con `include_shared`; nessuna dimensione → nessun vincolo | `semantic.py` (post-filtro del recall), `fact_forget` (scope R3) | `tests/test_scope.py`, `tests/test_fact_forget_scope_r3.py`, `tests/test_lo_scope_vuoto_non_e_un_via_libera.py` | - | FUNZIONA COME PROMESSO | pytest 67 passed |

Reperti: (a) **la multi-tenancy per utente/agente/run è una capacità intera, provata da 67 test e servita da tutte le porte, che il README non racconta** (nessuna riga su `user_id`/`agent_id`/`run_id`/`include_shared`): da aggiungere alla vetrina (ws7) o dichiarare interna; (b) l'isolamento è per prefisso di topic, senza autenticazione per scrittore (dichiarato nel README alla riga 191 per la supersessione): un tenant che conosce il prefisso di un altro lo legge. Nessun P0.
