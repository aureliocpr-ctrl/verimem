# Mappa — `verimem/tools.py`

*ws3 Ricerca. 19 fra classi, metodi e funzioni: **l'esecutore di codice** e
**l'analizzatore statico** che l'agente usa per scrivere e provare software.
Banco: `ws3-mappa-prova-tools.py` (09/09 13:36). Il codice eseguito nel banco è
mio e innocuo — un `print`, un `assert`, un `sleep` per provare il timeout.*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/tools.py:31` `ToolResult` · `verimem/tools.py:37` `ToolResult.to_observation` | il risultato di uno strumento e la sua resa per l'osservazione | **FUNZIONA COME PROMESSO** | riuscito → `'OK\n5900 euro'` · fallito → `"ERROR\ne' caduto"`: l'esito **si legge dalla prima riga**, e un errore non si confonde con un'uscita vuota |
| 2 | `verimem/tools.py:44` `PythonExecutor` · `verimem/tools.py:52` `PythonExecutor.__init__` | «esegue sorgente Python in un sottoprocesso **con un timeout duro**. Non è un sandbox di sicurezza completo — assume che il modello non sia ostile» | **FUNZIONA COME PROMESSO, e il limite è dichiarato nel docstring** | costruttore con `timeout_s=3, max_chars=200`; il resto delle righe misura i suoi rami |
| 3 | `verimem/tools.py:56` `PythonExecutor.run` | esegue e restituisce `ToolResult` | **FUNZIONA COME PROMESSO — quattro rami, incluso il TIMEOUT provato davvero** | `print(2+2)` → `ok=True`, uscita `'4'` · `raise ValueError(...)` → `ok=False` col traceback nell'errore · **`time.sleep(30)` con timeout 3 s → `ok=False`, `error='Timeout after 3s'`, tornato in 3,0 s misurati** (il timeout non è dichiarato, è cronometrato) · `print('x'*5000)` con `max_chars=200` → uscita di **135 caratteri**: il taglio c'è · `stdin='ciao'` con `print(input())` → `'ciao'` |
| 4 | `verimem/tools.py:87` `PythonExecutor.run_with_tests` | «esegue il `source_code` candidato e poi una batteria di test che lo chiama» | **FUNZIONA COME PROMESSO — entrambi i lati** | con `assert somma(2,3) == 5` → `ok=True`; con `assert somma(2,3) == 6` → `ok=False` e l'errore **contiene `AssertionError`**: il fallimento del test arriva al chiamante invece di essere inghiottito |
| 5 | `verimem/tools.py:93` `DockerPythonExecutor` · `verimem/tools.py:112` `DockerPythonExecutor.__init__` | «esegue in un container Docker effimero. Isolamento più forte: `--network=none` → nessuna uscita di rete…» | **FUNZIONA COME PROMESSO nel ramo degradato** | su questa macchina il modulo `docker` non c'è: il costruttore **non solleva**, registra `docker_unavailable error="No module named 'docker'"` e mette `_available=False`. Un componente opzionale che manca non rompe l'import |
| 6 | `verimem/tools.py:137` `DockerPythonExecutor.available` | dice se il backend Docker è utilizzabile | **FUNZIONA COME PROMESSO** | `dk.available` → **False**. ⚠️ È una **`@property`**, non un metodo: `dk.available()` dà `'bool' object is not callable`. L'`ast` la conta come `def`, il decoratore no — errore mio, e il chiamante vero la usa giusta (`tools.py:230`, `if ex.available:`) |
| 7 | `verimem/tools.py:140` `DockerPythonExecutor.run` | esegue nel container | **FUNZIONA COME PROMESSO nel ramo senza Docker; il ramo che esegue è NON MISURATO** | chiamata comunque: `ok=False`, `error='docker backend unavailable (docker SDK or daemon missing)'` — **dice quale pezzo manca**, non «errore». Il ramo con Docker vivo (rete spenta, memoria limitata) qui **non si può misurare**, e lo scrivo |
| 8 | `verimem/tools.py:209` `DockerPythonExecutor.run_with_tests` | come sopra, con la batteria di test | **NON MISURATO** | dipende da 7: senza Docker non arriva a eseguire nulla di significativo. Non l'ho chiamata per non spacciare per misura un secondo «unavailable» |
| 9 | `verimem/tools.py:214` `make_python_executor` | «fabbrica: sceglie il backend da `HIPPO_PYTHON_EXEC_BACKEND` ∈ {subprocess (default), docker}. **Se backend=docker ma Docker non è disponibile**, ripiega» | **FUNZIONA COME PROMESSO — tre casi** | senza variabile → `PythonExecutor` · **`backend=docker` con Docker assente → `PythonExecutor`** (il ripiego promesso, misurato) · `backend=banana` (valore ignoto) → `PythonExecutor`, cioè il default e non un errore |
| 10 | `verimem/tools.py:240` `CodeAnalyzer` · `verimem/tools.py:244` `CodeAnalyzer.syntax_check` | «controlli statici via AST» | **FUNZIONA COME PROMESSO** | valido → `ToolResult(ok=True, output='syntax ok', extra={'nodes': 5})` · rotto → `ok=False, error='SyntaxError: invalid syntax at line 1'`: **la riga è nel messaggio** |
| 11 | `verimem/tools.py:252` `CodeAnalyzer.find_function` | trova una funzione per nome | **FUNZIONA COME PROMESSO — presente e assente** | `find_function(codice, 'somma')` → `ok=True`, `'function `somma` found'`, `extra={'args': ['a','b'], …}` — **restituisce anche la firma** · `'differenza'` → `ok=False`, `'function `differenza` not found'` |
| 12 | `verimem/tools.py:268` `CodeAnalyzer.cyclomatic` | la complessità ciclomatica | **FUNZIONA COME PROMESSO — e cresce coi rami** | funzione lineare → `complexity=1`; funzione con `if/elif/for/if/while` → **`complexity=6`**, e il confronto `>` è `True`. Su codice **rotto** → `ok=False, error='SyntaxError: invalid syntax (<unknown>, line 1)'` invece di un numero inventato |
| 13 | `verimem/tools.py:282` `_ast_node_count` | conta i nodi dell'albero | **FUNZIONA COME PROMESSO** | funzione semplice → **7** nodi · funzione ramificata → **44**: la misura separa i due casi di un fattore sei |
| 14 | `verimem/tools.py:290` `ToolSpec` · `verimem/tools.py:297` `default_tools` | gli strumenti di default dell'agente | **FUNZIONA COME PROMESSO** | `default_tools()` → **4**: `['submit_solution', 'run_python', 'syntax_check', 'find_function']`, ognuno con `name`, `description`, `schema` (JSON Schema) e `handler`. ⚠️ Da tenere insieme all'altra riga della mia parte: **`submit_solution` è esattamente il terminatore che `compilation.execute_macro` pretende** perché una macro sia `ok=True` — i due file si parlano, e il legame regge |
