# Mappa di `verimem/topic_cleanup_suggestions.py` — 2 righe, 125 righe di codice (lead, 09/09 03:58)

Letto per intero. Prova: pytest del lotto G sul tip `20257636` (`117 passed, 1 skipped in 94.44s`, EXIT=0, con `tests/test_topic_cleanup_suggestions.py` e `tests/test_residual_stack_callers_defensive_filter.py`). Chiamanti letti: `verimem/mcp_server.py:15049` (`hippo_topic_cleanup_suggestions`, nome dall'elenco dei tool), `verimem/dashboard_overview_v2.py:15,29`, `benchmark/stress_bench_10k_facts.py:31`. La bozza attribuiva a `_cosine_matrix` un chiamante in `skill_exposure_audit.py`: **falso positivo del nome**. Ciclo #85: 86/836 fatti (10,3%) senza topic in un corpus reale; per ognuno i k vicini fra i fatti con topic (coseno sugli embedding), il topic più votato è la proposta. Claim README: nessuna riga.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/topic_cleanup_suggestions.py:22` `_cosine_matrix` | coseno fra un vettore e ogni riga della matrice; norma 0 → zeri | `topic_cleanup_suggestions` (85) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 117 passed |
| 2 | `verimem/topic_cleanup_suggestions.py:34` `topic_cleanup_suggestions` | per ogni orfano vivo: i 5 vicini con coseno ≥ 0,6, voto per topic con spareggio sulla similarità, proposta; tetto 20; matrice dei vivi costruita una volta | `mcp_server.py:15049`, `dashboard_overview_v2.py:29` | `tests/test_topic_cleanup_suggestions.py` | - | FUNZIONA COME PROMESSO | pytest 117 passed |

Reperti: (a) `np.stack` su TUTTI gli embedding dei fatti con topic (15.000+ a casa × 384 float32 ≈ 23 MB) a ogni chiamata, e `argsort` completo per orfano: O(orfani × vivi); dichiarato «pure-local», non dichiarato il costo; (b) usa `semantic._connect()` (privato) e legge `embedding` grezzo: se un fatto ha embedding a dimensione diversa (`deserialize` fallisce) l'INTERA chiamata rende zero proposte (riga 76-77: un solo fatto rotto azzera il tool) — classe «un'interrogazione vuota che sembra una risposta»; (c) i quarantenati con topic votano (nessun filtro sullo status): T49 nella forma «voto». Nessun P0.
