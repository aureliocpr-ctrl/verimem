# Mappa di `verimem/justified_memory.py` — 12 righe, 264 righe di codice (lead, 08/09 21:20)

Letto per intero. Prova eseguita su origin/main 20257636: `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_justified_memory.py tests/test_justified_memory_bridge.py tests/test_justified_audit_mcp.py tests/test_justified_belief_bench.py` → `37 passed, 1 warning in 13.73s`, EXIT=0. Chiamanti letti: `verimem/mcp_server.py:9744` (il tool MCP di audit chiama `audit_facts`), `verimem/mcp_server.py:859,865` (`collect_contradicted_ids`); `verimem/semantic.py:720,1394,1656` lo citano solo nei commenti. Il README non nomina mai la memoria giustificata (`grep -nEi "justified" README.md` → vuoto); il design sta in `docs/JUSTIFIED_MEMORY.md` (esiste). La docstring promette «the 2027 thesis … no agent-memory product does this»: una promessa che il prodotto espone su UNA porta (il tool MCP di audit, sola lettura) e non racconta all'utente.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/justified_memory.py:33` `Belief` | una proposizione tenuta PERCHÉ ha una giustificazione (fonte + punteggio di grounding), con stato `believed/rejected/retracted/contested/stale`, scadenza, dipendenze da altre credenze | `admit`, `maintain`, `propagate`, `fact_to_belief` | `tests/test_justified_memory.py` (tutti) | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 2 | `verimem/justified_memory.py:45` `Belief.justified` | vera solo nello stato `believed` | `propagate._justified` (98), `served` | `tests/test_justified_memory.py::test_served_excludes_unjustified_and_expired` | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 3 | `verimem/justified_memory.py:50` `admit` | ammette una credenza SOLO se la fonte la giustifica (grounder ≥ θ, default 85), altrimenti `rejected` | nessuno nel prodotto: `git grep -n justified_memory -- verimem/` → solo mcp_server (audit e contraddizioni); il gate di ammissione reale è `anti_confab_gate`/`grounding_gate`, non questa funzione | `::test_admit_grounded_is_believed`, `::test_admit_ungrounded_is_rejected` | - | MAI CHIAMATA (candidata: libreria pura testata, il prodotto ammette con il proprio gate) | comando sopra |
| 4 | `verimem/justified_memory.py:61` `maintain` | passata di manutenzione: superseduta → `retracted`, contraddetta → `contested`, scaduta → `stale`, altrimenti torna `believed`; gli stati terminali non si toccano; pura | `audit_facts` (203) → `mcp_server.py:9744` | `::test_supersede_retracts`, `::test_contradiction_contests`, `::test_expiry_makes_stale`, `::test_contested_recovers_when_contradiction_resolved`, `::test_rejected_and_retracted_are_terminal` | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 5 | `verimem/justified_memory.py:88` `propagate` | ritiro TRANSITIVO (ATMS): una credenza derivata da altre perde la giustificazione quando una di quelle non è più giustificata, fino al punto fisso; pura | `audit_facts` (202) → `mcp_server.py:9744` | `::test_propagate_cascades_retraction`, `::test_propagate_keeps_belief_when_support_holds`, `::test_propagate_ignores_raw_source_deps`, `tests/test_justified_audit_mcp.py::test_typed_derives_from_cascades_end_to_end` | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 6 | `verimem/justified_memory.py:98` `propagate._justified` | plumbing: giustificata = `believed` e non scaduta | `propagate` (111) | via i test di `propagate` | - | FUNZIONA COME PROMESSO (plumbing esercitato dai test della cascata) | pytest 37 passed |
| 7 | `verimem/justified_memory.py:117` `served` | ciò che la memoria serve COME VERITÀ: solo `believed` e non scaduto | `audit_facts` (209) | `::test_served_excludes_unjustified_and_expired` | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 8 | `verimem/justified_memory.py:124` `justified_belief_integrity` | JBI: fra le credenze servite come vere, la frazione davvero vera oggi; 1.0 se non serve niente | nessuno nel prodotto (solo `__all__` e il banco `tests/test_justified_belief_bench.py`) | `::test_jbi_metric`, `::test_jbi_empty_is_vacuously_one`, `tests/test_justified_belief_bench.py` | - | MAI CHIAMATA (candidata: metrica del banco, non del prodotto) | `git grep -n justified_belief_integrity -- verimem/` → solo questo file |
| 9 | `verimem/justified_memory.py:142` `_attr` | plumbing: legge un campo da dict o da oggetto | `fact_to_belief`, `audit_facts`, `collect_contradicted_ids` | via `tests/test_justified_memory_bridge.py::test_fact_to_belief_accepts_dict_and_str_lineage` | - | FUNZIONA COME PROMESSO (plumbing esercitato dal test dict/oggetto) | pytest 37 passed |
| 10 | `verimem/justified_memory.py:148` `fact_to_belief` | da un Fact (duck-typed) a una credenza: la provenienza è la giustificazione; `depends_on` SOLO da un arco tipizzato (`derives_from`/`depends_on`/`lineage_parents`), MAI da `lineage_to` (narrativo: produceva ritiri falsi, R26) | `audit_facts` (201) | `tests/test_justified_memory_bridge.py::test_fact_to_belief_maps_core_fields`, `::test_fact_to_belief_ignores_narrative_lineage_to`, `::test_fact_to_belief_no_provenance_is_unjustified_source`, `::test_fact_to_belief_accepts_dict_and_str_lineage` | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 11 | `verimem/justified_memory.py:178` `audit_facts` | audit di sola lettura sui fatti REALI con trigger deterministici: cosa è servito e cosa VERREBBE ritirato (superseduto o cascata), contestato (contraddetto dal chiamante), scaduto; `scope_topic` filtra il RAPPORTO, non il grafo; nessuna mutazione | `verimem/mcp_server.py:9744` (il tool MCP di audit) | `tests/test_justified_memory_bridge.py::test_audit_facts_*` (6), `tests/test_justified_audit_mcp.py` (6, fra cui `::test_tool_is_registered_with_schema`, `::test_topic_scopes_the_report_not_the_graph`) | - | FUNZIONA COME PROMESSO | pytest 37 passed |
| 12 | `verimem/justified_memory.py:225` `collect_contradicted_ids` | gli id dei fatti VIVI contraddetti da un altro fatto vivo (prefiltro coseno → giudice NLI iniettato); TUTTI E DUE i membri della coppia; i superseduti esclusi; dichiara di ereditare ~3,4% di falsi positivi dell'NLI e di non mutare lo store | `verimem/mcp_server.py:859,865` (opt-in del tool di audit) | `tests/test_justified_memory_bridge.py::test_collect_contradicted_ids_*` (3), `::test_collect_then_audit_contests_the_pair_end_to_end`, `tests/test_justified_audit_mcp.py::test_contradiction_optin_contests_pair` | - | FUNZIONA COME PROMESSO (il 3,4% di falsi positivi è un numero della docstring, misurato da un banco che qui non gira: NON MISURATO) | pytest 37 passed |

Reperti: (a) una capacità intera (memoria giustificata con ritiro transitivo, 37 test verdi) è esposta solo dal tool MCP di audit e il README non la nomina: o si racconta o si dichiara di ricerca; (b) `admit` e `justified_belief_integrity` non hanno chiamanti nel prodotto; (c) `propagate` resta dormiente sui fatti reali finché la scrittura non registra un arco di derivazione tipizzato (dichiarato nel docstring di `fact_to_belief`, R23): la promessa «invalidate a foundational fact and everything inferred from it auto-retracts» oggi vale solo sui fatti che hanno `derives_from`, e nessun writer lo scrive (da verificare quando si mappa `semantic.store`, ws6). Nessun P0.
