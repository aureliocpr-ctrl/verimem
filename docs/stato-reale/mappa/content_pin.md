# Mappa di `verimem/content_pin.py` — 9 righe, 184 righe di codice (lead, 08/09 22:30)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_content_pin.py tests/test_il_riferimento_in_un_fatto_non_esce_dalla_radice.py` → `31 passed, 1 warning in 15.97s`, EXIT=0. Chiamante letto: `verimem/client.py:2743-2797` (`_content_pins`: alla scrittura, per ogni `file:<path>:<riga>` in `verified_by` calcola il pin e lo mette nella ricevuta di audit; un file illeggibile è registrato come `unresolved`). **Nessuno nel prodotto chiama `verify_pin`** (`git grep -nE "verify_pin\(" -- verimem/` → solo questo file): la metà «una lettura successiva sa se l'evidenza dice ancora lo stesso» (docstring, righe 3-7) è scritta e provata dai test, ma nessuna porta la esegue. Il README non nomina i pin. Regola di contenimento ereditata dal verificatore: senza `repo_root` nessun percorso viene aperto (test `test_il_riferimento_in_un_fatto_non_esce_dalla_radice.py`, 4).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/content_pin.py:57` `_normalise` | spazi finali e fine riga non sono contenuto (un checkout CRLF non deve leggersi come modificato) | `line_of_pin`, `pin_for_ref`, `verify_pin` | `::test_pin_ignores_trailing_whitespace`, `::test_pin_ignores_the_line_ending` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 2 | `verimem/content_pin.py:66` `_digest` | `sha256:<hex>` del testo | `line_of_pin`, `pin_for_ref`, `verify_pin` | `::test_pin_is_stable_and_content_derived` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 3 | `verimem/content_pin.py:78` `_is_distinctive` | una riga testimonia la propria sopravvivenza solo se ha almeno 16 caratteri e 3 parole (`pass`, `}` ricorrono ovunque) | `verify_pin` (182) | `::test_a_trivial_line_is_never_reported_as_moved`, `::test_a_distinctive_line_still_reports_moved`, `::test_a_shifted_but_trivial_line_is_changed` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 4 | `verimem/content_pin.py:83` `line_of_pin` | la riga il cui digest è il pin, `''` se nessuna | `verify_pin` (182) | via i test di `moved` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 5 | `verimem/content_pin.py:92` `_resolve` | il percorso sotto la radice di contenimento, altrimenti None (senza radice, symlink fuori, traversal, file mancante) | `_cited_line` (138) | `::test_no_pin_outside_the_repo_root`, `tests/test_il_riferimento_in_un_fatto_non_esce_dalla_radice.py` (4: dentro si apre, le forme che escono sono rifiutate, il byte nullo non apre niente, il file fuori non si raggiunge col nome esatto) | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 6 | `verimem/content_pin.py:112` `_read_lines` | le righe del file; oltre 200.000 righe o su errore, None (non si scorre un gigabyte per un pin) | `_cited_line` (141) | `::test_truncated_file_is_unresolvable`, `::test_deleted_file_is_unresolvable` (il tetto delle 200.000 righe non ha un test: letto) | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 7 | `verimem/content_pin.py:125` `_cited_line` | (la riga citata, tutte le righe) da un `file:<path>:<n>`; None dove non si può leggere | `pin_for_ref` (151), `verify_pin` (170) | `::test_no_pin_for_non_file_refs`, `::test_no_pin_past_the_end_of_file` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 8 | `verimem/content_pin.py:147` `pin_for_ref` | il pin della riga citata, o None = «nessuna affermazione», mai «nessun cambiamento» | `verimem/client.py:2749-2757` (ogni scrittura con `file:` in `verified_by`) | `::test_pin_is_stable_and_content_derived`, `::test_write_receipt_pins_what_it_cited`, `::test_an_unresolvable_file_ref_is_recorded_as_such`, `::test_receipt_without_file_refs_records_no_pins` | - | FUNZIONA COME PROMESSO | pytest 31 passed |
| 9 | `verimem/content_pin.py:155` `verify_pin` | `ok` / `moved` / `changed` / `unresolvable` confrontando il pin registrato con l'evidenza di ADESSO; senza pin o col marcatore `unresolved` → `unresolvable`, mai «ok» | **nessuno nel prodotto** (grep sopra); solo i test | `::test_unchanged_evidence_verifies_ok`, `::test_edited_line_is_changed`, `::test_a_distinctive_line_still_reports_moved`, `::test_deleted_file_is_unresolvable`, `::test_the_unresolved_marker_verifies_as_unresolvable`, `::test_missing_pin_is_unresolvable_not_ok` | - | MAI CHIAMATA (la funzione è corretta e provata; nessuna porta la esegue: i pin si scrivono e nessuno li rilegge) | comando sopra |

Reperti: (a) **T45**: la promessa del modulo è a metà: le ricevute pinnano cosa è stato letto (fatto, provato), ma nessuna superficie (`explain`, `doctor`, `facts get`, TrustReport) chiama `verify_pin` per dire se l'evidenza è ancora quella: un utente non può sapere che il suo `file:` citato è cambiato; (b) il tetto delle 200.000 righe non ha un test. Nessun P0.
