# Mappa di `verimem/settings_v2.py` — 5 righe, 115 righe di codice (lead, 09/09 12:30)

Letto per intero. Prova: pytest del lotto H sul tip `20257636` (`81 passed in 88.10s`, EXIT=0, con `tests/test_settings_v2.py`; `tests/conftest.py` lo usa per il reset dell'ambiente). Chiamanti letti: `refresh_settings` ← `verimem/settings.py:171-172` (dopo `apply_to_env()`); `get_settings` ← `verimem/settings.py:168`; `Settings`/`use_mock` ← `verimem/llm.py:1607-1609`, `verimem/dashboard_routes/settings.py:71,294,432`, `dashboard_routes/layout.py:85` (+19). Proiezione tipizzata e in sola lettura delle variabili `HIPPO_*` (pydantic-settings), affiancata al vecchio `settings.py` per non riscriverlo («migrazione incrementale, no flag-day»). Claim README: nessuna riga descrive le variabili `HIPPO_*` come contratto (grep su «settings» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/settings_v2.py:36` `Settings` | i campi tipizzati con default sicuri: provider/modelli, `llm_max_tokens`, `offline`, `wake_max_steps` [1, 64], i cinque flag di sandbox (shell e computer-use spenti, web e vision accesi), la policy del filesystem, `dashboard_auth_disabled=True`, `dashboard_token`, `trusted_network` | `llm.py`, `dashboard_routes/*`, `settings.py` | `tests/test_settings_v2.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 2 | `verimem/settings_v2.py:90` `Settings.use_mock` | `HIPPO_OFFLINE=1` → MockLLM | `verimem/llm.py:1607-1609`, `dashboard_routes/settings.py:432` | `tests/test_settings_v2.py`, `tests/test_llm_client.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 3 | `verimem/settings_v2.py:100` `_build_settings` | il singleton in `lru_cache(1)` | `get_settings`, `refresh_settings` | `tests/conftest.py` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 81 passed |
| 4 | `verimem/settings_v2.py:104` `get_settings` | il singleton | `verimem/settings.py:168` | `tests/test_settings_v2.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |
| 5 | `verimem/settings_v2.py:109` `refresh_settings` | svuota la cache e rilegge l'ambiente | `verimem/settings.py:171` | `tests/test_settings_v2.py` | - | FUNZIONA COME PROMESSO | pytest 81 passed |

Reperti: (a) **`dashboard_auth_disabled=True` è il default**: la console web nasce senza autenticazione salvo variabile esplicita — coerente con «Niente default OFF» solo se il README lo dice; la mappa di `dashboard.py` (lead, ieri) e quella della console (ws2) devono dire se la porta ascolta solo su localhost; (b) la migrazione «modulo per modulo» dichiarata nel docstring: quanti `os.environ.get("HIPPO_…")` restano fuori da `Settings` non è misurato qui (`git grep` → conteggio da fare nella mappa di `config.py`, ws5); (c) i flag `enable_shell`/`enable_computer_use` sono letti da chi? non da questo modulo: la loro applicazione sta nel sandbox (mappa di ws5). Nessun P0 misurato.
