# Mappa di `verimem/provider_registry.py` — 10 righe, 211 righe di codice (lead, 08/09 22:25)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_provider_registry.py tests/test_llm_default_model_from_registry_scan68.py` → `13 passed in 10.85s`, EXIT=0. Chiamanti letti: `verimem/llm.py:1319` (i modelli di default dei provider dal registro), `verimem/cli.py:2846-2850` (`verimem provider check <nome>`: risoluzione degli alias e lookup), `verimem/dashboard_routes/settings.py:362` (il selettore dei modelli). Il registro è `verimem/providers.yaml` (esiste, 21.076 byte; incluso nel pacchetto: `pyproject.toml` righe 209-233, `package-data` con `providers.yaml`), validato con Pydantic ALL'IMPORT del modulo (riga 153): uno YAML rotto rompe l'import di `llm`. Claim README: riga 262-264 («needs a configured LLM provider … reports `no_provider`»). Il docstring cita ancora `hippoagent/providers.yaml` e `hippo provider check`: nomi vecchi, il codice usa `verimem/`.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/provider_registry.py:32` `RecommendedModel` | un modello curato per il selettore (id, etichetta, fascia, note, capacità che sovrascrivono quelle del provider) | `ProviderSpec.recommended_models`, `dashboard_routes/settings.py:362` | `tests/test_provider_registry.py::test_every_provider_has_required_fields` (via lo schema) | - | FUNZIONA COME PROMESSO | pytest 13 passed |
| 2 | `verimem/provider_registry.py:59` `ProviderSpec` | un provider come dichiarato nello YAML: nome `^[a-z0-9_]+$`, variabile della chiave, endpoint, modello di default, famiglia, alias | `load_registry`, `get_provider`, `llm.py:1319` | `::test_every_provider_has_required_fields`, `::test_invalid_name_pattern_rejected` | `README.md:262` | FUNZIONA COME PROMESSO | pytest 13 passed |
| 3 | `verimem/provider_registry.py:92` `ProviderSpec._aliases_lowercase` | alias minuscoli e senza duplicati, altrimenti `ValueError` | Pydantic, alla validazione | `::test_aliases_map_to_real_providers`, `::test_alias_resolution_case_insensitive` | - | FUNZIONA COME PROMESSO (il rifiuto dei duplicati non ha un test con il nome: letto) | pytest 13 passed |
| 4 | `verimem/provider_registry.py:99` `ProviderSpec.to_legacy_dict` | la forma dict che `llm.py` e la pagina settings si aspettano (`env`, `base_url`, `default_model`, `base_url_env` se c'è) | `LEGACY_PROVIDERS_DICT` (163), `reload_registry` (190) | `::test_legacy_dict_shape_matches_llm_py_expectations`, `::test_legacy_dict_excludes_native_families` | - | FUNZIONA COME PROMESSO | pytest 13 passed |
| 5 | `verimem/provider_registry.py:115` `ProviderRegistry` | lo schema di primo livello: la lista dei provider | `load_registry` (144) | `::test_registry_loads_at_least_20_providers` | - | FUNZIONA COME PROMESSO | pytest 13 passed |
| 6 | `verimem/provider_registry.py:121` `ProviderRegistry._no_duplicate_names` | nomi unici, altrimenti `ValueError` | Pydantic | `::test_no_duplicate_provider_names` | - | FUNZIONA COME PROMESSO | pytest 13 passed |
| 7 | `verimem/provider_registry.py:133` `_registry_path` | `verimem/providers.yaml` accanto al modulo | `load_registry` (140) | via tutti | - | FUNZIONA COME PROMESSO (il file esiste ed è nel pacchetto: `ls` e `pyproject` sopra) | `ls -la verimem/providers.yaml` |
| 8 | `verimem/provider_registry.py:137` `load_registry` | legge e valida lo YAML; su errore di schema `RuntimeError` con il percorso | l'import del modulo (153), `reload_registry` (186) | `::test_invalid_yaml_rejected`, `::test_invalid_name_pattern_rejected` | - | FUNZIONA COME PROMESSO | pytest 13 passed |
| 9 | `verimem/provider_registry.py:175` `get_provider` | il provider per nome canonico o alias, None se ignoto | `verimem/cli.py:2846-2850` | `::test_alias_resolution_case_insensitive`, `::test_get_provider_returns_none_for_unknown` | `README.md:262` | FUNZIONA COME PROMESSO | pytest 13 passed |
| 10 | `verimem/provider_registry.py:182` `reload_registry` | ricarica da un percorso (test) | nessuno nel prodotto (`git grep -n reload_registry -- verimem/` → solo qui); i test | `::test_invalid_yaml_rejected` | - | MAI CHIAMATA (candidata: utilità dei test nel prodotto) | comando sopra |

Reperti: (a) la validazione all'import (riga 153) fa dipendere `import verimem.llm` da uno YAML: giusto per un registro dichiarato come dati, ma un errore nello YAML si presenta come un `RuntimeError` all'import, non come un `doctor` che lo dice (letto, non provato dal prodotto); (b) `tests/test_llm_default_model_from_registry_scan68.py` presidia che i default inline di `llm.py` e il registro non divergano: due copie tenute uguali da un test invece che da una sola fonte. Nessun P0.
