# Mappa di `verimem/reasoning.py` — 4 righe, 256 righe di codice (lead, 09/09 02:22)

Letto per intero. Prova: pytest del lotto D sul tip `20257636` (`73 passed, 6 xfailed in 72.21s`, EXIT=0, con `tests/test_reasoning.py` e `tests/test_hippo_reason_non_esplode_sulle_skill.py`; `_appaia` è nominata anche da `tests/test_recall_chain.py` e `tests/test_il_doppio_prometteva_piu_del_vero.py`). Chiamanti letti: `verimem/mcp_server.py:12392-12420` (`hippo_reason`, che **cabla un coseno vero** via `embedding.encode` su `name\ntrigger` con cache per chiamata), `verimem/recall_chain.py:14` (`_appaia`). Quattro lenti in una chiamata: recall semantico, piani SR in avanti, STRIPS, analogia strutturale; puro locale. Il docstring di `_appaia` racconta il difetto di produzione: `hippo_reason` 6 chiamate, 5 eccezioni (`retrieve` dava oggetti nudi, non coppie), e la scelta «score assente = None, non 0.0». Claim README: nessuna riga nomina `hippo_reason` (grep su «reason» → 65, 210-211, 438: altro senso).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/reasoning.py:30` `_appaia` | `(skill, score)` da coppie o da oggetti nudi (score `None`) | `_safe_recall` (63, 68), `verimem/recall_chain.py:14` | `tests/test_recall_chain.py`, `tests/test_il_doppio_prometteva_piu_del_vero.py` | - | FUNZIONA COME PROMESSO | pytest 73 passed |
| 2 | `verimem/reasoning.py:54` `_safe_recall` | la prima superficie di richiamo disponibile (`skills.retrieve`, poi `recall_skills`); eccezioni → `[]` | `reason_about_task` (164) | `tests/test_hippo_reason_non_esplode_sulle_skill.py` | - | FUNZIONA COME PROMESSO | pytest 73 passed |
| 3 | `verimem/reasoning.py:74` `_summarise` | un paragrafo deterministico dalle quattro sezioni | `reason_about_task` (252) | via `test_reasoning.py` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 73 passed |
| 4 | `verimem/reasoning.py:110` `reason_about_task` | recall top-k; SR in avanti dal top-1 sulle sequenze `skills_used` degli ultimi 500 episodi; STRIPS solo con stato iniziale E finale; analogie del top-1 (struttura ≥ 0,3, coseno ≤ 0,6); riassunto | `verimem/mcp_server.py:12417` (`hippo_reason`) | `tests/test_reasoning.py` | - | FUNZIONA COME PROMESSO | pytest 73 passed |

Reperti: (a) le soglie dell'analogia qui (0,3 / 0,6) sono diverse dai default di `analogy.py` (0,4 / 0,5), e il coseno fra skill ha minimo 0,7425 sul corpus reale (misura del 31/07, mappa `analogy.md`): anche a 0,6 la sezione «analogues» resta vuota per costruzione sul corpus di casa (letto, non misurato oggi); (b) `except Exception: return []` in `_safe_recall` e in `mem.all` nasconde qualsiasi rottura dello store come «nessun richiamo»: la classe del silenzio che `_appaia` ha curato una volta; (c) il costo «< 200 ms» del docstring non è misurato qui. Nessun P0.
