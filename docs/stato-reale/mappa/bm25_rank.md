# Mappa di `verimem/bm25_rank.py` — 6 righe, 220 righe di codice (lead, 08/09 23:55)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_bm25_rank.py tests/test_fusion_quality_guards.py tests/test_la_guardia_dell_overlap_parlava_solo_inglese.py tests/test_due_domande_diverse_stessa_risposta.py` → `24 passed, 1 warning in 20.15s`, EXIT=0. Chiamanti letti: `verimem/semantic.py:4147` (il riordino lessicale della via calda del recall), `:4960` (la fusione RRF: denso + PPR + BM25), `verimem/client.py:1783,2214`, `verimem/documents.py:287`, `verimem/semantic.py:5310` (`_tokens` come «token informativi»), `verimem/ignorance_map.py:63` (la lista delle stopword). Claim README: righe 237-238 (`{"rerank": "applied", "fusion": "applied"}` quando i tre segnali girano). Modulo foglia su FTS5 con trigger di sincronizzazione (O(1) per scrittura) e backfill una tantum; fail-soft: `[]` su qualunque errore, la fusione degrada al solo denso.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/bm25_rank.py:38` `vista` | la clausola della vista curata: senza superseduti, orfani, quarantinati e credenze; con `include_superseded` apre SOLO il ritiro, mai la quarantena (il 02/09 la porta MCP serviva il sostituto e non il ritirato perché la costante fermava il flag) | `bm25_fact_ids` (209) | `tests/test_bm25_rank.py::test_bm25_curated_filter_excludes_quarantined` | - | FUNZIONA COME PROMESSO (il ramo `include_superseded=True` non ha un test con il nome qui: è nei test di `include_superseded` del recall, mappa di `semantic.py`, ws6) | pytest 24 passed |
| 2 | `verimem/bm25_rank.py:115` `_tokens` | i token della query (≥ 2 caratteri) senza le stopword EN+IT (funzionali, interrogative, quantità, preposizioni articolate generate dalla regola; `ai`/`al` escluse di proposito perché «AI» è contenuto nel corpus) | `_to_fts_query`, `_informative_fts_query`, `client.py:1783,2214`, `documents.py:287`, `semantic.py:5310` | `tests/test_la_guardia_dell_overlap_parlava_solo_inglese.py` (3), `tests/test_due_domande_diverse_stessa_risposta.py` | - | FUNZIONA COME PROMESSO | pytest 24 passed |
| 3 | `verimem/bm25_rank.py:120` `_to_fts_query` | OR di token fra virgolette (mai un errore di sintassi FTS5) | **nessuno** (`git grep -n "_to_fts_query(" -- verimem/` → solo la definizione): `bm25_fact_ids` usa `_informative_fts_query` | - | - | MAI CHIAMATA (candidata: la versione senza filtro df, superata) | comando sopra |
| 4 | `verimem/bm25_rank.py:127` `_informative_fts_query` | la MATCH limitata ai token con document-frequency ≤ 25% quando l'indice ha almeno 50 righe (un token onnipresente non ordina nulla); sotto, tutti i token; nessun token informativo → stringa vuota → `[]` | `bm25_fact_ids` (203) | `tests/test_fusion_quality_guards.py`, `tests/test_bm25_rank.py::test_bm25_ranks_exact_rare_token_first` | - | FUNZIONA COME PROMESSO | pytest 24 passed |
| 5 | `verimem/bm25_rank.py:156` `_ensure_fts` | la tabella FTS5 e i tre trigger (insert/delete/update) se mancano; backfill se l'indice è vuoto e `facts` no | `bm25_fact_ids` (202) | `::test_bm25_resyncs_on_new_fact` | - | FUNZIONA COME PROMESSO | pytest 24 passed |
| 6 | `verimem/bm25_rank.py:191` `bm25_fact_ids` | gli id ordinati per BM25 sulla proposizione, con la vista curata; `[]` su query vuota, senza token o su qualunque errore | `verimem/semantic.py:4147,4960` | `::test_bm25_ranks_exact_rare_token_first`, `::test_bm25_failsoft_paths`, `::test_bm25_resyncs_on_new_fact`, `::test_bm25_curated_filter_excludes_quarantined` | `README.md:237-238` | FUNZIONA COME PROMESSO | pytest 24 passed |

Reperti: (a) `_to_fts_query` è la versione superata e nessuno la chiama; (b) la lista delle stopword è una seconda verità accanto a `query_intent._STOP` (dichiarato nel commento, righe 75-78): due liste, due lingue coperte a metà: la classe «liste monolingue» della memoria di casa. Nessun P0.
