# Mappa di `verimem/hallucination_rate.py` — 2 righe, 132 righe di codice (lead, 09/09 01:22)

Letto per intero. Prova: pytest del lotto B sul tip `20257636` (`84 passed, 1 xfailed in 37.23s`, EXIT=0, con `tests/test_hallucination_rate.py`). Chiamante letto: `verimem/mcp_server.py:10413-10415` (`hippo_hallucination_rate`). Il docstring (riscritto il 12/08) è la regola di casa applicata alla metrica: **dove lo store non espone un verdetto la metrica è indefinita, non alta**, e «un prodotto che quarantina i fatti senza fonte non può pubblicare giudizi sui concorrenti senza fonte». Claim README: nessuna riga nomina hallucination-rate (grep → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/hallucination_rate.py:50` `rate_from_verdicts` | per UNA query: `(rischiosi/n, stale/n)` con rischiosi = `obsolete/contested/unverified`; vuoto → `(0, 0)` | `hallucination_rate_at_k` (101) | `tests/test_hallucination_rate.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |
| 2 | `verimem/hallucination_rate.py:63` `hallucination_rate_at_k` | per ogni query non vuota `sm.recall(q, k, trust_signals=True)`; macro sulle query con hit, micro sul totale, `stale_rate` a parte, `verdict_breakdown` a forma fissa; **blackout** (query vere, zero hit) → tassi `None` e `degraded=True`, mai uno 0,0 perfetto | `verimem/mcp_server.py:10415` (`hippo_hallucination_rate`) | `tests/test_hallucination_rate.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |

Reperti: (a) la metrica eredita T50: un fatto `quarantined` che arrivasse ai top-k porterebbe verdetto `trusted` e NON conterebbe come rischio (il recall ordinario li nasconde, quindi oggi il numero è giusto per assenza, non per costruzione); (b) «unverified» include i `model_claim` sotto 0,5 ma NON i `model_claim` sopra 0,5 senza fonte, che sono la maggioranza del corpus di casa (11.271 vivi): la frazione «rischiosa» misura la confidenza dichiarata, non l'assenza di giudizio del moat (`grounding_score=null`); (c) `coverage` e `degraded` sono il controllo positivo che manca altrove: un modello per gli altri righelli. Nessun P0.
