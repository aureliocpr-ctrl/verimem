# Mappa dei 32 moduli del lead a una funzione — 32 righe (lead, 09/09 12:30)

Tutti letti per intero (3.090 righe di sorgente più le bozze, `lotto_J`). Prove sul checkout a `20257636` (il tip), in due esecuzioni: **J1** `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_active_learning.py tests/test_active_probe.py tests/test_adaptive_threshold.py tests/test_agent_workload.py tests/test_apply_recommendations.py tests/test_audit_summary.py tests/test_bench_corpus_scale.py tests/test_causal_skill_mine.py tests/test_compose_macro.py tests/test_counterfactual_rollout.py tests/test_counterfactual_threshold_r3.py tests/test_curate_pipeline.py tests/test_decay.py tests/test_decay_simulate.py tests/test_dream_stuck_hook.py tests/test_recall_diversify.py tests/test_verimem_probe_contested.py tests/test_verimem_probe_independence.py tests/test_verimem_probe_rank.py tests/test_verimem_subject_identity.py` → `133 passed, 22 warnings in 97.06s`, EXIT=0; **J2** `… tests/test_fuse_recall.py tests/test_gate_semantic_conflict_wire.py tests/test_il_cap_non_entra_nel_conteggio.py tests/test_il_recall_mette_davanti_chi_non_e_stato_verificato.py tests/test_llm_keywords_batch.py tests/test_local_relation.py tests/test_metacognition.py tests/test_multi_signal_fusion.py tests/test_outlier_summary.py tests/test_query.py tests/test_recommend_actions.py tests/test_reconcile_judge_env_wiring.py tests/test_session_recap.py tests/test_snapshot_at_time.py tests/test_strips_planner.py tests/test_success_factor.py tests/test_supersede_enforce_sdk.py tests/test_thompson_sampling.py tests/test_time_decay_score.py tests/test_topic_priors.py tests/test_trajectory_fork.py tests/test_una_skill_ritirata_non_si_testa.py` → `193 passed, 1 xfailed, 22 warnings in 140.58s`, EXIT=0. I chiamanti sono da `git grep` per import e per nome su `verimem/ scripts/ benchmark/` (09/09 12:15); i nomi dei tool MCP dalla riga `if name == "hippo_…"` più vicina sopra l'import in `mcp_server.py`. **Un modulo senza chiamante si prova con un grep vuoto sull'intero repo** (fatto per active_probe, decay, diversify, snapshot_at_time: solo il CHANGELOG li nomina).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/__init__.py:70` `__getattr__` | `Memory`, `Client`, `open_memory`, `AutoMemory` caricati pigramente (PEP 562), così `import verimem` resta leggero; e `__version__ = "0.7.7"` allineata a pyproject dal test `test_version_single_source.py` | chiunque scriva `from verimem import Memory` (README:119, 136, 172, 177 usano `Memory(...)`) | `tests/test_engram_code.py`, `tests/test_il_pannello_non_esplode_senza_il_db.py` (+2) | README:119 (`Memory(llm=…)`) | FUNZIONA COME PROMESSO | pytest J1/J2 + import vivo |
| 2 | `verimem/active_learning.py:60` `select_stuck_candidates` | gli id delle skill `candidate` con 3-10 prove e fitness lisciata in (0,3, 0,5), le più vecchie prima, max 3; DB assente → `[]` | `verimem/dream_stuck_hook.py:40` | `tests/test_active_learning.py`, `tests/test_dream_stuck_hook.py` | - | FUNZIONA COME PROMESSO (l'ipotesi H1 «4,3% → >10% in 20 cicli» del docstring non ha un esito registrato) | pytest J1 |
| 3 | `verimem/active_probe.py:44` `probe_fact` | la sonda che falsifica un fatto copulare contro lo store: rivale vivo, stesso soggetto, valore diverso, fonte indipendente; `refuted_proposed` solo con garanzia STRETTAMENTE migliore, `contested` a parità, `inconclusive` se ogni rivale è squalificato, `survived` con `unbeaten+1` | **nessuno** (`git grep active_probe|probe_fact` sull'intero repo → solo CHANGELOG:1302 e un commento in `composer.py:160`) | `tests/test_active_probe.py`, `tests/test_verimem_probe_contested.py`, `tests/test_verimem_probe_independence.py`, `tests/test_verimem_probe_rank.py` | README:317-319 («Paired with **active probes** that build the query which would falsify a stored fact … proposes a `refuted` label, surviving grows its `unbeaten` bound») | NON COME PROMESSO: il README promette le sonde attive come parte del prodotto e **nessuna porta le chiama** (T54) | grep vuoto + pytest J1 |
| 4 | `verimem/adaptive_threshold.py:28` `adaptive_thresholds` | `(purezza, coesione)` decrescenti col corpus: (0,40, 0,20) fino a 1.305 fatti, (0,20, 0,10) a 1.889, (0,10, 0,05) da 5.000 «extrapolation; revisit when reached» | `verimem/auto_dream_worker.py:187`, `scripts/bench_observer_shift.py` | `tests/test_adaptive_threshold.py` | - | FUNZIONA COME PROMESSO (il corpus di casa è a 15.675 vivi: la soglia «da rivedere quando raggiunta» è superata tre volte e mai rivista) | pytest J1 |
| 5 | `verimem/agent_workload.py:16` `compute_workload` | «aggregate facts + episodes by agent_id» con sbilanciamento (σ/max) | `verimem/mcp_server.py:10258` (`hippo_agent_workload`) | `tests/test_agent_workload.py` | - | NON COME PROMESSO: gli episodi non sono attribuiti, `n_episodes` è SEMPRE 0 («placeholder for future attribution», riga 37) e il payload non lo dice | pytest J1 |
| 6 | `verimem/ann_gate.py:35` `default_min_n` | `ENGRAM_ANN_MIN_N` se numerica, altrimenti 100.000: la domanda «serve l'ANN?» senza importare faiss (misurato il 31/07: 274,6 ms risparmiati per una risposta che è «no» nel 100% dei casi reali) | `verimem/ann_cache.py:30`, `verimem/ann_index.py:36`, `verimem/semantic.py:58` | nessuno per nome | - | NON MISURATO (due righe, lette; nessun test la nomina) | letto |
| 7 | `verimem/apply_recommendations.py:22` `apply_recommendations` | `skill_health` su ogni skill e, con `apply`, `promote/retire` persistiti via `skills.store()`; dry-run di default | `verimem/mcp_server.py:12017` (`hippo_apply_recommendations`), `verimem/curate_pipeline.py:55` | `tests/test_apply_recommendations.py`, `tests/test_curate_pipeline.py`, `tests/test_skill_promote_by_threshold.py` | - | FUNZIONA COME PROMESSO (`except Exception: continue` sullo store: una skill non salvata sparisce da `changes` senza contarsi) | pytest J1 |
| 8 | `verimem/audit_summary.py:13` `summarize_audit` | conteggi per esito e per tool, ultimi rifiuti, rate-limit, riga di riassunto | `verimem/mcp_server.py:11241` (`hippo_audit_summary`) | `tests/test_audit_summary.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 9 | `verimem/bench_corpus_scale.py:36` `run_corpus_scale_bench` | banco sintetico del coseno (corpus casuale a 384 dimensioni, percentili di latenza) | nessuno | `tests/test_bench_corpus_scale.py` | - | MAI CHIAMATA (banco dentro il pacchetto) | pytest J1 |
| 10 | `verimem/causal_skill_mine.py:19` `causal_skill_mine` | le regole di `causal_extract` che ricorrono ≥ 2 volte, con conteggio, confidenza media e coppie di evidenza | `verimem/mcp_server.py:10647` (`hippo_causal_skill_mine`), due script demo | `tests/test_causal_skill_mine.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 11 | `verimem/compose_macro.py:19` `compose_macro` | una meta-skill `schema` da ≥ 2 skill ordinate: pre della prima, post dell'ultima, lineage, `candidate`; il chiamante la salva | `verimem/mcp_server.py:11948` (`hippo_compose_macro`), `promote_chain` (per test) | `tests/test_compose_macro.py`, `tests/test_momentum_macro.py`, `tests/test_promote_chain.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 12 | `verimem/counterfactual_rollout.py:19` `rollout_actions` | `simulate_action` per ogni azione candidata, ordinate per `p_success`; raccomandata SOLO sopra la soglia 0,55 (prima usciva la prima comunque: soglia morta, curata) | `verimem/mcp_server.py:10424` (`hippo_rollout_actions`) | `tests/test_counterfactual_rollout.py`, `tests/test_counterfactual_threshold_r3.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 13 | `verimem/curate_pipeline.py:25` `curate_pipeline` | sei passi in uno (predicati, raccomandazioni, duplicati, grafo, taglia, anteprima del decadimento), dry-run di default | `verimem/mcp_server.py:11828` (`hippo_curate_pipeline`), `verimem/dashboard_overview.py:5` | `tests/test_curate_pipeline.py`, `tests/test_dashboard_overview.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 14 | `verimem/decay.py:34` `apply_time_decay` | penalità lineare sulle similarità oltre 3 giorni di grazia (5%/giorno, tetto 20%); «used by the embedding daemon» | **nessuno** (`git grep apply_time_decay` sull'intero repo → solo CHANGELOG:1938) | `tests/test_decay.py` | - | MAI CHIAMATA (il docstring dice che il daemon la usa: falso; il decadimento vivo del recall è `freshness.is_stale`, mappa a parte) | grep vuoto + pytest J1 |
| 15 | `verimem/decay_simulate.py:12` `decay_simulate` | anteprima in sola lettura dei candidati alla potatura (`decay_pruning_candidates`) | `verimem/mcp_server.py:11843` (`hippo_decay_simulate`), `curate_pipeline.py:84` | `tests/test_decay_simulate.py`, `tests/test_curate_pipeline.py` | - | FUNZIONA COME PROMESSO | pytest J1 |
| 16 | `verimem/diversify.py:38` `mmr_select` | MMR (Carbonell-Goldstein): `n` candidati diversi da un pool profondo; misurato il 25/07 sulle 5 domande multi-hop LoCoMo sbagliate: k=30 1/5, k=100 grezzo 2/5, k=100→MMR 30 3/5; «OFF by default» perché sul recall reale la ridondanza è 7% | **nessuno** (`git grep mmr_select|diversify` in `verimem/ scripts/` → niente) | `tests/test_recall_diversify.py` | - | MAI CHIAMATA («OFF by default» senza un interruttore: non c'è codice che la accenda) | grep vuoto + pytest J1 |
| 17 | `verimem/fuse_recall.py:50` `fuse_recall` | RRF sulle liste recency/confidence/recency_decayed più liste esterne | `scripts/bench_fuse_recall.py`, `scripts/bench_hybrid_vs_fuse.py` (nessun modulo del prodotto) | `tests/test_fuse_recall.py`, `tests/test_il_recall_mette_davanti_chi_non_e_stato_verificato.py` | README:237-238 nomina `fusion: applied` — ma quella fusione è `semantic.py:4925-5014` (`_ranking_note("fusion", …)`), non questo modulo | MAI CHIAMATA dal prodotto (la catena cicli 191-197 `rank_list_builders → time_decay_score → fuse_recall` vive solo nei banchi) | grep + pytest J2 |
| 18 | `verimem/llm_keywords_batch.py:60` `augment_keywords_batch` | il batch che cammina sui fatti con `trigger_keywords` vuote o corte, chiama il modello iniettato e persiste; per-riga senza abortire | nessuno | `tests/test_llm_keywords_batch.py` | - | MAI CHIAMATA (la catena `llm_keywords_augment → batch` non ha porta né hook) | pytest J2 |
| 19 | `verimem/local_relation.py:76` `_build_label_mapper.mapper` | una riga di probabilità → `{contradiction, entailment, neutral}` via l'`id2label` del modello (mai posizionale: due modelli in cache hanno ordini opposti) | `make_nli_classifier` (123) | `tests/test_local_relation.py` | - | FUNZIONA COME PROMESSO (plumbing; il resto del modulo è in `local_relation.md`) | pytest J2 |
| 20 | `verimem/metacognition.py:34` `assess_recall_confidence` | `high/medium/low/none` da similarità massima (0,7/0,5/0,3), ≥ 3 episodi e accordo ≥ 75% per «high»; `score = 0,7·sim + 0,3·accordo`; suggerimento di ripiego | `verimem/mcp_server.py:10632` (`hippo_assess_confidence`), due script demo | `tests/test_metacognition.py` | - | FUNZIONA COME PROMESSO (l'«accordo» esiste solo su risultati con `outcome`: sui fatti vale 0) | pytest J2 |
| 21 | `verimem/multi_signal_fusion.py:30` `rrf_fuse` | RRF di Cormack 2009 (k=60), spareggio sul rango nella PRIMA lista (WF3 20/06), voci non stringa saltate | `verimem/ppr_seed.py:24`, `verimem/fuse_recall.py:38` | `tests/test_multi_signal_fusion.py`, `tests/test_fuse_recall.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 22 | `verimem/outlier_summary.py:9` `summarize_top_outliers` | i primi N di `detect_anomalies` (cluster ≥ 3, maggioranza 0,6) con la spiegazione | `verimem/mcp_server.py:9920` (`hippo_outlier_summary`) | `tests/test_outlier_summary.py` | - | FUNZIONA COME PROMESSO (eredita il criterio `num_steps` mancante di `anomaly_detection`) | pytest J2 |
| 23 | `verimem/query.py:22` `query_skills` | filtri AND (status, prove, fitness, nome, predicati, macro) + ordinamento + tetto | `verimem/mcp_server.py:12270` (`hippo_query_skills`) | `tests/test_query.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 24 | `verimem/recommend_actions.py:30` `recommend_actions` | `skill_health` per tutte, raggruppate per azione e ordinate; i TOTALI prima del taglio (`n_by_action`: il sommario diceva «test: 50» dove erano 254, misurato 50→50, 200→200, 10000→254) e `retired` nel sommario (315 su 325) | `verimem/mcp_server.py:12218,7093` (`hippo_skills_recommend_actions`), `apply_recommendations.py` | `tests/test_recommend_actions.py`, `tests/test_il_cap_non_entra_nel_conteggio.py`, `tests/test_una_skill_ritirata_non_si_testa.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 25 | `verimem/session_recap.py:13` `session_recap` | episodi, esiti, token, skill e fatti nuovi da `since` | `verimem/mcp_server.py:11200` (`hippo_session_recap`) | `tests/test_session_recap.py` | - | FUNZIONA COME PROMESSO (due `except Exception: pass` muti — la classe dei 30/37 `list_facts` di Piattaforma — e `list_facts(limit=10000)` senza `hide_low_trust`: i quarantenati contano come «fatti nuovi») | pytest J2 |
| 26 | `verimem/snapshot_at_time.py:25` `snapshot_at_time` | i fatti VIVI a un istante T (`created_at ≤ T` e `superseded_at` nullo o > T); `status_floor` «currently NOT enforced — placeholder» | **nessuno** (`git grep snapshot_at_time` sull'intero repo → solo CHANGELOG:1833) | `tests/test_snapshot_at_time.py` | README:572/590 (`GET /v1/snapshot`) è un'altra cosa: lo snapshot del gateway | MAI CHIAMATA (seconda implementazione di «vivo a T» accanto all'`as_of` del recall, T33: classe ①; e accetta un parametro che ignora) | grep vuoto + pytest J2 |
| 27 | `verimem/strips.py:41` `plan_strips` | BFS in avanti sui predicati: il piano più corto ≤ `max_depth` o None; goal già vero → `[]`; skill senza post potate | `verimem/mcp_server.py:12526` (`hippo_plan_strips`), `verimem/reasoning.py:26`, `verimem/chain_render.py:3` | `tests/test_strips_planner.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 28 | `verimem/success_factor.py:17` `analyze_success_factors` | per skill usata ≥ 3 volte: usi, successi, tasso, ordinati | `verimem/mcp_server.py:10298` (`hippo_success_factors`) | `tests/test_success_factor.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 29 | `verimem/thompson_sampling.py:40` `thompson_sample_candidates` | campione Beta(s+1, t−s+1) per skill `candidate` con < 10 prove, i `max_n` migliori; `rng_seed=None` non deterministico | `verimem/dream_thompson_hook.py:27` | `tests/test_thompson_sampling.py`, `tests/test_dream_thompson_hook.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 30 | `verimem/time_decay_score.py:28` `decay_score` | moltiplicatore in [0, 1] per età: exp (emivita 14 g), power, linear (90 g); input cattivo → 1,0 | `verimem/rank_list_builders.py:29,145` (chiamato solo da `fuse_recall`: banchi) | `tests/test_time_decay_score.py` | - | FUNZIONA COME PROMESSO (raggiungibile solo dai banchi, vedi riga 17) | pytest J2 |
| 31 | `verimem/topic_priors.py:55` `apply_topic_penalty` | −10% ai fatti con topic `lessons/*` quando la domanda è «da task» (nessun token meta: lesson, lezione, regola, errore, come funziona, what is, definizione) | `verimem/semantic.py:1074,1084` (il recall) | `tests/test_topic_priors.py`, `tests/test_topic_penalty_wire.py` | - | FUNZIONA COME PROMESSO | pytest J2 |
| 32 | `verimem/trajectory_fork.py:19` `trajectory_fork` | i passi con `step_idx < from_step` più un seme controfattuale opzionale a `from_step`, `fork_id` casuale; `from_step < 0` → `ValueError` | `verimem/mcp_server.py:9811` (`hippo_trajectory_fork`), `scripts/trajectory_demo.py` | `tests/test_trajectory_fork.py` | - | FUNZIONA COME PROMESSO | pytest J2 |

Reperti del lotto: (a) **T54**: `active_probe.probe_fact` è promessa dal README (317-319) e non ha nessuna porta: la capacità esiste, è testata su quattro file, e nessun utente può eseguirla — la classe «capacità pronta e mai esercitata» nella forma peggiore, perché è in vetrina; (b) altri cinque moduli senza chiamante nel prodotto (decay, diversify, fuse_recall e la sua catena, llm_keywords_batch, snapshot_at_time, bench_corpus_scale): la lista del lead arriva a **diciassette** (resource_monitor, hot_reload, sos_compensator, recall_usage, codebase_ingest, coding_reflection, betweenness_cache, embedding_quantize, self_curation, temporal_narrative, lab_longmemeval_adapter, parallel_drafter, decay, diversify, fuse_recall, llm_keywords_batch, snapshot_at_time) più bench_corpus_scale; (c) due docstring che dicono cosa credeva l'autore: `decay` («used by the embedding daemon»), `adaptive_threshold` («revisit when reached», superato tre volte); (d) `agent_workload` rende un campo sempre a zero senza dirlo (NON COME PROMESSO); (e) `session_recap` e `stats_velocity` contano i quarantenati come fatti (T49, forma «conteggio»).
