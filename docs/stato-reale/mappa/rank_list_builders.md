# Mappa di `verimem/rank_list_builders.py` — 3 righe, 159 righe di codice (lead, 09/09 01:26)

Letto per intero. Prova: pytest del lotto B sul tip `20257636` (`84 passed, 1 xfailed in 37.23s`, EXIT=0, con `tests/test_rank_list_builders.py` e `tests/test_il_recall_mette_davanti_chi_non_e_stato_verificato.py`). Chiamante letto: `verimem/fuse_recall.py:98,104,110` (le tre liste per la fusione RRF). Leggono il DB **direttamente con `sqlite3` dal path**, non tramite `SemanticMemory`: un altro posto con la propria copia del filtro degli status. Claim README: nessuna riga (grep su «recency» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/rank_list_builders.py:32` `recency_rank` | id dei fatti vivi non `orphaned/quarantined` per `created_at DESC`, filtro topic opzionale; DB assente o errore SQL → `[]` | `verimem/fuse_recall.py:98` | `tests/test_rank_list_builders.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |
| 2 | `verimem/rank_list_builders.py:68` `confidence_rank` | idem per `confidence DESC, created_at DESC` | `verimem/fuse_recall.py:104` | `tests/test_rank_list_builders.py`, `tests/test_il_recall_mette_davanti_chi_non_e_stato_verificato.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |
| 3 | `verimem/rank_list_builders.py:104` `recency_decayed_rank` | sovracampiona 2× per recency, poi ordina per `decay_score(età)` (curva exp/power/linear, emivita 14 g) e taglia a `limit` | `verimem/fuse_recall.py:110` | `tests/test_rank_list_builders.py` | - | FUNZIONA COME PROMESSO | pytest 84 passed |

Reperti: (a) **il filtro degli status nascosti è una copia parziale**: qui `NOT IN ('orphaned','quarantined')`, in `list_facts(hide_low_trust=True)` anche `user_belief` — tre liste per lo stesso concetto (`_STATUS_RANK` del recall, `list_facts`, questa SQL): classe «una copia invece della superficie unica», e `user_belief` entra nella fusione RRF per recency e confidenza; (b) il «sovracampiona 2×» di `recency_decayed_rank` è un ordinamento per `created_at DESC` tagliato a `2·limit` e poi riordinato per decadimento monotono nell'età: **il decadimento non può cambiare l'ordine** di una lista già ordinata per età (la funzione è monotona), quindi le tre curve rendono lo stesso ordine dei primi `limit` per recency — letto, non misurato; se confermato, il segnale «recency decayed» è il segnale «recency» con un altro nome. Nessun P0.
