# Mappa di `verimem/coding_reflection.py` — 4 righe, 189 righe di codice (lead, 09/09 01:52)

Letto per intero. Prova: pytest del lotto C sul tip `20257636` (`62 passed in 20.70s`, EXIT=0, con `tests/test_coding_reflection.py`). Chiamanti: **nessuno in `verimem/`** (nessun import del modulo nel prodotto: `git grep` su `verimem/`); solo `scripts/bench_coding_reflection.py:89,111,132,150`. Il docstring (ciclo #142, 18/05) lo dichiara «il primo pezzo della copertura coding: registra ogni Edit/Bash failure come episodio»: **nessuna porta (MCP, CLI, SDK) registra un errore di coding con questa API**. Claim README: nessuna riga (grep su «coding» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/coding_reflection.py:54` `extract_error_signature` | la firma `ErrorType:file:riga:prima_parola` dall'ultimo frame di un traceback Python; vuoto → `empty:::`; non Python → `unknown:?:?:<sha1[:8]>` | `capture_coding_error` (113), `recall_similar_errors` (181), il banco | `tests/test_coding_reflection.py` | - | MAI CHIAMATA dal prodotto (pura e provata) | pytest 62 passed |
| 2 | `verimem/coding_reflection.py:88` `_extract_correction` | il blocco dopo `--- CORRECTION ---` in `final_answer` | `recall_similar_errors` (187) | via il test | - | MAI CHIAMATA dal prodotto | pytest 62 passed |
| 3 | `verimem/coding_reflection.py:96` `capture_coding_error` | un episodio `failure` con traceback (+diff, +correzione) e `task_id` `coding/error/<Tipo>`; rende `{episode_id, signature, task_id}` | il banco `scripts/bench_coding_reflection.py:111` | `tests/test_coding_reflection.py` | - | MAI CHIAMATA dal prodotto | pytest 62 passed |
| 4 | `verimem/coding_reflection.py:146` `recall_similar_errors` | top-k episodi `failure` per firma (o testo) con `min_similarity=-1` (la firma è una stringa non naturale: il pavimento del coseno è disattivato, «il chiamante filtra da sé») | il banco (132, 150) | `tests/test_coding_reflection.py` | - | MAI CHIAMATA dal prodotto | pytest 62 passed |

Reperti: (a) **modulo intero senza chiamante nel prodotto** (si aggiunge a resource_monitor, hot_reload, sos_compensator, recall_usage, codebase_ingest): 189 righe, 4 funzioni, un banco, zero porte; (b) `min_similarity=-1.0` rende TUTTI i fallimenti candidati e nessuna soglia a valle: se un giorno viene cablato, l'astensione va ricostruita; (c) l'idea (firma canonica dell'errore come chiave di recall) è quella giusta per la «copertura coding» promessa nel docstring, e la lezione di casa vale: una capacità spenta non emette segnale. Nessun P0.
