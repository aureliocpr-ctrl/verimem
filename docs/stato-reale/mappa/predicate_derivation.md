# Mappa di `verimem/predicate_derivation.py` — 3 righe, 209 righe di codice (lead, 09/09 03:14)

Letto per intero. Prova: pytest del lotto F sul tip `20257636` (`105 passed in 31.36s`, EXIT=0, con `tests/test_predicate_derivation.py`, `tests/test_batch_predicate_derivation.py`; lo nomina anche `tests/test_curate_pipeline.py`). Chiamanti letti: `derive_predicates_from_episodes` ← `mcp_server.py:12348,12365` (`hippo_skill_derive_predicates`, nome dall'elenco dei tool); `derive_predicates_batch` ← `curate_pipeline.py:21,41`, `mcp_server.py:12332` (`hippo_skills_derive_predicates_batch`). Bootstrap dei predicati STRIPS dai dati: `after_<X>` come precondizione se X precede IMMEDIATAMENTE la skill in ≥ soglia delle sue occorrenze; `after_<skill>` sempre come postcondizione. Claim README: nessuna riga (grep su «predicate» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/predicate_derivation.py:33` `_self_marker` | `after_<id>` | le due sotto | via i test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 105 passed |
| 2 | `verimem/predicate_derivation.py:37` `derive_predicates_from_episodes` | per UNA skill: precondizioni dai predecessori immediati a frequenza ≥ soglia (auto-predecessore escluso), postcondizione `after_<id>`; soglia fuori da [0, 1] → `ValueError` | `verimem/mcp_server.py:12365` | `tests/test_predicate_derivation.py` | - | FUNZIONA COME PROMESSO | pytest 105 passed |
| 3 | `verimem/predicate_derivation.py:91` `derive_predicates_batch` | tutte le skill in una passata (indice globale `(pred, succ) → conteggio`), dry-run di default, `apply` persiste, `overwrite` sostituisce; statistiche | `verimem/curate_pipeline.py:41`, `verimem/mcp_server.py:12332` | `tests/test_batch_predicate_derivation.py`, `tests/test_curate_pipeline.py` | - | FUNZIONA COME PROMESSO | pytest 105 passed |

Reperti: (a) l'indice globale del batch conta `prev != cur`, la funzione singola conta `prev != skill_id`: due implementazioni della stessa regola che coincidono solo perché la seconda condizione implica la prima quando `cur == skill_id` (letto: coincidono; classe ①); (b) `except Exception: applied_this = False` nasconde un errore dello store come «non applicato» (silenzio dichiarato dal payload, non dalla causa); (c) «game-changer for STRIPS-on-real-data» (docstring) non è misurato: quante skill del corpus di casa hanno predicati dopo il batch non è scritto qui. Nessun P0.
