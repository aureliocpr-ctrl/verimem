# Mappa di `verimem/hopfield.py` — 4 righe, 150 righe di codice (lead, 09/09 02:00)

Letto per intero. Prova: pytest del lotto C sul tip `20257636` (`62 passed in 20.70s`, EXIT=0, con `tests/test_hopfield_completion.py`; lo nomina anche `tests/test_recall_hopfield.py`, non eseguito qui). Chiamanti letti: `hopfield_recall` ← `verimem/memory.py:1772-1778` (dentro `EpisodicMemory.recall`, SOLO se `use_hopfield=True`, parametro a riga 1703 con default `False`); **nessuna porta passa `use_hopfield`** (`git grep` su `mcp_server.py`, `cli.py`, `client.py` → nessuna riga; l'unico chiamante con `use_hopfield=True` è `scripts/bench_recall_ablation.py:185`). `hopfield_complete` ← `hopfield_recall` e `scripts/bench_hopfield.py`. La bozza attribuiva a `_normalize` chiamanti in `dentate_gyrus.py` e `hippo_pagerank.py`: **falsi positivi del nome**. Ramsauer et al. 2020, `completed = Mᵀ·softmax(β·M·cue)`. Claim README: nessuna riga (grep su «hopfield» → nessuna).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/hopfield.py:47` `_normalize` | il vettore a norma 1 (invariato se norma 0) | `hopfield_complete` (102) | via il test | - | FUNZIONA COME PROMESSO (plumbing) | pytest 62 passed |
| 2 | `verimem/hopfield.py:52` `_stable_softmax` | softmax con sottrazione del massimo (niente overflow a β alto); vuoto → vuoto; somma 0 → uniforme | `hopfield_complete` (107) | via il test | - | FUNZIONA COME PROMESSO | pytest 62 passed |
| 3 | `verimem/hopfield.py:66` `hopfield_complete` | `(pattern completato, pesi di attenzione che sommano a 1, id)` sulla matrice dei summary embedding; memoria vuota → tre vuoti | `hopfield_recall` (132), `scripts/bench_hopfield.py` | `tests/test_hopfield_completion.py` | - | FUNZIONA COME PROMESSO | pytest 62 passed |
| 4 | `verimem/hopfield.py:112` `hopfield_recall` | top-k episodi per peso di attenzione (una probabilità sull'intera memoria, non un coseno) | `verimem/memory.py:1778` solo con `use_hopfield=True` ← `scripts/bench_recall_ablation.py:185` | `tests/test_recall_hopfield.py` (letto per nome) | - | MAI CHIAMATA dal prodotto (il percorso esiste in `EpisodicMemory.recall` e nessuna porta lo accende) | pytest 62 passed |

Reperti: (a) **percorso di recall alternativo pronto e mai acceso da una porta** (classe «capacità spenta»): la scelta è cablarlo (un parametro di `hippo_recall`/CLI) o dichiararlo banco; (b) usa due privati di `EpisodicMemory` (`_ensure_recall_index`, `_batch_get_episodes`, `noqa: SLF001`): accoppiamento dichiarato; (c) il docstring dice «milliseconds for N=10k»: non misurato qui. Nessun P0.
