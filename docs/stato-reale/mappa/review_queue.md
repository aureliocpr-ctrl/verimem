# Mappa di `verimem/review_queue.py` — 8 righe, 211 righe di codice (lead, 08/09 21:14)

Letto per intero. Prova eseguita sul checkout a `7b9e8ca1` (i 16 commit fino a `20257636` non toccano questo file né i test eseguiti: `git diff --stat 7b9e8ca1 20257636` vuoto su di essi): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_review_queue_backpressure.py tests/test_l_avviso_nomina_il_comando_che_drena.py tests/test_il_quarantinato_ritirato_ha_lasciato_la_coda.py` → `22 passed, 1 warning in 19.65s`, EXIT=0. Chiamante letto: `verimem/client.py:993-1000`: SOLO sulla scrittura che finisce `quarantined` la ricevuta riceve `backpressure_warning`; `verimem/cli.py:400`, `corpus_health_metrics.py:109` e `soggetto_valore.py:32` lo citano nei commenti. Il README (righe 147, 153) promette che una scrittura in banda viene «held for review» e non bloccata; il modulo misura quanto è profonda quella coda e lo dice nella ricevuta. Limite DICHIARATO nel docstring (righe 13-19): non esiste una ragione di quarantena per fatto, quindi la profondità conta TUTTO il backlog quarantinato, non la sola classe «review»; è lo stesso reperto del design del terzo stato di ws3 (08/09).

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/review_queue.py:45` `reset_cache` | svuota le profondità memoizzate (test e chi ha appena drenato) | nessuno nel prodotto (`git grep -n reset_cache -- verimem/` → solo questo file): la usano i test | `tests/test_review_queue_backpressure.py` | - | MAI CHIAMATA (candidata: utilità per i test; chi drena la coda dal prodotto, `facts requalify-quarantined`, non la chiama, quindi il numero memoizzato resta vecchio fino a 60 s) | comando sopra |
| 2 | `verimem/review_queue.py:50` `threshold` | `ENGRAM_REVIEW_QUEUE_MAX`: 0 spegne il segnale di proposito; un valore malformato o negativo torna al default 500 (un refuso non deve spegnere un allarme) | `pressure` (163) | `::test_threshold_comes_from_the_env`, `::test_zero_threshold_disables_the_signal`, `::test_bad_threshold_values_never_disable_the_check` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 3 | `verimem/review_queue.py:68` `_count` | quarantinati e non superseduti (un superseduto ha lasciato la coda); store illeggibile → 0, mai un'eccezione su una scrittura | `depth` (139) | `::test_depth_counts_the_quarantined_backlog`, `::test_depth_ignores_admitted_facts`, `::test_depth_ignores_superseded_rows`, `::test_unreadable_store_reports_no_pressure`, `tests/test_il_quarantinato_ritirato_ha_lasciato_la_coda.py` (4) | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 4 | `verimem/review_queue.py:95` `recenti` | quanti del backlog sono ENTRATI negli ultimi 7 giorni (deposito contro flusso; `created_at` è un epoch, non una data: il confronto con `date('now')` restituiva 0 in silenzio) | `backpressure_warning` (184) | `::test_il_conteggio_recente_separa_il_deposito_dal_flusso`, `::test_il_recente_legge_created_at_come_EPOCH_non_come_data` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 5 | `verimem/review_queue.py:130` `depth` | la profondità corrente; con `max_age_s > 0` riusa una lettura recente (la via di scrittura), altrimenti la verità adesso | `pressure` (164) | `::test_the_count_is_cached_not_run_per_write` | - | FUNZIONA COME PROMESSO | pytest 22 passed |
| 6 | `verimem/review_queue.py:144` `note_enqueued` | conta nella profondità memoizzata la scrittura che sta entrando, così il numero in cache non tace un picco fino alla prossima COUNT; può solo sotto-contare (le scritture di altri processi restano invisibili fino alla finestra) | `backpressure_warning` (173) | nessuno per nome (`git grep -n note_enqueued -- tests/` → vuoto); esercitata dentro `::test_backpressure_lands_on_the_write_that_adds_to_the_queue` solo come effetto | - | FUNZIONA COME PROMESSO (la promessa «il numero in cache cresce di uno a ogni scrittura in coda» NON ha un test proprio: NON MISURATA in isolamento) | pytest 22 passed |
| 7 | `verimem/review_queue.py:160` `pressure` | `{depth, threshold, over}`; `over` è False se il segnale è spento o lo store illeggibile | `backpressure_warning` (172) | `::test_zero_threshold_disables_the_signal`, `::test_unreadable_store_reports_no_pressure`, `::test_no_backpressure_below_the_threshold` | `README.md:147,153` | FUNZIONA COME PROMESSO | pytest 22 passed |
| 8 | `verimem/review_queue.py:168` `backpressure_warning` | la voce di ricevuta per una scrittura che si aggiunge a una coda oltre soglia, o None; mai alza; dice quanti sono arrivati negli ultimi 7 giorni e NOMINA i comandi che drenano (`verimem facts quarantine-log`, `facts requalify-quarantined`), non solo come far tacere l'allarme | `verimem/client.py:997-998` (solo sulla scrittura quarantinata) | `::test_backpressure_lands_on_the_write_that_adds_to_the_queue`, `::test_admitted_writes_are_never_annotated`, `tests/test_l_avviso_nomina_il_comando_che_drena.py` (3: l'avviso nomina un comando eseguibile, OGNI comando nominato esiste davvero, non spiega solo come tacere) | `README.md:147` | FUNZIONA COME PROMESSO | pytest 22 passed |

Reperti: (a) il limite dichiarato (la profondità conta tutto il backlog quarantinato, non la classe «review») è lo stesso buco che il terzo stato di ws3 va a chiudere con `held_reason`: quando quella colonna entra, questo modulo deve contare la classe giusta (il docstring lo dice da sé); (b) `reset_cache` non è chiamata da chi drena: dopo un `requalify` il numero in ricevuta può restare vecchio per 60 s. Nessun P0.
