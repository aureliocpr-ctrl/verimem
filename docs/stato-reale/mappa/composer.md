# Mappa — `verimem/composer.py`

*ws3 Galileo. 8 funzioni. Qui vive **la definizione di «stesso soggetto»** che
tutto il prodotto usa per far incontrare due fatti rivali: se sbaglia, due fatti
che si contraddicono non si incontrano mai e restano entrambi in memoria.
Banchi: `ws3-mappa-prova-quattro-moduli.py`, `ws3-mappa-prova-tre-dubbi.py`
(09/09 12:21-12:23).*

| # | funzione (file:riga) | cosa promette | verdetto | prova (comando e esito) |
|---|---|---|---|---|
| 1 | `verimem/composer.py:214` `subject_key` | «**L'UNICA** definizione di "stesso soggetto", per ogni lettore che raggruppa fatti rivali — il rilevamento conflitti del guardiano e il contro-esempio della sonda attiva» | **FUNZIONA COME PROMESSO** | `'Il canone del capannone'` e `'il  Canone   del capannone'` (maiuscole e spazi doppi) → **la stessa chiave** `'canone del capannone'`; `'The dog'` e `'a dog'` → entrambe `'dog'` (l'articolo cade da tutte e due le lingue); `"L'aula magna"` → `"l'aula magna"` |
| 2 | `verimem/composer.py:296` `normalizza_apostrofi` | «porta ogni variante tipografica di apostrofo su `U+0027`» | **FUNZIONA COME PROMESSO** | le tre varianti `l’aula` (U+2019), `lʼaula` (U+02BC), `l'aula` → **tutte e tre** `"l'aula"` |
| 3 | `verimem/composer.py:318` `_copula_parse` | «`"Rex is a labrador."` → `("rex", "labrador", "a labrador")` — soggetto in minuscolo come scritto, testa dell'oggetto **senza** articolo, oggetto **con** articolo» | **FUNZIONA COME PROMESSO — incluso il caso che temevo** | `'Rex is a labrador.'` → `('rex', 'labrador', 'a labrador')`. E il controllo che ho fatto **contro il mio stesso sospetto**: `'Rex is not a labrador.'` → `('rex', 'not a labrador', 'not a labrador')` — la negazione finisce nell'**oggetto**, il soggetto resta `'rex'`, quindi `subject_key` dei due fatti opposti è **la stessa** (`True`) e il rilevamento conflitti li fa incontrare. Avevo visto una frase malformata dare `('questa non', …)` e stavo per scrivere un difetto: il caso realistico lo ha smentito |
| 4 | `verimem/composer.py:301` `_copula_match` | riconosce la forma copulativa | **FUNZIONA COME PROMESSO** (misurata attraverso `_copula_parse`, che la usa) | le frasi che non sono copule danno `None` a valle: `"Il canone e' 5900 euro."` → `None`, `"Rex e' nel capannone."` → `None` |
| 5 | `verimem/composer.py:201` `_strip_article` | «toglie l'articolo iniziale. Con `lingua` usa SOLO gli articoli di quella lingua (l'oggetto di una copula: lì «a» inglese e «a» italiano vogliono trattamenti diversi)» | **FUNZIONA COME PROMESSO — e la prova è il caso incrociato** | `('the dog','en')` → `dog` · `('il cane','it')` → `cane` · **`('a dog','it')` → `a dog` immutato**: l'articolo inglese non viene tolto quando la lingua dichiarata è l'italiano, che è esattamente ciò che il docstring promette |
| 6 | `verimem/composer.py:235` `_apre_un_locativo` | «la prima parola dell'oggetto è una preposizione di `lingua`? Confronta **PAROLE INTERE, non prefissi**: `nel` è una preposizione, `Nelson` e `nelle`…» | **FUNZIONA COME PROMESSO** | `('nel','it')` → `True` · `('Nelson','it')` → **`False`** (il prefisso non basta) · `('nelle','it')` → `True` (è una preposizione articolata, non un falso positivo) |
| 7 | `verimem/composer.py:46` `_min_score` | il punteggio minimo perché una composizione sia ammessa | **FUNZIONA COME PROMESSO** | `_min_score()` → **55.0** |
| 8 | `verimem/composer.py:332` `compose_once` | «una passata di composizione sullo store vivo. Restituisce un rapporto **onesto**: `{eligible, candidates, admitted, rejected_gate, skipped_known, admitted_ids}`» | **FUNZIONA COME PROMESSO (rapporto onesto su un corpus che non produce candidati)** | store con 2 fatti ammessi → `{'eligible': 2, 'copula_facts': 0, 'candidates': 0, 'admitted': 0, 'rejected_gate': 0, 'rejected_noncommittal': 0, 'skipped_known': 0, 'admitted_ids': [], 'run_id': 'ws3-mappa'}`. **`eligible: 2` e `copula_facts: 0`**: il rapporto dice *perché* non ha composto (nessuno dei due fatti è in forma copulativa) invece di restituire uno zero muto. ⚠️ **NON MISURATO** il ramo che ammette: serve un corpus con due copule sullo stesso soggetto |
