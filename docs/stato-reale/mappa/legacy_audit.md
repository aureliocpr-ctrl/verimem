# Mappa di `verimem/legacy_audit.py` — 6 righe, 231 righe di codice (lead, 09/09 00:29)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_legacy_audit.py tests/test_mcp_legacy_audit.py tests/test_legacy_cleanup.py` (insieme a `test_working_memory.py`) → `30 passed, 1 warning in 16.58s`, EXIT=0. Chiamanti letti: `verimem/legacy_cleanup.py:28` (l'orchestrazione che agisce sui bucket), `verimem/mcp_server.py:15164` (il tool `hippo_legacy_audit`). Il README non nomina i fatti `legacy_unverified`. Origine dichiarata (16/05): 815 fatti su 864 erano `legacy_unverified`, nascosti dal filtro ma non curati; il classificatore li divide in tre bucket per una decisione umana, senza mutare niente (V1 = rapporto). Il docstring cita `scripts/audit_legacy_corpus.py`: la sua esistenza non è stata verificata in questa mappa.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/legacy_audit.py:72` `_has_verified_signal` | vero al primo segnale di provenienza nel testo (`bash:`, `file:<path>:<n>`, `url:`, arxiv/github, `sha256:`, `pytest`, `exit 0`, `commit <sha>`) | `classify_legacy_fact` (123) | `tests/test_legacy_audit.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 2 | `verimem/legacy_audit.py:81` `_has_forget_signal` | corto (< 16), confidenza < 0,35 o parole di dubbio (`TODO`, `FIXME`, `deprecated`, `not sure`, `???`) | `classify_legacy_fact` (132) | `tests/test_legacy_audit.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 3 | `verimem/legacy_audit.py:98` `LegacyClassification` | il risultato per un fatto (bucket e motivo) | `classify_legacy_fact` | `tests/test_legacy_audit.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 4 | `verimem/legacy_audit.py:113` `classify_legacy_fact` | ordine: verificato-alla-rilettura > dimenticabile > recuperabile (un riferimento vero vince anche se il fatto è corto o poco confidente) | `verimem/legacy_cleanup.py:28`, `audit_legacy_corpus` (207) | `tests/test_legacy_audit.py`, `tests/test_legacy_cleanup.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 5 | `verimem/legacy_audit.py:154` `_select_facts` | la popolazione: tutti (`any`) o solo `legacy_unverified` (saltando chi non ha lo stato) | `audit_legacy_corpus` (196) | `tests/test_legacy_audit.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |
| 6 | `verimem/legacy_audit.py:175` `audit_legacy_corpus` | il rapporto: conteggi per bucket e fino a 5 campioni ciascuno, nessuna mutazione | `verimem/mcp_server.py:15164` | `tests/test_mcp_legacy_audit.py`, `tests/test_legacy_audit.py` | - | FUNZIONA COME PROMESSO | pytest 30 passed |

Reperti: (a) il segnale «verificato alla rilettura» è LESSICALE: un fatto che contiene la parola `pytest` o `exit 0` nel testo viene promosso a candidato «verified», senza verificare che il riferimento esista (il commento lo dichiara: «look like provenance evidence»); chi agisce sui bucket (`legacy_cleanup`, mappa a parte) deve saperlo; (b) `sm.all()` carica tutto il corpus in memoria per classificarlo: sul corpus di casa (18.070 fatti) è misurabile, non misurato qui. Nessun P0.
