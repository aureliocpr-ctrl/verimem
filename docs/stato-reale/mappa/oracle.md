# Mappa di `verimem/oracle.py` — 3 righe, 147 righe di codice (lead, 09/09 00:40)

Letto per intero. Prove eseguite sul checkout a `20257636` (il tip): (1) `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_fact_chain.py tests/test_find_duplicate_facts.py tests/test_facts_merge.py tests/test_il_duplicato_identico_passava_in_silenzio.py tests/test_oracle.py tests/test_l_oracolo_non_risponde_su_due_preposizioni.py tests/test_prompt_skeleton.py tests/test_failure_diagnosis.py tests/test_cross_agent_consensus.py tests/test_memory_compaction.py` → `61 passed, 22 warnings in 82.08s`, EXIT=0 (i sette moduli di questo lotto insieme); (2) **prova alla porta MCP** (`_call_tool_impl`, store isolato con `HIPPO_DATA_DIR`, controllo positivo stampato `True`): due fatti nello store, uno `quarantined` («Kubernetes 1.31») e uno `model_claim` («1.28»); `hippo_oracle_query` con la domanda «quale versione di Kubernetes usa il cluster OnlyPaws» → `quarantenato SERVITO=True · vivo servito=True · 521 char`. Chiamante letto: `verimem/mcp_server.py:10133-10150` (`hippo_oracle_query`), che pesca con `a.semantic.list_facts(limit=10000, offset=0)` **senza `hide_low_trust`**: il default di `list_facts` (`semantic.py:3750-3796`) esclude solo i superseded, e nasconde `quarantined/orphaned/user_belief` **solo su opt-in**. Sul DB di casa (`~/.engram/semantic/semantic.db`, letto il 08/09 22:29): totale 18.087 · vivi 15.675 · di cui nascosti al recall 1.403 · `list_facts(limit=10000)` ne rende 10.000 ordinati per `created_at DESC`: **5.675 fatti vivi non vengono mai scansionati**, e i 1.403 quarantenati sì. Claim README: nessuna riga nomina l'oracolo (grep su oracle/oracolo → nessuna riga); la promessa che viola è quella del server (istruzioni MCP e README): «a fact its source does not support is QUARANTINED — stored, but kept OUT of default recall».

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/oracle.py:18` `_tokens` | i token che portano argomento, senza le parole vuote (lista presa da `document_index._PAROLE_VUOTE`, non ricopiata: il 01/08 due preposizioni bastavano a superare la soglia) | `oracle_query` (58, 70, 87, 106) | `tests/test_l_oracolo_non_risponde_su_due_preposizioni.py` | - | FUNZIONA COME PROMESSO | pytest 61 passed |
| 2 | `verimem/oracle.py:42` `_jaccard` | Jaccard, 0 se un insieme è vuoto (copia n. 11 di 18 in `verimem/`) | `oracle_query` | via `test_oracle.py` | - | FUNZIONA COME PROMESSO (plumbing) | pytest 61 passed |
| 3 | `verimem/oracle.py:48` `oracle_query` | il recupero cross-tier in una chiamata (episodi per `task_text`, fatti per `proposition`, skill per `trigger`, con verdetto di confidenza a soglie 0,3/0,5/0,7) | `verimem/mcp_server.py:10143` (`hippo_oracle_query`) | `tests/test_oracle.py`, `tests/test_l_oracolo_non_risponde_su_due_preposizioni.py` | - | NON COME PROMESSO alla porta: serve i fatti QUARANTENATI come risultati (prova 2), e vede solo i 10.000 più recenti | porta MCP 08/09 22:33 + pytest 61 passed |

Reperti: (a) **T49**: `hippo_oracle_query`, `hippo_chain_facts`, `hippo_prompt_skeleton`, `hippo_cross_agent_consensus`, `hippo_find_duplicate_facts`, `hippo_facts_find_duplicates` servono un fatto `quarantined` (6/6 alla porta, stessa esecuzione; `hippo_recall` sullo stesso store ha reso 2 char ed è la risposta GIUSTA: è la memoria EPISODICA e lo store non aveva episodi — correzione di Porte/ws2 alle 22:42, il mio «non indagato» era una riga da non scrivere); la cura è `hide_low_trust=True` nei chiamanti di recupero (oracle, chain, skeleton, consensus) e un campo `status` nell'uscita dei due strumenti di dedup, che possono legittimamente vedere tutto; decisione collegiale, non applicata; (b) il tetto di 10.000 per `created_at DESC` è silenzioso: il payload non dice quanti fatti NON ha visto (5.675 su questo corpus); (c) la confidenza è il massimo delle similarità Jaccard: «high» sopra 0,7 di sovrapposizione lessicale, non un giudizio sul contenuto.
