# Mappa di `verimem/stable_partition.py` — 8 righe, 211 righe di codice (lead, 08/09 23:15)

Letto per intero. Prova eseguita sul checkout a `20257636` (il tip): `env -u HIPPO_ENCODE_DELEGATE_ONLY python -m pytest -q tests/test_stable_partition.py tests/test_auto_dream_stable_partition_envvar.py` → `21 passed in 12.06s`, EXIT=0. Chiamanti letti: `verimem/skill_emergence_detector.py:216-222` e `:262-266` (il rilevatore di skill emergenti chiama `stable_partition` con la partizione precedente quando la stabilizzazione è abilitata; la variabile d'ambiente che la accende è provata da `test_auto_dream_stable_partition_envvar.py`, 3 test sui due punti di chiamata). Il README non nomina la partizione stabile né l'emergenza delle skill. Il docstring dichiara il compromesso (A4): la partizione stabile NON è ottima per modularità a t1; è stabile e monotona, giusta per la pipeline delle skill, sbagliata per l'analisi dell'evoluzione delle comunità.

| # | funzione (`file:riga`) | cosa promette | chiamata da | test che la esercita | claim README | verdetto | prova |
|---|---|---|---|---|---|---|---|
| 1 | `verimem/stable_partition.py:42` `Partition` | nodo → comunità, estendibile senza riassegnare i nodi esistenti | `stable_partition`, `partition_jaccard`, `skill_emergence_detector.py:55` | `tests/test_stable_partition.py` (tutti) | - | FUNZIONA COME PROMESSO | pytest 21 passed |
| 2 | `verimem/stable_partition.py:50` `Partition.values_as_sets` | le comunità come lista di insiemi | `partition_jaccard.co_pairs` (190), `skill_emergence_detector.py` | `::test_jaccard_zero_for_unchanged` | - | FUNZIONA COME PROMESSO | pytest 21 passed |
| 3 | `verimem/stable_partition.py:57` `Partition.community_of` | la comunità di un nodo, None se assente | nessuno nel prodotto (`git grep -n "community_of(" -- verimem/` → solo qui) | - | - | MAI CHIAMATA (candidata) | comando sopra |
| 4 | `verimem/stable_partition.py:61` `_louvain_fresh` | Louvain da zero con seme; grafo vuoto o errore → partizione vuota | `stable_partition` (150) | `::test_new_nodes_get_assignment` (il caso senza prior), `::test_empty_db_returns_empty` | - | FUNZIONA COME PROMESSO | pytest 21 passed |
| 5 | `verimem/stable_partition.py:79` `_extend_with_new_nodes` | ogni nodo nuovo va nella comunità del vicino già assegnato con il peso più alto; senza vicini, un singleton `fresh-NNN`; ordine deterministico | `stable_partition` (167) | `::test_new_nodes_get_assignment` | - | FUNZIONA COME PROMESSO | pytest 21 passed |
| 6 | `verimem/stable_partition.py:111` `stable_partition` | la partizione stabile del grafo semantico: senza prior Louvain fresco; con prior i nodi invariati ereditano e solo i nuovi vengono assegnati; DB mancante o grafo vuoto → vuota; i nodi spariti (superseduti) sono lasciati cadere in silenzio (dichiarato «future work») | `verimem/skill_emergence_detector.py:217,263` | `::test_inherits_unchanged_assignments`, `::test_new_nodes_get_assignment`, `::test_empty_db_returns_empty`, `::test_missing_db_returns_empty`, `tests/test_auto_dream_stable_partition_envvar.py` (3) | - | FUNZIONA COME PROMESSO | pytest 21 passed |
| 7 | `verimem/stable_partition.py:170` `partition_jaccard` | distanza di Jaccard fra due partizioni sulle coppie co-raggruppate, con restrizione a un insieme di nodi (per isolare i nodi invariati); 0 se concordano | nessuno nel prodotto (`git grep -n partition_jaccard -- verimem/` → solo qui); i test e i banchi | `::test_jaccard_zero_for_unchanged` | - | MAI CHIAMATA (candidata: è il righello della garanzia «ΔJ sui nodi invariati = 0», usato dai test e non dal prodotto) | comando sopra |
| 8 | `verimem/stable_partition.py:188` `partition_jaccard.co_pairs` | le coppie co-raggruppate di una partizione, ristrette | `partition_jaccard` (198-199) | `::test_jaccard_zero_for_unchanged` | - | MAI CHIAMATA (come la 7) | comando sopra |

Reperti: (a) la garanzia del docstring («ΔJ sui nodi invariati = 0») ha il suo test (`test_jaccard_zero_for_unchanged`) e il righello (`partition_jaccard`) vive nel prodotto senza chiamanti: sta bene in un banco; (b) la caduta silenziosa dei nodi spariti è dichiarata come lavoro futuro dal 23/05: un fatto superseduto esce dalla partizione senza che la pipeline lo sappia. Nessun P0.
