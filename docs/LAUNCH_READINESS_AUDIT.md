# Engram — Launch Readiness Audit (2026-06-06)

> Living register for the public-launch readiness review. Every verdict carries
> **evidence** (command output / commit SHA / file:line), severity, and action.
> No "OK" without a receipt. Driven autonomously; Aurelio steers.

Legend — Status: ✅ pass · ⚠️ gap (non-blocking) · ❌ blocker · ⏳ in progress · ⬜ not started
Severity: P0 launch-blocker · P1 important · P2 polish

| # | Dimension | Status | Evidence (receipt) | Action |
|---|-----------|--------|--------------------|--------|
| A | Functional — CI pytest (clean env) | ✅ | **CI GREEN — commit fcdb22b, run 27076016424: 11/11 test jobs (3 OS × 4 Python) + build ALL pass. FIRST all-green CI in 40+ runs.** | done |
| B | Bugs (real, not contention) | ✅ | the ~432F were NOT 432 bugs — SINGLE root: delegate-only (9da43c4) made the test stub raise EncodeDelegateUnavailable (stub patched `_model` but not `_MODEL`→is_loaded()=False) once HIPPO_ENCODE_DELEGATE_ONLY leaked. Fix proven: 86 pass/1 skip with flag forced ON | — |
| C | Installable (git clone → pip → run) | ✅ | CI build (sdist+wheel) + clean-wheel smoke-install pass on fcdb22b; `engram`/`hippo` entrypoints work | done |
| D | First-run UX (model download) | ✅ | `engram warmup` shipped 3935ba4 (live 35.8s) | done |
| E | CLI UX (commands, --help, coherence) | ✅ | 24 commands, clear --help/descriptions, sub-apps (skills/episodes/facts/providers); `reset` guarded by typer.confirm/--yes | done |
| F | MCP server (schemas, dispatch, errors) | ✅ | `test_list_tools_returns_expected_set` pins the advertised set + asserts every tool has a non-empty description & a JSON-schema with `type`; **NEW** `test_every_advertised_tool_is_dispatchable` invokes all advertised tools and asserts none is an orphan (listed but →"unknown tool" on call), guarding list↔dispatch drift across the full surface; dispatch runs schema-validation + capability-gate + rate-limit before every handler | done |
| G | Security (secrets, sandbox, deps) | ✅ | CI "security" workflow (pip-audit/safety/bandit/ruff-S) GREEN consistently; 0 hardcoded secrets (only redaction.py regex); sandbox=subprocess (documented honestly) | done |
| H | Scalability (recall@scale, write lock) | ✅ | recall cosine-search scales linear but fast: 0.5ms@5k → 5.7ms@100k → 10.4ms@200k (encode-dominated @71ms; 585MB matrix @200k); write bounded 6274107; FAISS opt-in for >200k | done |
| I | Save/recall reliability (the 1-month bug) | ✅ | 2 roots fixed: 9da43c4 (cold-load) + 6274107 (write-lock), proven | deploy-on-restart |
| J | Presentable (README, naming, badges) | ✅ | README config/stats fixed 78d5ea6; `hippoagent/` is a clean compat SHIM (1 file→engram), not a dup; naming migration handled | badges refresh |
| K | Docs coherence (PLATFORM/QUICKSTART/STATE) | ✅ | fixed PLATFORM.md MCP config (hippo→engram, no-key) + MCP_QUICKSTART tool count 175→228; archive/ docs left as dated snapshots | done |
| L | Packaging (LICENSE, version, CHANGELOG) | ✅ | LICENSE+CHANGELOG+CONTRIBUTING+SECURITY.md+CODE_OF_CONDUCT.md present, v0.3.0, 6 extras, urls ok | done |
| M | Dashboard / web UX | ✅ | test_dashboard_api 28/28 pass (pages render, auth gate, design-system, CSS, KPI endpoints) | done |

## Findings log (newest first)
<!-- append: [SEV] dimension — finding — evidence — fix/commit -->
- **[P1→FIXED] I Reliability — recall byte-filter frozen-at-import (latent blackout)** — `semantic._EXPECTED_EMBEDDING_BYTES` was computed ONCE at import. A runtime embedding model/dim switch (MiniLM-384→e5-768) updated `model_signature()` live but left the recall length-guard at the old dim → every new-dim vector silently excluded from facts/episodes/skills recall (total blackout, zero error). FIX (`567a1f8`): PEP 562 module `__getattr__` recomputes `embedding.expected_embedding_bytes()` LIVE on every access (covers the in-function `from .semantic import` in memory.py/skill.py too); 2 internal callers call it directly. TDD `tests/test_recall_byte_filter_live.py` RED(`1536!=9001`)→GREEN; 14/14 + 67 recall/isolation/skill/episode regression green; ruff clean. Defused `scripts/flip_embedding.py` (hardcoded MiniLM-L12-384 landmine → refuse-to-run stub); added model-agnostic `scripts/reembed_to_active_model.py`.
- **[VERIFIED] I Reliability — corpus embedding consistency + self-curation** — Flipped the live corpus to the active model: facts **4605/4605** now 768d/e5 (was 2591), skills 326/326 e5 (re-embedded 2020 facts + 39 skills incl. 37 mislabeled by a prior partial flip + 6 dim=0 repaired incl. chain-tip). HONEST recall impact: only **18** of those facts are non-quarantined (newly reachable); 2002 are quarantined (now consistent, not silently dim-mismatched). Falsification PASS: formerly-stranded facts now recalled (rank 1 & 5). **Self-management is RELIABLE**: the 44% quarantine (2014 facts, 1879 at conf≥0.6) is curation WORKING — it hides test-pollution (stress-w0/w1, cap20, PYTEST wire, bridge roundtrip), auto-hook duplicates, archives, diary from live recall; sampled user-role(59)+random = 100% test/noise, ZERO good facts wrongly hidden. Test data-dir IS isolated (conftest cycle#25 `HIPPO_DATA_DIR`+`ENGRAM_DIR`→tmp) — live pollution is historical residue, not an active leak. Backups: `~/.engram/backups/manual/*.pre-reembed-*`. Residual: MCP restart needed for the 18 to surface (running cache stale); ~2000 historical cruft facts sit quarantined (bloat only).
- **[bonus, VERIFIED] Docker zero-setup on-ramp (#56)** — built `engram:local` from the existing Dockerfile and SMOKE-TESTED it: `docker run --rm engram:local engram status` → rc 0 (entrypoint + DB init OK); and OFFLINE (`HF_HUB_OFFLINE=1 HIPPO_OFFLINE=1 TRANSFORMERS_OFFLINE=1`) `engram warmup --no-daemon` → "✓ model ready in 28.5s (vector dim 768)" — the **embedding model is baked into the image and loads with ZERO network/download**, i.e. a real zero-setup mainstream on-ramp + air-gap-ready. **CAVEAT (P2, deferred):** the image is **7.35 GB** because `torch` pulls the full CUDA stack (nvidia-* wheels) — wasted for a CPU-only memory layer. A CPU-only torch (`torch==…+cpu` / `--extra-index-url …/cpu`) would slim it to ~1–1.5 GB — that is the real "lean headless edition" work in #56 (NOT done tonight; Aurelio chose "verify + stop").
- **[P1→FIXED] A Functional (Windows CI)** — after the conftest+16-fixes, CI dropped to **7/11 green**; the 3 windows-latest jobs (py3.10/3.11/3.12) failed on `test_contradiction_budget` (numeric+boolean `zero_budget_returns_early`) — and only there, ubuntu+macos green, and they PASS on the dev Windows box. ROOT: the budget guard used strict `>` (`time.monotonic()-start > time_budget_s`); on the coarse Windows CI clock (~15ms) the first iteration's elapsed is exactly `0.0`, so `0.0 > 0.0` is False → a pair leaks through → `!= []`. FIX (`contradiction.py` ×2): `>=` — `elapsed >= 0.0` is always True, so a 0.0 budget returns early deterministically on any clock. Verified: 4/4 local pass, ruff clean.
- **[P1→FIXED] A Functional — remaining 16** — after the conftest fix CI dropped 432→16 failures, triaged + fixed: (a) `test_mcp_eager_preload` asserted pre-delegate-only behavior → updated to the new contract (a503a84); (b) `test_ide_path_injection`×8 bounced on 401 (dashboard X-Hippo-Token auth not sent) → client fixture authenticates (a503a84); (c) 6 tests (airgap egress, resonator embed×3, concurrent-save-SLO, cross-process index) failed with `OSError: couldn't connect to huggingface.co` — they use the REAL model (subprocess / un-stubbed) but the CI runner had no cached model + offline → added a `engram warmup` CI step to populate the HF cache before the offline Tests; (d) `test_parallel_drafter` flaky perf (0.75× at t_seq=2.7ms timer noise) → skip when timing is unmeasurable. Pending CI confirm.
- **[P0→FIXED] A/B Functional** — CI pytest, once unblocked by the lint fix, failed ~400 tests (all `test_wake_*` + `test_verified_by_validation` + `test_write_path_auto_supersede`) with `EncodeDelegateUnavailable`. **ROOT (my regression):** delegate-only `9da43c4` made `_encode_one` raise when `HIPPO_ENCODE_DELEGATE_ONLY=1` is active; that flag LEAKS permanently into the pytest process whenever a test runs `mcp_server.main()` in-process (`os.environ.setdefault`, not undone); and the conftest stub patched `_model` but not the `_MODEL` global, so `is_loaded()`=False → raise instead of using the stub. **FIX:** `tests/conftest.py` stub now also `setattr(embedding,"_MODEL",stub)` + `delenv(HIPPO_ENCODE_DELEGATE_ONLY)`. **Evidence:** repro `HIPPO_ENCODE_DELEGATE_ONLY=1 pytest test_wake_metrics` → 4F before; after fix 86 pass/1 skip across the CI-failing sample with the flag forced ON. Commit `<conftest>`.
