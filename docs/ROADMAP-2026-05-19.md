# HippoAgent Roadmap — 2026-05-19

Handoff doc for a fresh Claude Code instance picking up HippoAgent
development. Verified empirically against `git log main` and
`gh pr list` at 2026-05-19 07:37 UTC. **No confabulation** — every
item here is either an explicit fact in HippoAgent memory or a
verified pending design with file paths.

## Starting state (verified)

- **main is clean**: 10 PRs merged tonight (cycle 145→159 stack
  including cycle 159 sub-experiments).
- Last 5 commits on main: cycle 159 → 152 → 157 → 155 → 154.
- 0 PRs open. No outstanding rebase work.
- `pytest tests/` against the freshly merged main: 32/32 cycle 159
  tests pass; full regression 3269 passed, 3 pre-existing flakies
  (test_ide shell-env + test_ssrf internal-ips + test_real_provider_smoke
  no-API-key — all known, not new).

## Top-priority pending work (cycle 160+)

### 1. HaluMem benchmark adapter
- **Paper**: arxiv 2511.03506 (HaluMem — operation-level hallucination
  detection benchmark for memory-augmented LLMs).
- **Why**: HippoAgent's anti-confab story (L1/L1.5/L1.7 detectors,
  Charter v1, validate_claim gates) needs an external yardstick.
- **What**: implement an adapter that ingests HaluMem episodes,
  routes through HippoAgent's write path with `validate=full`, scores
  the detector's TPR/FPR against the labeled ground truth.
- **Effort**: ~2-3 days. Adapter is a CLI subcommand
  (`engram bench halumem ...`) reading JSONL + emitting metrics.
- **Status**: not started. Fact `lessons/halumem-benchmark-adapter`
  has prior notes (recheck with `hippo_facts_search`).

### 2. LongMemEval adapter
- **Why**: complements HaluMem on the *recall* axis (HaluMem stresses
  hallucination, LongMemEval stresses long-horizon retrieval).
- **What**: 6-step design already drafted (search HippoAgent memory
  for `lessons/longmemeval-design-6step`). Implement the adapter.
- **Effort**: ~1 week.
- **Status**: design pronta, code 0%.

### 3. arxiv paper: "Write-time Confabulation Gates"
- **State**: DRAFT 2694 words in `docs/papers/` (verify with
  `ls docs/papers/`).
- **What's missing**: empirical section. Now we have new evidence
  from cycle 159.7/159.8 scaling experiments — fold them in.
- **Effort**: ~2 days for finalize + arxiv submission.

### 4. Cycle 158 — subprocess cross-process integration test for UNIQUE INDEX
- **Context**: cycle 156 design doc (`docs/cycle156_unique_index_cross_process_design.md`)
  proposed alternative A (partial UNIQUE INDEX), implemented cycle 157
  (`engram/semantic.py` `_migrate_v3_to_v4`).
- **What's missing**: cycle 156 §5.2 step 2 — `subprocess.Popen × 2`
  empirical test that two HippoAgent processes racing on the same DB
  cannot both persist a master fact for the same topic. Windows
  subprocess + db_path coordination delicate, deferred from cycle 157.
- **Effort**: ~1 day.
- **Files**: add to `tests/test_consolidation_unique_index_cross_process.py`.

### 5. Cycle 159 follow-ups (queued from opus review + sonnet team
   findings, NOT applied tonight to avoid scope creep):

   a. **Charter v2 with `verify_claim(path, wc_l)` filesystem read**
      (lessons cycle 159.4 + 159.8). Without enforcement at the protocol
      level, both single-sonnet and team can confabulate cite-line.
   b. **Three real consolidation.py bugs found by team Arm-D**
      (cycle 159.8) but NOT applied to avoid clashing with merged
      cycle 154+155+157 — re-verify they still reproduce on the new
      main, then fix:
      - Self-loop `(ep.id, ep.id)` in `causal_edges` when sub-facts
        have no `source_episodes` (line 282-ish, post-rebase).
      - Idempotency probe checks `proposition` only, ignores `topic`
        (could be obsoleted by the cycle 157 UNIQUE INDEX — verify).
      - LIKE wildcard injection on `prefix` (`%`/`_` in topic
        segments). Cycle 157 may or may not have closed this.
   c. **Test gaps from opus review** (cycle 159.5): concurrent poll
      cursors diverge, file corruption mid-poll, member name with `[`.

## Lower-priority / research

- **Orphan worktree decision** (cycle 159 ADR
  `docs/cycle159_ADR_parallel_worktrees.md`): there is an alternative
  cycle-159 implementation in `worktrees/quizzical-khorana-625663/
  hippoagent/teams/` (224 LoC, MemberState class, file-based teams).
  Aurelio to decide: delete, port ideas, or document as design
  alternative. Doesn't block any forward work.
- **Repeat scaling experiment with N≥5** across task families
  (cycle 159.7 + 159.8 give N=2, both bug-hunt). Try refactor /
  design-review / test-authoring families.

## Hippoagent fact-memory pointers (use `hippo_facts_search`)

- `project/hippoagent/cycle159-loop-final-summary-2026-05-19` →
  `c13baa4e3b3a` — overall loop summary of the cycle 159 sub-stack.
- `project/hippoagent/cycle159.7-scaling-experiment-armA-vs-armB-llm.py-2026-05-19`
  → `591a8ea5f8ce`.
- `project/hippoagent/cycle159.8-scaling-experiment2-opus-vs-team-2026-05-19`
  → `fe83f45b938d`.
- `lessons/cycle159-charter-v1-failure-echo-chamber-confabulation-2026-05-19`
  → `d604ffd5d6c6`.

## Operating rules for the next instance (from Aurelio v2)

- **O1 memoria-first**: `hippo_facts_recall` + `hippo_recall` BEFORE
  any new architecture decision. Cite the relevant lesson before
  acting against it.
- **A1 anti-confab**: never claim a file path / line number / commit
  without `Read`/`grep`/`git log` empirically. Two cycle-159 incidents
  showed both the team-lead and teammates can confabulate paths
  when the prompt is wrong.
- **A2 anti-halluc**: no "it works" without `pytest` exit-code 0 in
  hand.
- **A3 stop-check**: when Aurelio challenges scope, pause, read the
  freshest fact (`hippo_facts_recent`), then answer. The "old
  roadmap I had cached" pattern bit twice tonight.
- **O5 brevity**: 3-5 lines default in Italian; argumentative tasks
  may run long automatically.
- **O8 subagent**: read-only research OK; never delegate write
  operations. Personal `WebFetch`+`Read` per claim.

That's the handoff. Sleep well.
