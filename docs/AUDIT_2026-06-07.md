# Engram Functional Audit — 2026-06-07 (10-Opus adversarial, read-only)

> Run `wf_711709a4-66e` · 10 agents · 1.69M tok · file:line + empirically reproduced.
> Full JSON: tasks/wgmsb5yk1.output. This is the FIX backlog — close one at a time, TDD.

## Verdict
**NOT certainly-functional on a real (non-empty) corpus.** Three silent recall-loss
paths defeat the core promise (durable recall) — data stays on disk but vanishes from
recall with **zero error**, which for a memory engine = data loss. Plus security
half-wired (episode path unscreened, admission gate ignores its own reject verdicts),
durability holes (no flush-on-exit, non-atomic supersede_chain), and a **dead install**
(README path crashes: `mcp` not in core deps). Honest correction: the earlier
"engine is production-quality" claim was WRONG — this audit proves it.

## P0 (data-loss / crash / security — fix first)
- [ ] **P0-1 → re-scoped P1: missing model/corpus-mismatch guard** — `config.py:43/117` + `semantic.py:1417-1420/1661-1663`. VERIFIED read-only 2026-06-07: the LIVE corpus (`~/.engram/semantic|episodes|skills`) is **100% e5-base** (4605 facts / 547 ep / 326 skills, all 3072B) → it **matches** the active config → **NOT a blackout for the live system**. The audit's "100% legacy dark" was reproduced on a *clean/un-migrated* DB only (the maintainer's live corpus WAS migrated). Residual real bug: there is **no guard** if config-model ≠ corpus-model, so a NEW user, a stale env, or a restore of a `pre-flip` backup silently goes dark. Fix (defense, still worth it): boot/recall guard detects corpus model distribution vs active → auto-fallback or warn loudly; never force e5 on an un-migrated corpus. Also fix the stale `config.py:99-101` comment.
- [ ] **P0-2 90-day freshness drops everything** — `semantic.py:1591-1599/1684-1695/378/1177-1181`. Every default-provenance fact invisible 90d after creation, no opt-out, both recall paths. Fix: scope to capability-claim namespaces OR re-stamp `last_verified_at` on recall OR `include_stale`/`half_life` param.
- [ ] **P0-3 cold-defer clobber** — `semantic.py:1201-1219/1114-1135`. Re-store via `embed='auto'` (prod hippo_remember path) when daemon cold → emb=None → UPSERT overwrites good vector with `b''` → dropped from recall. Fix: omit/COALESCE embedding in DO UPDATE SET when emb is None (never degrade an embedded row).
- [ ] **P0-4 episode injection bypass** — `memory.py:478-496`. EpisodicMemory.store has NO injection screen (facts do); poisoned `final_answer` recalled verbatim. Fix: run `detect_injection` on task_text/final_answer, symmetric to SemanticMemory.store.
- [ ] **P0-5 install dead-on-arrival** — `pyproject.toml:38-70` + `mcp_server.py:51-54`. `mcp` only in extras; README primary install omits it → `engram mcp` ModuleNotFoundError → zero tools. Fix: move `mcp` to core deps (or lazy import + actionable error + fix README).

## P1 (reliability / contract / security-hardening)
- [ ] **P1-6 deferred write** — `semantic.py:162-184`. `ok_deferred` then swallows bg failure; daemon thread killed on restart → acknowledged write lost. Fix: atexit drain/join + surface `box['err']`.
- [ ] **P1-7 supersede_chain not crash-atomic** — `semantic.py:2248-2404/2087/2133`. Per-hop commits on separate connections → partial chain on crash despite "state unchanged" contract. Fix: single BEGIN/COMMIT on one connection.
- [ ] **P1-8 admission gate ignores verdicts** — `semantic.py:988-998`. Only ROUTE_TELEMETRY acted on; REJECT_POLLUTED/REJECT_DUPLICATE/FLAG_* computed then discarded. Fix: handle reject/flag verdicts.
- [ ] **P1-9 capability-token fail-open** — `syscall_bridge.py:248/260-261`. Token-less call skips auth; token-present skips peer-binding. Fix: fail-closed mesh ops + always pass peer_id_required.
- [ ] **P1-10 secrets not redacted on write** — `redaction.py` only called on transcript path; hippo_remember/record_episode store secrets verbatim. Fix: redact_secrets on proposition + task_text/final_answer.
- [ ] **P1-11 emerging-drafts raw INSERT** — `emerging_skill_register.py:162-178`. Writes `b''` embedding + bypasses store() gates → invisible to recall (its own purpose). Fix: route through SemanticMemory.store.
- [ ] **P1-12 null numeric arg crash** — `mcp_server.py:838`. Lenient schema accepts null → `int(None)` TypeError (~143 tools). Fix: `int(x or default)` / tolerant coerce.
- [ ] **P1-13 validation disabled w/o jsonschema** — `mcp_server.py:765/794-795/838`. Manual validator can't handle list-typed schemas; jsonschema undeclared. Fix: declare jsonschema core + fix _manual_validate for list types.
- [ ] **P1-14 duplicate tool def** — `mcp_server.py:3786/5308/8115/9498`. `hippo_find_stale_facts` advertised twice. Fix: delete one def + dead branch.

Dropped (speculative): 9. P2 (quality): 11.
